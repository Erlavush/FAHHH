---
phase: 01-shared-room-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sharedRoomTypes.ts
  - src/lib/sharedRoomSeed.ts
  - src/lib/sharedRoomSession.ts
  - tests/sharedRoomTypes.test.ts
  - tests/sharedRoomSession.test.ts
autonomous: true
requirements:
  - PAIR-01
  - ROOM-03
user_setup: []
must_haves:
  truths:
    - "Phase 1 has a shared-room document model that wraps RoomState instead of replacing it."
    - "The client has a persistent lightweight player profile and one active shared-room session record."
    - "A development room seed can be generated from the current room data while preserving inventory, anchors, and wall placements."
  artifacts:
    - path: "src/lib/sharedRoomTypes.ts"
      provides: "Shared room, invite, member, and profile domain types"
      min_lines: 60
      contains: "export interface SharedRoomDocument"
    - path: "src/lib/sharedRoomSeed.ts"
      provides: "RoomState-to-shared-room seed conversion helpers"
      min_lines: 40
      contains: "export function createSharedRoomSeed"
    - path: "src/lib/sharedRoomSession.ts"
      provides: "Persistent lightweight profile and one-room session helpers"
      min_lines: 50
      contains: "export function loadOrCreateSharedPlayerProfile"
    - path: "tests/sharedRoomTypes.test.ts"
      provides: "Shared-room domain regression coverage"
      contains: "describe(\"sharedRoomTypes\""
  key_links:
    - from: "src/lib/sharedRoomSeed.ts"
      to: "src/lib/roomState.ts"
      via: "cloneRoomState and RoomState imports"
      pattern: "from \"\\.\\/roomState\""
    - from: "src/lib/sharedRoomSession.ts"
      to: "src/lib/sharedRoomTypes.ts"
      via: "profile and session type imports"
      pattern: "from \"\\.\\/sharedRoomTypes\""
---

<objective>
Define the Phase 1 shared-room domain contract before any backend or UI integration starts.

Purpose: Give the later persistence and app-shell plans stable room, invite, profile, and session shapes that preserve the existing RoomState invariants.
Output: Shared-room domain types, seed conversion helpers, lightweight profile/session persistence, and targeted regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-room-backbone/01-CONTEXT.md
@.planning/phases/01-shared-room-backbone/01-RESEARCH.md
@src/lib/roomState.ts
@src/lib/devLocalState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared-room domain wrappers around RoomState</name>
  <files>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomSeed.ts</files>
  <read_first>src/lib/roomState.ts, .planning/phases/01-shared-room-backbone/01-CONTEXT.md, .planning/phases/01-shared-room-backbone/01-RESEARCH.md</read_first>
  <action>Create `src/lib/sharedRoomTypes.ts` with exact exported interfaces for `SharedPlayerProfile`, `SharedRoomMember`, `SharedRoomInvite`, `SharedRoomDocument`, and `SharedRoomSession`. `SharedRoomDocument` must wrap `roomState: RoomState` and include `roomId`, `memberIds`, `revision`, `sharedCoins`, `seedKind`, `createdAt`, and `updatedAt`. Create `src/lib/sharedRoomSeed.ts` with `buildSharedRoomOwnerId(roomId: string)` and `createSharedRoomSeed(roomId: string, sourceRoomState: RoomState)` that clones the incoming RoomState, rewrites `metadata.roomId` to the shared room id, rewrites every `ownedFurniture.ownerId` to `shared-room:${roomId}`, and preserves `ownedFurnitureId`, `anchorFurnitureId`, `surfaceLocalOffset`, `wall_front`, and `wall_right` placements without dropping data.</action>
  <verify>cmd /c npm test -- tests/sharedRoomTypes.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomTypes.ts contains "export interface SharedRoomDocument"
    - src/lib/sharedRoomTypes.ts contains "sharedCoins: number"
    - src/lib/sharedRoomSeed.ts contains "export function createSharedRoomSeed"
    - src/lib/sharedRoomSeed.ts contains "shared-room:${roomId}"
  </acceptance_criteria>
  <done>The repo has stable shared-room wrapper types and a seed helper that preserves current room invariants while reinterpreting ownership as room-owned.</done>
</task>

<task type="auto">
  <name>Task 2: Add persistent lightweight profile and one-room session helpers</name>
  <files>src/lib/sharedRoomSession.ts</files>
  <read_first>src/lib/devLocalState.ts, src/lib/sharedRoomTypes.ts, .planning/phases/01-shared-room-backbone/01-CONTEXT.md</read_first>
  <action>Create `src/lib/sharedRoomSession.ts` with browser-local helpers using exact keys `cozy-room-shared-profile-v1` and `cozy-room-shared-session-v1`. Export `loadOrCreateSharedPlayerProfile(displayName?: string)`, `saveSharedPlayerProfile(profile)`, `loadSharedRoomSession()`, `saveSharedRoomSession(session)`, and `clearSharedRoomSession()`. The stored profile must keep a stable `playerId` plus `displayName`, `createdAt`, and `updatedAt`. The stored session must represent one active partner/room only with `playerId`, `partnerId`, `roomId`, `inviteCode`, and `lastKnownRevision`, so saving a new session replaces the previous one instead of supporting multiple rooms.</action>
  <verify>cmd /c npm test -- tests/sharedRoomSession.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomSession.ts contains "const SHARED_ROOM_PROFILE_KEY = \"cozy-room-shared-profile-v1\""
    - src/lib/sharedRoomSession.ts contains "const SHARED_ROOM_SESSION_KEY = \"cozy-room-shared-session-v1\""
    - src/lib/sharedRoomSession.ts contains "export function loadOrCreateSharedPlayerProfile"
    - src/lib/sharedRoomSession.ts contains "export function clearSharedRoomSession"
  </acceptance_criteria>
  <done>The client has a lightweight persistent identity and a single active shared-room session contract that later plans can use for pairing and reconnect.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for seed preservation and session behavior</name>
  <files>tests/sharedRoomTypes.test.ts, tests/sharedRoomSession.test.ts</files>
  <read_first>tests/roomState.test.ts, src/lib/sharedRoomTypes.ts, src/lib/sharedRoomSeed.ts, src/lib/sharedRoomSession.ts</read_first>
  <action>Create `tests/sharedRoomTypes.test.ts` to assert that `createSharedRoomSeed` preserves placed furniture count, `ownedFurnitureId`, `anchorFurnitureId`, `surfaceLocalOffset`, and all wall-surface placements while changing only room-level identity fields and owner ids. Create `tests/sharedRoomSession.test.ts` to assert that `loadOrCreateSharedPlayerProfile` reuses the same `playerId` across reloads, `saveSharedRoomSession` overwrites the previous session, and `clearSharedRoomSession` removes the stored session cleanly.</action>
  <verify>cmd /c npm test -- tests/sharedRoomTypes.test.ts tests/sharedRoomSession.test.ts</verify>
  <acceptance_criteria>
    - tests/sharedRoomTypes.test.ts contains "preserves anchorFurnitureId"
    - tests/sharedRoomTypes.test.ts contains "wall_front"
    - tests/sharedRoomSession.test.ts contains "reuses the same playerId"
    - cmd /c npm test -- tests/sharedRoomTypes.test.ts tests/sharedRoomSession.test.ts exits 0
  </acceptance_criteria>
  <done>There is automated coverage for the shared-room seed contract and the lightweight profile/session persistence contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- tests/sharedRoomTypes.test.ts tests/sharedRoomSession.test.ts tests/roomState.test.ts
- [ ] cmd /c npm run build
- [ ] Shared-room types wrap `RoomState` instead of replacing it and do not touch Preview Studio or Mob Lab persistence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared-room identity, invite/session persistence, and dev seed conversion are defined in code before backend or UI work begins
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-room-backbone/01-01-SUMMARY.md`
</output>
