---
phase: 01-shared-room-backbone
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/App.tsx
  - src/app/components/InventoryPanel.tsx
  - src/app/components/SceneToolbar.tsx
  - src/app/components/SharedRoomEntryShell.tsx
  - src/app/components/SharedRoomStatusStrip.tsx
  - src/app/components/SharedRoomBlockingOverlay.tsx
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/styles.css
  - tests/sharedRoomRuntime.test.ts
autonomous: true
requirements:
  - PAIR-01
  - ROOM-01
  - ROOM-02
  - ROOM-03
user_setup: []
must_haves:
  truths:
    - "A user must create or join a shared room before entering the live room runtime."
    - "Both clients load the canonical shared RoomState and committed place, store, sell, remove, and buy actions round-trip through the shared-room store."
    - "The UI surfaces shared-room identity, sync/reload states, and shared inventory wording without implying live drag sync."
  artifacts:
    - path: "src/app/hooks/useSharedRoomRuntime.ts"
      provides: "Shared-room bootstrap, load, save, and reconnect orchestration"
      min_lines: 80
      contains: "export function useSharedRoomRuntime"
    - path: "src/app/components/SharedRoomEntryShell.tsx"
      provides: "Create/join invite-code shell"
      min_lines: 60
      contains: "Create Shared Room"
    - path: "src/app/components/SharedRoomStatusStrip.tsx"
      provides: "Room identity and sync-state indicator"
      min_lines: 30
      contains: "Shared room updated"
    - path: "src/app/components/InventoryPanel.tsx"
      provides: "Shared inventory wording"
      contains: "Shared Inventory"
    - path: "tests/sharedRoomRuntime.test.ts"
      provides: "Shared runtime regression coverage"
      contains: "commits only confirmed room changes"
  key_links:
    - from: "src/App.tsx"
      to: "src/app/hooks/useSharedRoomRuntime.ts"
      via: "shared-room bootstrap and commit callbacks"
      pattern: "useSharedRoomRuntime"
    - from: "src/App.tsx"
      to: "src/lib/sharedRoomClient.ts"
      via: "canonical room load and commit path"
      pattern: "commitSharedRoomState|loadSharedRoom"
    - from: "src/app/components/SharedRoomStatusStrip.tsx"
      to: "src/styles.css"
      via: "shared-room status classes"
      pattern: "shared-room-status"
---

<objective>
Integrate the shared-room runtime into the app shell and room editor without regressing the existing room-builder behavior.

Purpose: Move the live room from solo-local authority to Phase 1's invite-based shared-room flow and canonical committed-edit sync.
Output: Pair/create/join UI, shared-room runtime hook, canonical room load/save integration in App, shared inventory wording, reconnect/save status UI, and runtime regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-room-backbone/01-CONTEXT.md
@.planning/phases/01-shared-room-backbone/01-RESEARCH.md
@.planning/phases/01-shared-room-backbone/01-UI-SPEC.md
@src/App.tsx
@src/app/components/InventoryPanel.tsx
@src/app/components/SceneToolbar.tsx
@src/app/hooks/useSandboxInventory.ts
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the create/join shell and shared-room bootstrap hook</name>
  <files>src/App.tsx, src/app/hooks/useSharedRoomRuntime.ts, src/app/components/SharedRoomEntryShell.tsx, src/app/components/SharedRoomBlockingOverlay.tsx</files>
  <read_first>src/App.tsx, src/lib/sharedRoomClient.ts, src/lib/sharedRoomSession.ts, .planning/phases/01-shared-room-backbone/01-CONTEXT.md, .planning/phases/01-shared-room-backbone/01-UI-SPEC.md</read_first>
  <action>Create `src/app/hooks/useSharedRoomRuntime.ts` to own profile/session bootstrap, create-room, join-room, canonical room load, reconnect reload, save state, and blocking error state. Create `src/app/components/SharedRoomEntryShell.tsx` as the centered pairing shell using the locked copy `Start your room together`, `Create Shared Room`, `Join with Code`, `Room ready to share`, and the invite helper text from the UI spec. Create `src/app/components/SharedRoomBlockingOverlay.tsx` for the blocking states `Loading shared room...`, `Reloading latest room...`, and the locked reload failure copy. Update `src/App.tsx` so the live room does not render until `useSharedRoomRuntime` resolves a canonical shared room, making pairing the main runtime path.</action>
  <verify>cmd /c npm test -- tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useSharedRoomRuntime.ts contains "export function useSharedRoomRuntime"
    - src/app/components/SharedRoomEntryShell.tsx contains "Create Shared Room"
    - src/app/components/SharedRoomEntryShell.tsx contains "Join with Code"
    - src/app/components/SharedRoomBlockingOverlay.tsx contains "We couldn't load the shared room. Retry to fetch the latest room state."
    - src/App.tsx contains "useSharedRoomRuntime"
  </acceptance_criteria>
  <done>The app boots into a shared-room create/join flow and blocks on canonical room load instead of entering the solo-local room by default.</done>
</task>

<task type="auto">
  <name>Task 2: Route committed room and shared inventory mutations through the canonical store</name>
  <files>src/App.tsx, src/app/components/InventoryPanel.tsx, src/app/components/SceneToolbar.tsx, src/app/components/SharedRoomStatusStrip.tsx</files>
  <read_first>src/App.tsx, src/lib/roomPlacementEquality.ts, src/app/components/InventoryPanel.tsx, src/app/components/SceneToolbar.tsx, .planning/phases/01-shared-room-backbone/01-CONTEXT.md</read_first>
  <action>Update `src/App.tsx` so only committed mutations write through the shared-room store. `onFurnitureSnapshotChange` must remain local-only for drag previews, while `handleCommittedFurnitureChange`, `handleBuyFurniture`, `handleSellStoredFurniture`, room-store/remove flows, and canonical reloads must call the shared-room commit helper with the latest `expectedRevision` and then adopt the returned canonical `roomState` and `sharedCoins`. Keep camera position, player position, and world settings local; do not write shared room contents back through `savePersistedSandboxState`. Create `src/app/components/SharedRoomStatusStrip.tsx` near the toolbar to show room identity plus the exact transient texts `Saving shared room...`, `Shared room updated`, and `Reloading latest room...`. Update `src/app/components/InventoryPanel.tsx` to use the player-facing label `Shared Inventory` and shared-inventory copy instead of solo-room phrasing.</action>
  <verify>cmd /c npm test -- tests/sharedRoomRuntime.test.ts tests/useRoomFurnitureEditor.test.ts</verify>
  <acceptance_criteria>
    - src/app/components/InventoryPanel.tsx contains "Shared Inventory"
    - src/app/components/SharedRoomStatusStrip.tsx contains "Saving shared room..."
    - src/app/components/SharedRoomStatusStrip.tsx contains "Shared room updated"
    - src/App.tsx contains "handleCommittedFurnitureChange"
    - src/App.tsx does not add shared-room commits inside "onFurnitureSnapshotChange"
  </acceptance_criteria>
  <done>Committed edits and shared inventory changes round-trip through the canonical store, while transient drag state remains local-only.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared-room styling and runtime regression coverage</name>
  <files>src/styles.css, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/styles.css, src/app/components/SharedRoomEntryShell.tsx, src/app/components/SharedRoomStatusStrip.tsx, .planning/phases/01-shared-room-backbone/01-UI-SPEC.md</read_first>
  <action>Extend `src/styles.css` with dedicated classes for the pairing shell, invite code card, blocking overlay, and shared status strip using the Phase 1 UI spec colors `#F4EADF`, `#273241`, `#6FAF83`, and `#C74F5A`, plus the serif/sans/monospace font assignments from the UI contract. Add `tests/sharedRoomRuntime.test.ts` that exercises pure runtime helpers or extracted shared-room state logic to prove three behaviors: paired bootstrap loads the canonical room, reconnect reload discards unconfirmed local edits, and commits happen only for confirmed room changes rather than snapshot/drag updates.</action>
  <verify>cmd /c npm test -- tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/styles.css contains ".shared-room-entry"
    - src/styles.css contains ".shared-room-status"
    - src/styles.css contains "#6FAF83"
    - tests/sharedRoomRuntime.test.ts contains "commits only confirmed room changes"
    - cmd /c npm test -- tests/sharedRoomRuntime.test.ts exits 0
  </acceptance_criteria>
  <done>The Phase 1 shared-room UI contract is represented in code and backed by runtime-focused regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test
- [ ] cmd /c npm run build
- [ ] The paired shared room is now the app's main runtime path and canonical reload happens before showing stale room state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app loads one canonical shared room, keeps committed edits authoritative, and uses Phase 1 shared-room copy and status surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-room-backbone/01-03-SUMMARY.md`
</output>
