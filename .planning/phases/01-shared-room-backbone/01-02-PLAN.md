---
phase: 01-shared-room-backbone
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .gitignore
  - vite.config.js
  - src/lib/sharedRoomStore.ts
  - src/lib/sharedRoomClient.ts
  - src/lib/sharedRoomValidation.ts
  - scripts/sharedRoomDevPlugin.mjs
  - tests/sharedRoomValidation.test.ts
  - tests/sharedRoomStore.test.ts
autonomous: true
requirements:
  - ROOM-01
  - ROOM-03
user_setup: []
must_haves:
  truths:
    - "The frontend talks to a replaceable shared-room store interface instead of treating localStorage as the shared backend."
    - "The development backend persists authoritative shared-room documents in a local file-backed store."
    - "Shared-room validation accepts all six valid placement surfaces and rejects malformed room documents before write."
  artifacts:
    - path: "src/lib/sharedRoomStore.ts"
      provides: "Replaceable shared-room store interface"
      min_lines: 40
      contains: "export interface SharedRoomStore"
    - path: "src/lib/sharedRoomClient.ts"
      provides: "Browser client for the dev shared-room API"
      min_lines: 40
      contains: "/api/dev/shared-room"
    - path: "src/lib/sharedRoomValidation.ts"
      provides: "Shared-room document validation with full wall support"
      min_lines: 50
      contains: "wall_front"
    - path: "scripts/sharedRoomDevPlugin.mjs"
      provides: "Vite dev middleware with file-backed persistence"
      min_lines: 80
      contains: "shared-room-dev-db.json"
    - path: "tests/sharedRoomStore.test.ts"
      provides: "Authoritative persistence regression coverage"
      contains: "last write wins"
  key_links:
    - from: "vite.config.js"
      to: "scripts/sharedRoomDevPlugin.mjs"
      via: "Vite plugin import"
      pattern: "sharedRoomDevPlugin"
    - from: "src/lib/sharedRoomClient.ts"
      to: "/api/dev/shared-room"
      via: "fetch requests"
      pattern: "/api/dev/shared-room"
    - from: "scripts/sharedRoomDevPlugin.mjs"
      to: "src/lib/sharedRoomValidation.ts"
      via: "shared document validation before write"
      pattern: "sharedRoomValidation"
---

<objective>
Add the authoritative shared-room persistence boundary and a development-only file-backed implementation.

Purpose: Replace the current browser-local room authority with a transport and storage layer that can later swap to a real backend without rewriting the room schema.
Output: Shared-room store/client modules, a Vite-backed file store, full shared-room validation, and regression tests for revisioned commits.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-room-backbone/01-CONTEXT.md
@.planning/phases/01-shared-room-backbone/01-RESEARCH.md
@src/lib/sharedRoomTypes.ts
@src/lib/sharedRoomSession.ts
@src/lib/roomState.ts
@src/lib/devLocalState.ts
@vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared-room store interface and browser client</name>
  <files>src/lib/sharedRoomStore.ts, src/lib/sharedRoomClient.ts</files>
  <read_first>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomSession.ts, .planning/phases/01-shared-room-backbone/01-RESEARCH.md</read_first>
  <action>Create `src/lib/sharedRoomStore.ts` with the exact `SharedRoomStore` interface and request/response types for `createSharedRoom`, `joinSharedRoom`, `loadSharedRoom`, and `commitSharedRoomState`. The commit request must include `roomId`, `expectedRevision`, `roomState`, `sharedCoins`, and a string `reason` so Phase 1 sync stays at the committed-room boundary. Create `src/lib/sharedRoomClient.ts` with fetch helpers targeting `/api/dev/shared-room/create`, `/api/dev/shared-room/join`, `/api/dev/shared-room/room/{roomId}`, and `/api/dev/shared-room/commit`, and make every client method return the canonical `SharedRoomDocument` from the server instead of mutating localStorage directly.</action>
  <verify>cmd /c npm test -- tests/sharedRoomStore.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomStore.ts contains "export interface SharedRoomStore"
    - src/lib/sharedRoomStore.ts contains "commitSharedRoomState"
    - src/lib/sharedRoomClient.ts contains "/api/dev/shared-room/create"
    - src/lib/sharedRoomClient.ts contains "/api/dev/shared-room/commit"
  </acceptance_criteria>
  <done>The frontend has a replaceable API boundary for shared-room operations instead of depending on browser-local room persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Add a Vite-backed local file store and complete shared-room validation</name>
  <files>.gitignore, vite.config.js, src/lib/sharedRoomValidation.ts, scripts/sharedRoomDevPlugin.mjs</files>
  <read_first>.gitignore, vite.config.js, src/lib/devLocalState.ts, src/lib/roomState.ts, src/lib/sharedRoomTypes.ts</read_first>
  <action>Add `.data/` to `.gitignore`, then create `scripts/sharedRoomDevPlugin.mjs` and wire it into `vite.config.js` so `vite dev` exposes a development-only `/api/dev/shared-room/*` API. Back the API with the exact file `.data/shared-room-dev-db.json` containing top-level `profiles`, `invites`, and `rooms` collections. Create `src/lib/sharedRoomValidation.ts` as the shared-authority validator instead of reusing `devLocalState.ts` directly. The validator must accept the exact placement surfaces `floor`, `wall_back`, `wall_left`, `wall_front`, `wall_right`, and `surface`; reject malformed room documents with a 400 response; and require `SharedRoomDocument.revision` to increment on every successful commit. In the plugin, a later accepted commit becomes canonical and the response body must return the full canonical document for the client to reload.</action>
  <verify>cmd /c npm test -- tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts</verify>
  <acceptance_criteria>
    - .gitignore contains ".data/"
    - src/lib/sharedRoomValidation.ts contains "wall_front"
    - src/lib/sharedRoomValidation.ts contains "wall_right"
    - scripts/sharedRoomDevPlugin.mjs contains "shared-room-dev-db.json"
    - vite.config.js contains "sharedRoomDevPlugin"
  </acceptance_criteria>
  <done>The repo can run a local authoritative shared-room API with file-backed persistence and complete room validation during development.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for validation, invite lifecycle, and canonical commits</name>
  <files>tests/sharedRoomValidation.test.ts, tests/sharedRoomStore.test.ts</files>
  <read_first>tests/roomState.test.ts, src/lib/sharedRoomValidation.ts, src/lib/sharedRoomStore.ts, scripts/sharedRoomDevPlugin.mjs</read_first>
  <action>Create `tests/sharedRoomValidation.test.ts` to assert that shared-room validation accepts valid `wall_front` and `wall_right` placements and rejects unknown surfaces. Create `tests/sharedRoomStore.test.ts` to exercise the pure store helpers or plugin-backed handlers for invite creation, one-time invite consumption, room load, revision increment, and a "last write wins" commit sequence where the second accepted commit is the canonical document returned on reload.</action>
  <verify>cmd /c npm test -- tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts</verify>
  <acceptance_criteria>
    - tests/sharedRoomValidation.test.ts contains "accepts wall_front and wall_right placements"
    - tests/sharedRoomStore.test.ts contains "last write wins"
    - tests/sharedRoomStore.test.ts contains "consumes invite only once"
    - cmd /c npm test -- tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts exits 0
  </acceptance_criteria>
  <done>Phase 1 has automated coverage for the wall-surface validation gap and the canonical shared-room persistence rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test
- [ ] cmd /c npm run build
- [ ] The browser client no longer depends on `savePersistedSandboxState` as the authoritative shared-room backend path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A replaceable shared-room adapter exists and the development backend persists authoritative room documents with full wall-surface validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-room-backbone/01-02-SUMMARY.md`
</output>
