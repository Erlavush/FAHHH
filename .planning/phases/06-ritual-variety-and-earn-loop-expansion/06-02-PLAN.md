---
phase: 06-ritual-variety-and-earn-loop-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/pcMinigame.ts
  - src/lib/sharedProgression.ts
  - src/components/PcMinigameOverlay.tsx
  - src/App.tsx
  - src/app/hooks/useSharedRoomPresence.ts
  - tests/pcMinigame.test.ts
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomPresenceUx.test.ts
autonomous: true
requirements:
  - RITL-02
  - ACTV-01
user_setup: []
must_haves:
  truths:
    - "Desk PC exposes three retro apps with independent once-per-room-day reward claims and unlimited practice play."
    - "Cozy Rest grants its payout only when both partners are lying together on the same bed and the reward is committed canonically."
    - "All activity rewards still flow through commitRoomMutation and survive reload or conflict replay."
  artifacts:
    - path: "src/components/PcMinigameOverlay.tsx"
      provides: "Retro desk-PC desktop with Snake, Block Stacker, and Runner app flow"
      min_lines: 250
      contains: "Block Stacker"
    - path: "src/lib/pcMinigame.ts"
      provides: "Per-app desk-PC definitions and normalized result helpers"
      min_lines: 90
      contains: "export type PcDeskActivityId = \"pc_snake\" | \"pc_block_stacker\" | \"pc_runner\""
    - path: "tests/sharedRoomRuntime.test.ts"
      provides: "Canonical reward-commit coverage for desk-PC apps and Cozy Rest"
      contains: "cozy_rest"
  key_links:
    - from: "src/components/PcMinigameOverlay.tsx"
      to: "src/App.tsx"
      via: "retro app results return activity ids and reward payloads into canonical commitRoomMutation flow"
      pattern: "activityId|onComplete|pc_game_reward"
    - from: "src/App.tsx"
      to: "src/lib/sharedProgression.ts"
      via: "desk-PC and Cozy Rest mutations use the shared activity-completion helper"
      pattern: "applySharedActivityCompletionToProgression|cozy_rest_reward"
    - from: "src/app/hooks/useSharedRoomPresence.ts"
      to: "tests/sharedRoomPresenceUx.test.ts"
      via: "same-bed / different-slot Cozy Rest eligibility depends on stable presence pose data"
      pattern: "slotId|furnitureId|pose"
---

<objective>
Implement the Phase 06 activity surfaces and canonical reward paths.

Purpose: expand the desk PC from one cooldown minigame into a retro three-app suite, and add a bed-based `Cozy Rest` earn loop that uses canonical hosted progression commits.
Output: retro desk-PC desktop/app flow, activity-id-based reward commits, and a non-PC couple reward path beyond the desk PC.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-01-PLAN.md
@src/lib/sharedProgression.ts
@src/lib/pcMinigame.ts
@src/components/PcMinigameOverlay.tsx
@src/App.tsx
@src/app/hooks/useSharedRoomPresence.ts
@src/components/room-view/useRoomViewInteractions.ts
@tests/sharedRoomRuntime.test.ts
@tests/sharedRoomPresenceUx.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the single desk-PC shift with a retro three-app desktop and per-app helper contract</name>
  <files>src/lib/pcMinigame.ts, src/components/PcMinigameOverlay.tsx, tests/pcMinigame.test.ts</files>
  <read_first>src/lib/pcMinigame.ts, src/components/PcMinigameOverlay.tsx, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md, src/App.tsx</read_first>
  <behavior>
    - Test 1: the desk-PC helper exposes exactly three app ids and each app can compute its own reward output from a score
    - Test 2: the overlay lets the player launch `Snake`, `Block Stacker`, and `Runner` from one retro desktop shell
    - Test 3: the result flow can show a paid run or a practice-only run without blocking replay after payout is spent
  </behavior>
  <action>Create `tests/pcMinigame.test.ts` first. In `src/lib/pcMinigame.ts`, replace the single-app helper with the exact exports `PcDeskActivityId = "pc_snake" | "pc_block_stacker" | "pc_runner"`, `PcDeskRunResult`, `PcDeskAppDefinition`, `getPcDeskAppDefinitions()`, `getPcDeskRewardCoins(activityId, score)`, and `createDefaultPcDeskProgress()`. In `src/components/PcMinigameOverlay.tsx`, turn the current modal into a retro desktop shell with three launch targets labeled exactly `Snake`, `Block Stacker`, and `Runner`, pixel-style window chrome, and one normalized `onComplete` callback returning `activityId`, `score`, `rewardCoins`, and `completedAt`. The overlay must keep `Escape`/close behavior, must allow replay even when today's payout is already spent, and must show exact result copy `Paid today` or `Practice run only` instead of the old one-minute cooldown framing.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/pcMinigame.test.ts</verify>
  <acceptance_criteria>
    - src/lib/pcMinigame.ts contains "export type PcDeskActivityId = \"pc_snake\" | \"pc_block_stacker\" | \"pc_runner\""
    - src/lib/pcMinigame.ts contains "export function getPcDeskAppDefinitions"
    - src/components/PcMinigameOverlay.tsx contains "Snake"
    - src/components/PcMinigameOverlay.tsx contains "Block Stacker"
    - src/components/PcMinigameOverlay.tsx contains "Practice run only"
    - tests/pcMinigame.test.ts contains "returns the three retro desk app ids"
  </acceptance_criteria>
  <done>The desk PC now feels like one retro bedroom computer with three apps instead of one modern cooldown minigame.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire desk-PC rewards through activity-id-based canonical commits</name>
  <files>src/lib/sharedProgression.ts, src/App.tsx, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/lib/sharedProgression.ts, src/App.tsx, src/lib/pcMinigame.ts, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-01-PLAN.md, tests/sharedRoomRuntime.test.ts</read_first>
  <behavior>
    - Test 1: each desk-PC app can grant coins once per room day for the active player
    - Test 2: replaying the same desk-PC app after payout is spent still records a run but does not add extra coins
    - Test 3: app-specific reward claim state survives a shared-room reload and revision replay
  </behavior>
  <action>Update `src/lib/sharedProgression.ts` so `applySharedActivityCompletionToProgression` supports the exact desk-PC ids `pc_snake`, `pc_block_stacker`, and `pc_runner`, keeps per-app best score / last score / games played data for the active player, and returns explicit `payoutGranted` plus `paidToday` state. Update `src/App.tsx` so desk-PC runs commit through `commitRoomMutation` with the exact reason strings `pc_game_reward:pc_snake`, `pc_game_reward:pc_block_stacker`, and `pc_game_reward:pc_runner`, and so the overlay receives canonical `paidToday` data from progression instead of the old short cooldown gate. Expand `tests/sharedRoomRuntime.test.ts` so one partner can claim each PC app once per room day, replay it without extra payout, and keep the claim state after reload.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/App.tsx contains "pc_game_reward:pc_snake"
    - src/App.tsx contains "pc_game_reward:pc_block_stacker"
    - src/App.tsx contains "pc_game_reward:pc_runner"
    - src/lib/sharedProgression.ts contains "paidToday"
    - tests/sharedRoomRuntime.test.ts contains "does not pay the same desk app twice in one room day"
    - tests/sharedRoomRuntime.test.ts contains "keeps desk app claim state after reload"
  </acceptance_criteria>
  <done>Desk-PC rewards are now canonical, app-specific, and once-per-room-day without blocking repeat play.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add bed-based Cozy Rest as the non-PC canonical earn loop</name>
  <files>src/App.tsx, src/app/hooks/useSharedRoomPresence.ts, tests/sharedRoomPresenceUx.test.ts, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/App.tsx, src/app/hooks/useSharedRoomPresence.ts, src/components/room-view/useRoomViewInteractions.ts, src/lib/sharedProgression.ts, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md, tests/sharedRoomPresenceUx.test.ts, tests/sharedRoomRuntime.test.ts</read_first>
  <behavior>
    - Test 1: Cozy Rest becomes eligible only when both partners are lying on the same bed and occupy different slots
    - Test 2: claiming Cozy Rest commits one couple-wide room-day reward and cannot double-pay that day
    - Test 3: Cozy Rest claim state survives reload and does not break normal bed interaction behavior
  </behavior>
  <action>Reuse the existing interaction pose data in `src/app/hooks/useSharedRoomPresence.ts` and the current room interaction plumbing to derive `Cozy Rest` eligibility in `src/App.tsx` when both partners have `pose.type === "lie"`, share the same `furnitureId`, and occupy different `slotId` values. Add one exact canonical commit path with reason `cozy_rest_reward` that calls `applySharedActivityCompletionToProgression` using `activityId: "cozy_rest"` and `claimMode: "couple"`. Expose exact player-facing copy `Cozy Rest` for the action/status, keep the bed interaction itself reusable after payout is spent, and expand `tests/sharedRoomPresenceUx.test.ts` plus `tests/sharedRoomRuntime.test.ts` so same-bed / different-slot detection, no-double-claim behavior, and reload persistence are covered.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresenceUx.test.ts tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/App.tsx contains "cozy_rest_reward"
    - src/App.tsx contains "Cozy Rest"
    - src/app/hooks/useSharedRoomPresence.ts contains "slotId"
    - tests/sharedRoomPresenceUx.test.ts contains "Cozy Rest"
    - tests/sharedRoomRuntime.test.ts contains "cozy_rest"
    - tests/sharedRoomRuntime.test.ts contains "does not pay Cozy Rest twice in one room day"
  </acceptance_criteria>
  <done>The room now has one canonical non-PC earn loop that fits the couple comfort-space fantasy and reuses the strongest existing interaction surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/pcMinigame.test.ts tests/sharedRoomRuntime.test.ts tests/sharedRoomPresenceUx.test.ts
- [ ] Desk PC offers three retro apps with once-per-room-day payouts and repeatable practice play
- [ ] Cozy Rest grants one couple-wide room-day reward through canonical shared progression
</verification>

<success_criteria>
Phase 06 now has both halves of the return loop: a retro multi-app desk PC and one room-native non-PC earn activity beyond the desk path.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-02-SUMMARY.md`
</output>
