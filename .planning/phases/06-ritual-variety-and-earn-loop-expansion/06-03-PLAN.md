---
phase: 06-ritual-variety-and-earn-loop-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/app/shellViewModel.ts
  - src/app/components/SharedRoomStatusStrip.tsx
  - src/components/ui/PlayerCompanionCard.tsx
  - src/components/ui/PlayerProgressStack.tsx
  - src/components/ui/PlayerRoomDetailsSheet.tsx
  - src/App.tsx
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - RITL-02
  - ACTV-01
user_setup: []
must_haves:
  truths:
    - "The player shell replaces Streak with Together Days and explains visit/activity availability without reintroducing debug clutter."
    - "Desk-PC retro styling stays scoped to the PC/apps while the main room shell remains cozy and room-first."
    - "Companion card, status strip, and room details all reflect canonical activity claim state instead of local guesswork."
  artifacts:
    - path: "src/app/shellViewModel.ts"
      provides: "Together Days and activity-status shell selectors"
      min_lines: 180
      contains: "togetherDaysLabel"
    - path: "src/components/ui/PlayerProgressStack.tsx"
      provides: "Player progress card with Together Days instead of Streak"
      min_lines: 25
      contains: "Together Days"
    - path: "src/components/ui/PlayerRoomDetailsSheet.tsx"
      provides: "Room details surface that lists today's activity availability"
      min_lines: 100
      contains: "Today's activities"
  key_links:
    - from: "src/app/shellViewModel.ts"
      to: "src/components/ui/PlayerCompanionCard.tsx"
      via: "Together Days and activity-status fields feed the main companion shell card"
      pattern: "togetherDaysLabel|deskActivityStatus|cozyRestStatus"
    - from: "src/app/shellViewModel.ts"
      to: "src/components/ui/PlayerRoomDetailsSheet.tsx"
      via: "details sheet receives canonical activity rows and visit-state summary"
      pattern: "activityRows|visitStatusLabel"
    - from: "src/App.tsx"
      to: "src/app/components/SharedRoomStatusStrip.tsx"
      via: "status strip and companion shell read the same canonical Together Days and activity state"
      pattern: "Together Days|Desk PC|Cozy Rest"
---

<objective>
Surface the new Phase 06 loop clearly in the player shell without losing the cozy room-first feel.

Purpose: replace streak copy with Together Days, show visit/activity readiness state in the active shell surfaces, and keep the retro treatment scoped to the desk PC/apps only.
Output: updated shell selectors, active `src/components/ui/*` surfaces, and App wiring for canonical Together Days plus activity-status UX.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-01-PLAN.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-02-PLAN.md
@src/app/shellViewModel.ts
@src/app/components/SharedRoomStatusStrip.tsx
@src/components/ui/PlayerCompanionCard.tsx
@src/components/ui/PlayerProgressStack.tsx
@src/components/ui/PlayerRoomDetailsSheet.tsx
@src/App.tsx
@tests/shellViewModel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace streak-facing shell selectors with Together Days and activity-status state</name>
  <files>src/app/shellViewModel.ts, tests/shellViewModel.test.ts</files>
  <read_first>src/app/shellViewModel.ts, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md, src/lib/sharedProgression.ts, tests/shellViewModel.test.ts</read_first>
  <behavior>
    - Test 1: the shell selector exposes `Together Days` instead of `Streak`
    - Test 2: the companion card state can distinguish visit status, desk-PC payout status, and Cozy Rest payout status
    - Test 3: the details sheet state can render a list of today's activities without introducing developer-only language
  </behavior>
  <action>In `src/app/shellViewModel.ts`, replace the old `streakLabel` field with the exact field `togetherDaysLabel`. Keep the existing partner-presence titles, but change the ritual-facing state to non-punitive wording and add the exact fields `visitStatusLabel`, `deskActivityStatus`, and `cozyRestStatus` to `PlayerCompanionCardState`. Extend `PlayerRoomDetailsState` with the exact field `activityRows: { label: string; value: string }[]`. Update `tests/shellViewModel.test.ts` to assert the exact strings `Together Days`, `Visited today`, `Paid today`, and `Ready now` from the selector output where appropriate.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/app/shellViewModel.ts contains "togetherDaysLabel"
    - src/app/shellViewModel.ts contains "visitStatusLabel"
    - src/app/shellViewModel.ts contains "deskActivityStatus"
    - src/app/shellViewModel.ts contains "cozyRestStatus"
    - tests/shellViewModel.test.ts contains "Together Days"
    - tests/shellViewModel.test.ts contains "Paid today"
  </acceptance_criteria>
  <done>The shell selector layer now speaks the new Together Days and activity-availability language instead of the old streak contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update the active player-shell surfaces and App wiring to show canonical activity state</name>
  <files>src/components/ui/PlayerCompanionCard.tsx, src/components/ui/PlayerProgressStack.tsx, src/components/ui/PlayerRoomDetailsSheet.tsx, src/app/components/SharedRoomStatusStrip.tsx, src/App.tsx</files>
  <read_first>src/components/ui/PlayerCompanionCard.tsx, src/components/ui/PlayerProgressStack.tsx, src/components/ui/PlayerRoomDetailsSheet.tsx, src/app/components/SharedRoomStatusStrip.tsx, src/App.tsx, src/app/shellViewModel.ts, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md</read_first>
  <behavior>
    - The progress stack shows `Together Days` instead of `Streak`
    - The companion card shows separate status lines for `Desk PC` and `Cozy Rest`
    - The room details sheet lists today's activities and their exact readiness / paid state
  </behavior>
  <action>Update `src/components/ui/PlayerProgressStack.tsx` so the third card label is exactly `Together Days` and it no longer renders the old `Streak` copy. Update `src/components/ui/PlayerCompanionCard.tsx` so it renders `togetherDaysLabel` plus two exact activity rows labeled `Desk PC` and `Cozy Rest`. Update `src/components/ui/PlayerRoomDetailsSheet.tsx` so it renders a card titled exactly `Today's activities` from `activityRows`. Update `src/app/components/SharedRoomStatusStrip.tsx` and `src/App.tsx` so the status strip, progress stack, and companion card all receive canonical Together Days, visit, desk-PC, and Cozy Rest state from progression. Keep the retro styling scoped to `src/components/PcMinigameOverlay.tsx`; do not move pixel-window chrome into the main shell surfaces.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/components/ui/PlayerProgressStack.tsx contains "Together Days"
    - src/components/ui/PlayerCompanionCard.tsx contains "Desk PC"
    - src/components/ui/PlayerCompanionCard.tsx contains "Cozy Rest"
    - src/components/ui/PlayerRoomDetailsSheet.tsx contains "Today's activities"
    - src/app/components/SharedRoomStatusStrip.tsx contains "Together Days"
    - src/App.tsx contains "togetherDaysLabel"
  </acceptance_criteria>
  <done>The player shell now explains the new return loop quietly and clearly without turning into a retro dashboard or debug overlay.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts
- [ ] cmd /c npm run build
- [ ] The player shell shows Together Days, Desk PC status, and Cozy Rest status without any streak-pressure copy
- [ ] Retro styling remains scoped to the desk PC/apps only
</verification>

<success_criteria>
Phase 06 now reads clearly in the room shell: Together Days replaces streak pressure, the desk PC and Cozy Rest loops are visible, and the room still feels like a comfort space first.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-03-SUMMARY.md`
</output>
