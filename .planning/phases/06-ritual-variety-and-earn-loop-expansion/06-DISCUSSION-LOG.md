# Phase 6 Discussion Log

**Date:** 2026-03-27
**Status:** Completed during `$gsd-discuss-phase 6`

## Topics Covered

### Ritual cadence and shared-presence metric
- User rejected punitive streaks and daily-pressure mechanics.
- User defined the soft shared-presence rule as: each room-day counts once when both partners visit within that room-day.
- User explicitly clarified that simultaneous overlap is **not** required; both visiting within the same room-day is enough.
- User wants the metric framed as a sentimental relationship marker rather than a reward faucet.

### Desk PC ritual variety
- User wants ritual variety to live inside the desk PC.
- User wants the desk PC/apps to feel aggressively `1990s` / retro / pixelated.
- User explicitly scoped the retro treatment to the desk PC/apps only, not the entire room shell.
- User requested three desk-PC game variants:
  - a snake-style game
  - a simple falling-block game inspired by classic block stackers
  - a runner inspired by the offline browser dinosaur game
- User explicitly wants each PC game to pay out once per day.

### Additional earn loop beyond the PC
- User confirmed Phase 6 should include a non-PC earn loop in addition to the desk PC suite.
- User did not name a final non-PC activity during discussion.

### Player-shell surfacing
- User confirmed the room should surface the new loop state in the shell without turning the whole shell into retro UI.

## Planning Defaults Locked By Orchestrator

The user moved directly from discussion into `$gsd-plan-phase 6` before the final follow-up batch on unresolved details. To keep planning moving, the following defaults were locked into `06-CONTEXT.md` as planning defaults rather than user-explicit requirements:

- Featured ritual rotation uses one optional featured activity per room-day from the activity pool.
- The non-PC earn loop defaults to `Cozy Rest`, a bed-based couple activity that reuses the existing two-slot lie interaction.
- `Cozy Rest` payout requires both partners to be present together for the activity.
- `Together Days` is sentimental/progress-facing only; rewards come from activities, not from the counter.
- The player shell replaces streak-facing copy with `Together Days` and shows visit/activity availability through the existing companion/progress/details surfaces.

## Outcome

Discussion produced enough direction to proceed into planning for:
- a soft `Together Days` metric
- a multi-app retro desk PC ritual suite
- one additional repeatable non-PC earn loop
- shell updates that explain availability and reset state without debug clutter

Open implementation details remain intentionally at planner discretion, including payout values, exact rotation logic, exact copy, and exact retro visual implementation.
