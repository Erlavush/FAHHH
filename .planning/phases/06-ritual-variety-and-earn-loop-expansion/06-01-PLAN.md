---
phase: 06-ritual-variety-and-earn-loop-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sharedProgressionTypes.ts
  - src/lib/sharedProgression.ts
  - tests/sharedProgression.test.ts
autonomous: true
requirements:
  - RITL-02
  - ACTV-01
user_setup: []
must_haves:
  truths:
    - "Together Days increments once when both partners visit the same room-day and never resets after missed days."
    - "Featured ritual selection and activity reward claims live in canonical shared progression state, not in ephemeral presence state."
    - "Legacy streak-based rooms migrate into the new Together Days fields without blanking existing sentimental progress."
  artifacts:
    - path: "src/lib/sharedProgressionTypes.ts"
      provides: "Phase 06 canonical activity, visit, and Together Days progression types"
      min_lines: 80
      contains: "export type SharedRoomActivityId = \"pc_snake\" | \"pc_block_stacker\" | \"pc_runner\" | \"cozy_rest\""
    - path: "src/lib/sharedProgression.ts"
      provides: "Pure room-day visit, featured activity, and reward-claim helpers"
      min_lines: 350
      contains: "export function recordSharedRoomVisit"
    - path: "tests/sharedProgression.test.ts"
      provides: "Together Days and activity-claim regression coverage"
      contains: "increments Together Days once when both partners visit the same room day"
  key_links:
    - from: "src/lib/sharedProgressionTypes.ts"
      to: "src/lib/sharedProgression.ts"
      via: "shared activity ids, visit-day state, and room-day claim records drive pure progression updates"
      pattern: "SharedRoomActivityId|SharedCoupleVisitDay|activityClaimsByDayKey"
    - from: "src/lib/sharedProgression.ts"
      to: "tests/sharedProgression.test.ts"
      via: "room-day visit counting, migration, featured activity, and duplicate-claim behavior are protected by unit tests"
      pattern: "recordSharedRoomVisit|applySharedActivityCompletionToProgression"
---

<objective>
Define the canonical Phase 06 progression contract before activity UI work starts.

Purpose: replace the punitive streak model with a `Together Days` visit ledger, add room-day activity claim state that can support both desk-PC apps and `Cozy Rest`, and preserve hosted-safe canonical progression semantics.
Output: updated progression types, migration-safe normalization, room-day visit helpers, featured-activity selection, and regression tests for the new non-punitive contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md
@.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md
@src/lib/sharedProgressionTypes.ts
@src/lib/sharedProgression.ts
@tests/sharedProgression.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Together Days, visit-day, and activity-claim types with legacy migration</name>
  <files>src/lib/sharedProgressionTypes.ts, src/lib/sharedProgression.ts, tests/sharedProgression.test.ts</files>
  <read_first>src/lib/sharedProgressionTypes.ts, src/lib/sharedProgression.ts, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md, tests/sharedProgression.test.ts</read_first>
  <behavior>
    - Test 1: a legacy progression object with only `streakCount` and `longestStreakCount` normalizes into `togetherDaysCount` and `bestTogetherDaysCount` without losing the stored number
    - Test 2: the current room day stores who has visited, whether the day has already been counted, and which featured activity is selected
    - Test 3: activity claims can represent per-player desk-PC rewards and one couple-wide `Cozy Rest` claim in the same day record
  </behavior>
  <action>Extend `src/lib/sharedProgressionTypes.ts` with the exact exports `SharedRoomActivityId = "pc_snake" | "pc_block_stacker" | "pc_runner" | "cozy_rest"`, `SharedActivityClaimMode = "per_player" | "couple"`, `SharedCoupleVisitDay`, `SharedFeaturedActivityDay`, `SharedRoomActivityClaim`, and `SharedRoomActivityClaimDay`. Update `SharedCoupleProgression` to add the exact fields `togetherDaysCount`, `bestTogetherDaysCount`, `lastTogetherDayKey`, `visitDay`, `featuredActivity`, and `activityClaimsByDayKey`. In `src/lib/sharedProgression.ts`, add normalization helpers so legacy rooms that only have `streakCount` / `longestStreakCount` seed the new Together Days fields from those existing values on first load instead of dropping sentimental progress. Add tests first in `tests/sharedProgression.test.ts` so migration, visit-day shape, and mixed claim-mode storage are locked before reward helper rewrites land.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedProgressionTypes.ts contains "export type SharedRoomActivityId = \"pc_snake\" | \"pc_block_stacker\" | \"pc_runner\" | \"cozy_rest\""
    - src/lib/sharedProgressionTypes.ts contains "togetherDaysCount: number;"
    - src/lib/sharedProgressionTypes.ts contains "activityClaimsByDayKey"
    - src/lib/sharedProgression.ts contains "function normalizeVisitDay"
    - src/lib/sharedProgression.ts contains "function normalizeActivityClaimsByDayKey"
    - tests/sharedProgression.test.ts contains "increments Together Days once when both partners visit the same room day"
  </acceptance_criteria>
  <done>The progression schema can now represent soft shared-presence progress and per-activity room-day claims without relying on the old streak contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add room-day visit, featured-activity, and reward-claim helpers while keeping runtime compatibility</name>
  <files>src/lib/sharedProgression.ts, tests/sharedProgression.test.ts</files>
  <read_first>src/lib/sharedProgression.ts, src/lib/sharedProgressionTypes.ts, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-CONTEXT.md, .planning/phases/06-ritual-variety-and-earn-loop-expansion/06-RESEARCH.md, tests/sharedProgression.test.ts</read_first>
  <behavior>
    - Test 1: `recordSharedRoomVisit` increments Together Days once when both member ids have visited the same room day and does not increment a second time that day
    - Test 2: `advanceRitualDayIfNeeded` rolls visit-day, featured-activity, and claim buckets forward to the next room day without resetting Together Days on a missed day
    - Test 3: `applySharedActivityCompletionToProgression` rejects duplicate room-day claims while still reporting whether the payout was granted
  </behavior>
  <action>In `src/lib/sharedProgression.ts`, keep the existing exports `toRoomDayKey` and `advanceRitualDayIfNeeded` available so `useSharedRoomRuntime.ts` does not break, but rewrite their internals around the new visit-day and claim-ledger model instead of streak reset logic. Add the exact exports `recordSharedRoomVisit`, `selectFeaturedActivityId`, `getSharedActivityClaimStatus`, and `applySharedActivityCompletionToProgression`. `recordSharedRoomVisit` must mark the current player's visit in `visitDay.visitedByPlayerId`, increment `togetherDaysCount` only when all current member ids have visited and `countedAt` is still null, and stamp `lastTogetherDayKey` when the day is counted. `applySharedActivityCompletionToProgression` must accept the exact inputs `activityId`, `claimMode`, `actorPlayerId`, `memberIds`, `rewardCoins`, `rewardXp`, `score`, and `nowIso`, and return an explicit payout flag so later plans can allow practice play without extra rewards. Update `tests/sharedProgression.test.ts` to cover visit counting, featured-activity persistence, duplicate-claim blocking, and no-reset behavior after missed days.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedProgression.ts contains "export function recordSharedRoomVisit"
    - src/lib/sharedProgression.ts contains "export function selectFeaturedActivityId"
    - src/lib/sharedProgression.ts contains "export function getSharedActivityClaimStatus"
    - src/lib/sharedProgression.ts contains "export function applySharedActivityCompletionToProgression"
    - src/lib/sharedProgression.ts contains "payoutGranted"
    - tests/sharedProgression.test.ts contains "does not reset Together Days after a missed room day"
  </acceptance_criteria>
  <done>The repo now has canonical room-day helpers for Together Days and activity claims that later PC and bed features can reuse safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts
- [ ] Shared progression can represent Together Days, featured activity, and per-activity room-day claims canonically
- [ ] Missed days no longer reset the sentimental shared-presence metric
</verification>

<success_criteria>
Phase 06 now has a durable progression contract to build on: Together Days, featured ritual metadata, and room-day activity claim tracking are explicit and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ritual-variety-and-earn-loop-expansion/06-01-SUMMARY.md`
</output>
