---
phase: 06.1-codebase-modularization-and-oversized-file-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/app/hooks/shared-room-runtime/runtimeTypes.ts
  - src/app/hooks/shared-room-runtime/runtimeMessages.ts
  - src/app/hooks/shared-room-runtime/runtimeSnapshot.ts
  - src/app/hooks/shared-room-runtime/bootstrapFlow.ts
  - src/app/hooks/shared-room-runtime/hostedFlow.ts
  - src/app/hooks/shared-room-runtime/roomCommits.ts
  - tests/sharedRoomRuntime.bootstrap.test.ts
  - tests/sharedRoomRuntime.hostedFlow.test.ts
  - tests/sharedRoomRuntime.commitFlow.test.ts
  - tests/sharedRoomRuntime.fixtures.ts
autonomous: true
requirements:
  - CODE-01
user_setup: []
must_haves:
  truths:
    - "Shared-room runtime ownership is split across snapshot/types helpers, bootstrap/pairing flow, and commit/recovery logic instead of one 1,100+ line hook file."
    - "`src/app/hooks/useSharedRoomRuntime.ts` stays the public hook surface but becomes a thin orchestrator under the 1k-line guardrail."
    - "The shared-room runtime regression suite is split into focused test files so the repo no longer carries a 1,300+ line runtime test file."
  artifacts:
    - path: "src/app/hooks/useSharedRoomRuntime.ts"
      provides: "Thin public shared-room runtime hook"
      contains: "export function useSharedRoomRuntime"
    - path: "src/app/hooks/shared-room-runtime/runtimeSnapshot.ts"
      provides: "Runtime snapshot and session helper exports"
      contains: "export function createSharedRoomRuntimeSnapshot"
    - path: "src/app/hooks/shared-room-runtime/roomCommits.ts"
      provides: "Commit and replay helpers for shared-room mutations"
      contains: "commitRoomMutation"
    - path: "tests/sharedRoomRuntime.commitFlow.test.ts"
      provides: "Focused runtime commit/conflict regression coverage"
      contains: "commitRoomMutation"
---

<objective>
Decompose `src/app/hooks/useSharedRoomRuntime.ts` and its oversized regression test into focused runtime modules.

Purpose: Reduce the risk of Phase 7+ feature work tangling bootstrap, hosted pairing, and commit/recovery logic in one giant hook/test pair.
Output: a small public runtime hook plus modular helpers under `src/app/hooks/shared-room-runtime/` and split runtime tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-CONTEXT.md
@src/app/hooks/useSharedRoomRuntime.ts
@src/app/hooks/useSharedRoomPresence.ts
@src/lib/sharedRoomClient.ts
@src/lib/sharedRoomOwnershipClient.ts
@tests/sharedRoomRuntime.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract runtime types, messages, and snapshot helpers into a dedicated subfolder</name>
  <files>src/app/hooks/useSharedRoomRuntime.ts, src/app/hooks/shared-room-runtime/runtimeTypes.ts, src/app/hooks/shared-room-runtime/runtimeMessages.ts, src/app/hooks/shared-room-runtime/runtimeSnapshot.ts, tests/sharedRoomRuntime.bootstrap.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomRuntime.ts, tests/sharedRoomRuntime.test.ts, src/lib/sharedRoomTypes.ts, .planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-CONTEXT.md</read_first>
  <action>Create the exact folder `src/app/hooks/shared-room-runtime/`. Move public interfaces and exported type aliases into `runtimeTypes.ts`. Move `getSharedRoomErrorMessage`, `createBlockingState`, `createHostedUnavailableBody`, and the runtime message constants into `runtimeMessages.ts`. Move `createSharedRoomSessionFromDocument`, `createSharedRoomRuntimeSnapshot`, and `shouldCommitSharedRoomChange` into `runtimeSnapshot.ts`. Update the public hook file to re-export or import from those helpers instead of defining them inline. Add `tests/sharedRoomRuntime.bootstrap.test.ts` for snapshot/bootstrap-focused coverage that previously lived inside the monolithic test file.</action>
  <verify>cmd /c npx vitest run tests/sharedRoomRuntime.bootstrap.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/shared-room-runtime/runtimeTypes.ts contains "export interface SharedRoomRuntimeSnapshot"
    - src/app/hooks/shared-room-runtime/runtimeMessages.ts contains "export function createBlockingState"
    - src/app/hooks/shared-room-runtime/runtimeSnapshot.ts contains "export function createSharedRoomRuntimeSnapshot"
    - src/app/hooks/useSharedRoomRuntime.ts contains "export function useSharedRoomRuntime"
    - cmd /c node -e "const fs=require('fs');process.exit(fs.readFileSync('src/app/hooks/useSharedRoomRuntime.ts','utf8').split(/\\r?\\n/).length < 1000 ? 0 : 1)" exits 0
  </acceptance_criteria>
  <done>The runtime hook no longer mixes public types, strings, and helper factories into the same oversized file.</done>
</task>

<task type="auto">
  <name>Task 2: Split hosted bootstrap/pairing and room commit/recovery logic into focused helper modules</name>
  <files>src/app/hooks/useSharedRoomRuntime.ts, src/app/hooks/shared-room-runtime/bootstrapFlow.ts, src/app/hooks/shared-room-runtime/hostedFlow.ts, src/app/hooks/shared-room-runtime/roomCommits.ts, tests/sharedRoomRuntime.hostedFlow.test.ts, tests/sharedRoomRuntime.commitFlow.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomRuntime.ts, src/lib/sharedRoomClient.ts, src/lib/sharedRoomOwnershipClient.ts, tests/sharedRoomRuntime.test.ts</read_first>
  <action>Move legacy/dev bootstrap helpers such as `loadLegacyRoom`, `createRoom`, `joinRoom`, and `bootstrapDevRoom` into `bootstrapFlow.ts`. Move hosted-auth and pairing helpers such as `loadHostedRoom`, `applyHostedBootstrapState`, `refreshHostedBootstrapState`, `submitPartnerCode`, `confirmPairLink`, and `cancelPairLink` into `hostedFlow.ts`. Move `reloadRoom`, `recoverFromStaleSharedEdit`, `commitRoomState`, and `commitRoomMutation` into `roomCommits.ts`. Keep `useSharedRoomRuntime.ts` as the only exported hook that wires those helpers together and exposes the same return contract to `App.tsx`.</action>
  <verify>cmd /c npx vitest run tests/sharedRoomRuntime.hostedFlow.test.ts tests/sharedRoomRuntime.commitFlow.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/shared-room-runtime/bootstrapFlow.ts contains "createRoom"
    - src/app/hooks/shared-room-runtime/hostedFlow.ts contains "confirmPairLink"
    - src/app/hooks/shared-room-runtime/roomCommits.ts contains "commitRoomMutation"
    - tests/sharedRoomRuntime.hostedFlow.test.ts contains "pending_link"
    - tests/sharedRoomRuntime.commitFlow.test.ts contains "commitRoomMutation"
  </acceptance_criteria>
  <done>The runtime hook now composes focused modules instead of owning bootstrap, hosted flow, and commit replay logic inline.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the monolithic runtime test with focused files and shared fixtures</name>
  <files>tests/sharedRoomRuntime.bootstrap.test.ts, tests/sharedRoomRuntime.hostedFlow.test.ts, tests/sharedRoomRuntime.commitFlow.test.ts, tests/sharedRoomRuntime.fixtures.ts</files>
  <read_first>tests/sharedRoomRuntime.test.ts, src/app/hooks/useSharedRoomRuntime.ts</read_first>
  <action>Split `tests/sharedRoomRuntime.test.ts` into the exact files `tests/sharedRoomRuntime.bootstrap.test.ts`, `tests/sharedRoomRuntime.hostedFlow.test.ts`, and `tests/sharedRoomRuntime.commitFlow.test.ts`. Move duplicated test builders, fake stores, and helper objects into `tests/sharedRoomRuntime.fixtures.ts` if needed. Delete the old monolithic `tests/sharedRoomRuntime.test.ts` once the focused files cover the same behavior so the repo no longer contains a 1,300+ line runtime regression file.</action>
  <verify>cmd /c npx vitest run tests/sharedRoomRuntime.bootstrap.test.ts tests/sharedRoomRuntime.hostedFlow.test.ts tests/sharedRoomRuntime.commitFlow.test.ts</verify>
  <acceptance_criteria>
    - tests/sharedRoomRuntime.bootstrap.test.ts contains "createSharedRoomRuntimeSnapshot"
    - tests/sharedRoomRuntime.hostedFlow.test.ts contains "confirmPairLink"
    - tests/sharedRoomRuntime.commitFlow.test.ts contains "recoverFromStaleSharedEdit"
    - tests/sharedRoomRuntime.fixtures.ts contains "createHostedStoreStub"
    - cmd /c if not exist tests\sharedRoomRuntime.test.ts exit /b 0 else exit /b 1 exits 0
  </acceptance_criteria>
  <done>The runtime test surface is modular enough that future behavior additions no longer reopen a 1k+ spec file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npx vitest run tests/sharedRoomRuntime.bootstrap.test.ts tests/sharedRoomRuntime.hostedFlow.test.ts tests/sharedRoomRuntime.commitFlow.test.ts
- [ ] cmd /c node -e "const fs=require('fs');process.exit(fs.readFileSync('src/app/hooks/useSharedRoomRuntime.ts','utf8').split(/\\r?\\n/).length < 1000 ? 0 : 1)"
- [ ] cmd /c if not exist tests\sharedRoomRuntime.test.ts exit /b 0 else exit /b 1
- [ ] `useSharedRoomRuntime.ts` still exports the same hook contract to `App.tsx`
</verification>

<success_criteria>
The shared-room runtime is broken into stable ownership seams that can absorb new hosted-room behavior without creating another mega-hook or mega-test.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-02-SUMMARY.md`
</output>