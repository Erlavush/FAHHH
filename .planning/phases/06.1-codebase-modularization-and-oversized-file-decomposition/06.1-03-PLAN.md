---
phase: 06.1-codebase-modularization-and-oversized-file-decomposition
plan: 03
type: execute
wave: 2
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - src/App.tsx
  - src/app/hooks/useAppViewPreferences.ts
  - src/app/hooks/useLocalRoomSession.ts
  - src/app/hooks/useAppRoomActions.ts
  - src/app/components/AppRoomStage.tsx
  - src/app/components/AppPlayerView.tsx
  - src/app/components/AppDeveloperView.tsx
  - tests/shellViewModel.test.ts
  - tests/sharedRoomPresenceUx.test.ts
autonomous: true
requirements:
  - CODE-01
user_setup: []
must_haves:
  truths:
    - "`src/App.tsx` stays the top-level shell but drops below the 1k-line guardrail by delegating state and view composition to focused hooks/components."
    - "Player and developer shells still expose the same current runtime behavior, including hosted entry overlays, inventory, preview studio, and room-stage wiring."
    - "Local sandbox state and shared-room actions remain explicit boundaries instead of being hidden in one giant React component."
  artifacts:
    - path: "src/App.tsx"
      provides: "Thin app-shell orchestrator"
      contains: "function App()"
    - path: "src/app/hooks/useAppViewPreferences.ts"
      provides: "Persisted view and developer-panel preference state"
      contains: "export function useAppViewPreferences"
    - path: "src/app/hooks/useLocalRoomSession.ts"
      provides: "Local room/player/pet/session state helpers"
      contains: "export function useLocalRoomSession"
    - path: "src/app/components/AppPlayerView.tsx"
      provides: "Player-shell composition extracted out of App"
      contains: "export function AppPlayerView"
---

<objective>
Break `src/App.tsx` into dedicated hooks and composition components so the top-level app shell no longer owns nearly 2,000 lines of state, callbacks, and JSX.

Purpose: Keep upcoming feature work out of a mega-component while preserving the current player/developer shell split and live room boundaries.
Output: `App.tsx` becomes a thin orchestrator over extracted view-preference hooks, local room/session hooks, action helpers, and player/developer surface components.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-CONTEXT.md
@src/App.tsx
@src/app/hooks/useSharedRoomRuntime.ts
@src/app/hooks/useSharedRoomPresence.ts
@src/app/hooks/useSandboxWorldClock.ts
@src/app/shellViewModel.ts
@src/app/components/PlayerRoomShell.tsx
@src/app/components/DeveloperWorkspaceShell.tsx
@src/components/RoomView.tsx
@src/components/FurniturePreviewStudio.tsx
@src/components/ui/InventoryPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract persisted view preferences and local room/session state out of App</name>
  <files>src/App.tsx, src/app/hooks/useAppViewPreferences.ts, src/app/hooks/useLocalRoomSession.ts</files>
  <read_first>src/App.tsx, src/lib/devWorldSettings.ts, src/lib/devLocalState.ts, src/app/hooks/useSandboxWorldClock.ts</read_first>
  <action>Create `src/app/hooks/useAppViewPreferences.ts` to own shell-view mode, developer workspace tab, preview-studio selection/open state, dev-panel collapsed state, and view/debug toggles currently initialized from world settings inside `App.tsx`. Create `src/app/hooks/useLocalRoomSession.ts` to own local camera/player/coins/room state, refs, selected memory frame state, breakup-reset dialog state, pet state, and other sandbox-side session values that `App.tsx` currently initializes inline. Keep the existing persistence boundaries intact: world settings stay in `devWorldSettings.ts`, world data stays in `devLocalState.ts`.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts tests/sharedRoomPresenceUx.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useAppViewPreferences.ts contains "export function useAppViewPreferences"
    - src/app/hooks/useLocalRoomSession.ts contains "export function useLocalRoomSession"
    - src/App.tsx contains "useAppViewPreferences"
    - src/App.tsx contains "useLocalRoomSession"
  </acceptance_criteria>
  <done>`App.tsx` no longer spends hundreds of lines declaring persisted view state and local room/session state inline.</done>
</task>

<task type="auto">
  <name>Task 2: Extract room commerce and activity callbacks into a dedicated action hook</name>
  <files>src/App.tsx, src/app/hooks/useAppRoomActions.ts</files>
  <read_first>src/App.tsx, src/lib/economy.ts, src/lib/sharedProgression.ts, src/lib/pets.ts, src/app/hooks/useSharedRoomRuntime.ts</read_first>
  <action>Create `src/app/hooks/useAppRoomActions.ts` to own the buy/place/sell/adopt/minigame/cozy-rest and related shared snapshot callback logic that currently sits inline inside `App.tsx`. Move callbacks such as furniture purchases, pet adoption, PC minigame completion, cozy-rest claim handling, and local/shared room snapshot application into this hook. Keep the public behavior unchanged and continue routing canonical shared-room mutations through `commitRoomMutation` rather than introducing a parallel mutation path.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresenceUx.test.ts tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useAppRoomActions.ts contains "export function useAppRoomActions"
    - src/app/hooks/useAppRoomActions.ts contains "handlePcMinigameComplete"
    - src/app/hooks/useAppRoomActions.ts contains "handleClaimCozyRest"
    - src/App.tsx contains "useAppRoomActions"
  </acceptance_criteria>
  <done>The App shell delegates room/action logic to one focused hook instead of mixing it with top-level JSX composition.</done>
</task>

<task type="auto">
  <name>Task 3: Extract player/developer shell composition and room stage nodes so App becomes a thin orchestrator</name>
  <files>src/App.tsx, src/app/components/AppRoomStage.tsx, src/app/components/AppPlayerView.tsx, src/app/components/AppDeveloperView.tsx</files>
  <read_first>src/App.tsx, src/app/components/PlayerRoomShell.tsx, src/app/components/DeveloperWorkspaceShell.tsx, src/components/RoomView.tsx, src/components/FurniturePreviewStudio.tsx, src/components/ui/InventoryPanel.tsx</read_first>
  <action>Create `src/app/components/AppRoomStage.tsx` for the lazy room-view and preview-studio stage composition currently built inline in `App.tsx`. Create `src/app/components/AppPlayerView.tsx` for the player-shell composition, including overlays, dock, progress stack, room details, and PC minigame surface wiring. Create `src/app/components/AppDeveloperView.tsx` for the developer workspace composition, including header, rail, inspector, utility stack, and stage switching. Reduce `src/App.tsx` to top-level orchestration that wires existing hooks and passes state into those components. Do not change the current player/developer shell behavior or the hosted-entry overlay flow.</action>
  <verify>cmd /c npm test && cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/AppRoomStage.tsx contains "export function AppRoomStage"
    - src/app/components/AppPlayerView.tsx contains "export function AppPlayerView"
    - src/app/components/AppDeveloperView.tsx contains "export function AppDeveloperView"
    - cmd /c node -e "const fs=require('fs');process.exit(fs.readFileSync('src/App.tsx','utf8').split(/\\r?\\n/).length < 1000 ? 0 : 1)" exits 0
    - src/App.tsx contains "AppPlayerView"
    - src/App.tsx contains "AppDeveloperView"
  </acceptance_criteria>
  <done>`App.tsx` becomes a readable top-level composition root instead of a 1,900+ line owner of every runtime detail.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test
- [ ] cmd /c npm run build
- [ ] cmd /c node -e "const fs=require('fs');process.exit(fs.readFileSync('src/App.tsx','utf8').split(/\\r?\\n/).length < 1000 ? 0 : 1)"
- [ ] Player and developer shell behavior remains wired through the extracted components without reintroducing duplicated state branches
</verification>

<success_criteria>
`App.tsx` is no longer a mega-component, and future feature work has clean seams for app preferences, local room session state, action handlers, and shell composition.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-03-SUMMARY.md`
</output>