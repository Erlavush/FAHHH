---
phase: 06.1-codebase-modularization-and-oversized-file-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sharedProgression.ts
  - src/lib/shared-progression/constants.ts
  - src/lib/shared-progression/normalizeProgression.ts
  - src/lib/shared-progression/dayState.ts
  - src/lib/shared-progression/selectors.ts
  - src/lib/shared-progression/mutations.ts
  - tests/sharedProgression.test.ts
autonomous: true
requirements:
  - CODE-01
user_setup: []
must_haves:
  truths:
    - "Shared progression logic is separated into normalization, day-state, selector, and mutation modules without changing public behavior."
    - "`src/lib/sharedProgression.ts` becomes a thin compatibility barrel under the 1k-line guardrail."
    - "Progression regression coverage still exercises wallet, activity, ritual, and day-roll behavior after the split."
  artifacts:
    - path: "src/lib/sharedProgression.ts"
      provides: "Stable public progression API surface"
      contains: "export * from \"./shared-progression/selectors\""
    - path: "src/lib/shared-progression/mutations.ts"
      provides: "Wallet and shared-activity mutation functions"
      contains: "export function applySharedActivityCompletionToProgression"
    - path: "src/lib/shared-progression/selectors.ts"
      provides: "Progression selectors and ritual status helpers"
      contains: "export function buildSharedRitualStatus"
    - path: "tests/sharedProgression.test.ts"
      provides: "Progression regression coverage after modular extraction"
      contains: "applySharedActivityCompletionToProgression"
---

<objective>
Split `src/lib/sharedProgression.ts` into focused domain modules while preserving the current public API and runtime semantics.

Purpose: Stop Phase 7 and later work from piling more logic into the current 1,100+ line progression file.
Output: `src/lib/sharedProgression.ts` becomes a small barrel/compatibility layer, while the real logic moves into clearly named `shared-progression` modules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-CONTEXT.md
@src/lib/sharedProgression.ts
@src/lib/sharedProgressionTypes.ts
@tests/sharedProgression.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract progression constants, normalization helpers, and day-state utilities into a dedicated module folder</name>
  <files>src/lib/sharedProgression.ts, src/lib/shared-progression/constants.ts, src/lib/shared-progression/normalizeProgression.ts, src/lib/shared-progression/dayState.ts, tests/sharedProgression.test.ts</files>
  <read_first>src/lib/sharedProgression.ts, src/lib/sharedProgressionTypes.ts, tests/sharedProgression.test.ts, .planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-CONTEXT.md</read_first>
  <action>Create the exact folder `src/lib/shared-progression/`. Move the exact exports `ROOM_LEVEL_XP_THRESHOLDS`, `DESK_PC_XP_OFFSET`, `DAILY_RITUAL_BONUS_COINS`, `DAILY_RITUAL_BONUS_XP`, and `SHARED_ROOM_ACTIVITY_IDS` into `constants.ts`. Move the clone/normalize helpers, `toRoomDayKey`, `createInitialSharedRoomProgression`, `ensureSharedRoomProgressionMembers`, and day-roll helpers into `normalizeProgression.ts` and `dayState.ts`. Keep `src/lib/sharedProgression.ts` as a compatibility barrel that re-exports the same public names so existing import sites outside this plan do not need to change yet.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/lib/shared-progression/constants.ts contains "export const ROOM_LEVEL_XP_THRESHOLDS"
    - src/lib/shared-progression/normalizeProgression.ts contains "export function ensureSharedRoomProgressionMembers"
    - src/lib/shared-progression/dayState.ts contains "export function advanceRitualDayIfNeeded"
    - src/lib/sharedProgression.ts contains "export * from \"./shared-progression/constants\""
    - cmd /c node -e "const fs=require('fs');process.exit(fs.readFileSync('src/lib/sharedProgression.ts','utf8').split(/\\r?\\n/).length < 1000 ? 0 : 1)" exits 0
  </acceptance_criteria>
  <done>The file-size problem is no longer concentrated in one monolithic helper file, and callers still import from `src/lib/sharedProgression.ts` safely.</done>
</task>

<task type="auto">
  <name>Task 2: Move selectors and mutation APIs into focused files while keeping regression coverage intact</name>
  <files>src/lib/sharedProgression.ts, src/lib/shared-progression/selectors.ts, src/lib/shared-progression/mutations.ts, tests/sharedProgression.test.ts</files>
  <read_first>src/lib/sharedProgression.ts, tests/sharedProgression.test.ts, src/app/hooks/useSharedRoomRuntime.ts, src/App.tsx</read_first>
  <action>Move selector-style exports such as `selectActivePlayerProgression`, `selectPartnerPlayerProgression`, `getSharedActivityClaimStatus`, and `buildSharedRitualStatus` into `src/lib/shared-progression/selectors.ts`. Move wallet and reward mutation APIs such as `applyPersonalWalletSpend`, `applyPersonalWalletRefund`, `applySharedActivityCompletionToProgression`, and `applyDeskPcCompletionToProgression` into `src/lib/shared-progression/mutations.ts`. Update `tests/sharedProgression.test.ts` so it still covers ritual roll-forward, wallet math, and activity-claim behavior through the public barrel export surface.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/lib/shared-progression/selectors.ts contains "export function buildSharedRitualStatus"
    - src/lib/shared-progression/selectors.ts contains "export function getSharedActivityClaimStatus"
    - src/lib/shared-progression/mutations.ts contains "export function applyPersonalWalletSpend"
    - src/lib/shared-progression/mutations.ts contains "export function applySharedActivityCompletionToProgression"
    - tests/sharedProgression.test.ts contains "buildSharedRitualStatus"
    - tests/sharedProgression.test.ts contains "applyPersonalWalletSpend"
  </acceptance_criteria>
  <done>`sharedProgression` becomes a stable barrel over focused modules instead of a single oversized mixed-responsibility file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts
- [ ] cmd /c node -e "const fs=require('fs');process.exit(fs.readFileSync('src/lib/sharedProgression.ts','utf8').split(/\\r?\\n/).length < 1000 ? 0 : 1)"
- [ ] `src/lib/sharedProgression.ts` is a compatibility barrel and callers still resolve the same public exports
</verification>

<success_criteria>
Shared progression logic is modular enough that Phase 7 and later work can extend the system without reopening a 1,100+ line utility file.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-codebase-modularization-and-oversized-file-decomposition/06.1-01-SUMMARY.md`
</output>