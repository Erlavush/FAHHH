---
phase: 02-live-presence-and-co-op-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/types.ts
  - src/components/MinecraftPlayer.tsx
  - src/components/RoomView.tsx
  - src/components/room-view/useRoomViewInteractions.ts
  - src/app/hooks/useSharedRoomPresence.ts
  - src/lib/sharedPresenceTypes.ts
  - src/lib/sharedPresenceValidation.ts
  - src/lib/sharedPresenceStore.ts
  - src/lib/sharedPresenceClient.ts
  - src/App.tsx
  - scripts/sharedRoomDevPlugin.mjs
  - tests/sharedRoomPresence.test.ts
autonomous: true
requirements:
  - PRES-01
user_setup: []
must_haves:
  truths:
    - "Live presence rides a separate lightweight boundary instead of piggybacking on canonical room commits."
    - "The room runtime emits local position, facing, and activity state in a shape that can drive a remote avatar directly."
    - "The shared room scene renders a full remote partner avatar with current appearance, interpolated motion, and synced interaction poses."
  artifacts:
    - path: "src/lib/sharedPresenceStore.ts"
      provides: "Replaceable live presence store interface"
      min_lines: 50
      contains: "export interface SharedPresenceStore"
    - path: "src/app/hooks/useSharedRoomPresence.ts"
      provides: "Client-side presence publishing and polling orchestration"
      min_lines: 80
      contains: "export function useSharedRoomPresence"
    - path: "src/components/MinecraftPlayer.tsx"
      provides: "Avatar component with transform reporting and remote-render mode"
      min_lines: 180
      contains: "onTransformChange"
    - path: "src/components/RoomView.tsx"
      provides: "Live room composition with remote partner actor support"
      min_lines: 140
      contains: "remotePresence"
    - path: "scripts/sharedRoomDevPlugin.mjs"
      provides: "Development presence endpoints separate from canonical room commits"
      min_lines: 160
      contains: "/api/dev/shared-room/presence/upsert"
    - path: "tests/sharedRoomPresence.test.ts"
      provides: "Presence transport and remote-avatar regression coverage"
      contains: "presence updates do not mutate the room revision"
  key_links:
    - from: "src/app/hooks/useSharedRoomPresence.ts"
      to: "src/lib/sharedPresenceStore.ts"
      via: "store interface imports and polling/publish calls"
      pattern: "from \"\\.\\.\\/\\.\\.\\/lib\\/sharedPresenceStore\"|from \"\\.\\.\\/\\.\\.\\/lib\\/sharedPresenceClient\""
    - from: "src/components/RoomView.tsx"
      to: "src/components/MinecraftPlayer.tsx"
      via: "local and remote avatar rendering"
      pattern: "MinecraftPlayer"
    - from: "scripts/sharedRoomDevPlugin.mjs"
      to: "src/lib/sharedPresenceValidation.ts"
      via: "presence payload validation before write"
      pattern: "sharedPresenceValidation"
---

<objective>
Add the live presence transport and in-room remote avatar rendering without weakening the Phase 1 canonical room authority model.

Purpose: Make the room visibly shared by sending lightweight partner presence separately from committed room edits.
Output: Presence types/store/client/validation, dev presence endpoints, a presence hook, runtime-facing local presence snapshots, remote avatar rendering, and regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-presence-and-co-op-consistency/02-CONTEXT.md
@.planning/phases/02-live-presence-and-co-op-consistency/02-RESEARCH.md
@.planning/phases/02-live-presence-and-co-op-consistency/02-UI-SPEC.md
@src/App.tsx
@src/components/RoomView.tsx
@src/components/MinecraftPlayer.tsx
@src/components/room-view/useRoomViewInteractions.ts
@src/lib/sharedRoomStore.ts
@scripts/sharedRoomDevPlugin.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the live presence contract and validation boundary</name>
  <files>src/lib/sharedPresenceTypes.ts, src/lib/sharedPresenceValidation.ts, src/lib/sharedPresenceStore.ts, src/lib/sharedPresenceClient.ts</files>
  <read_first>.planning/phases/02-live-presence-and-co-op-consistency/02-CONTEXT.md, .planning/phases/02-live-presence-and-co-op-consistency/02-RESEARCH.md, src/lib/sharedRoomTypes.ts, src/lib/sharedRoomStore.ts, src/app/types.ts</read_first>
  <action>Create `src/lib/sharedPresenceTypes.ts` with exact exported interfaces for `SharedPresenceActivity`, `SharedPresenceSnapshot`, `SharedPresenceRoomSnapshot`, and the request/response shapes needed for lightweight presence transport. The snapshot must include `roomId`, `playerId`, `displayName`, `skinSrc`, `position`, `facingY`, `activity`, `updatedAt`, and optional interaction-pose metadata. Create `src/lib/sharedPresenceValidation.ts` to validate presence payloads independently from room-document validation. Create `src/lib/sharedPresenceStore.ts` with exact methods `upsertPresence`, `loadRoomPresence`, and `leavePresence`, plus any typed request/response wrappers. Create `src/lib/sharedPresenceClient.ts` targeting `/api/dev/shared-room/presence/upsert`, `/api/dev/shared-room/presence/room/{roomId}`, and `/api/dev/shared-room/presence/leave` without reusing canonical room commit endpoints.</action>
  <verify>cmd /c npm test -- tests/sharedRoomPresence.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedPresenceStore.ts contains "export interface SharedPresenceStore"
    - src/lib/sharedPresenceStore.ts contains "upsertPresence"
    - src/lib/sharedPresenceClient.ts contains "/api/dev/shared-room/presence/upsert"
    - src/lib/sharedPresenceValidation.ts contains "facingY"
  </acceptance_criteria>
  <done>The repo has a replaceable live-presence API boundary that stays independent from canonical room document commits.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dev presence backend and runtime-facing local presence snapshots</name>
  <files>src/app/types.ts, src/components/MinecraftPlayer.tsx, src/components/room-view/useRoomViewInteractions.ts, src/app/hooks/useSharedRoomPresence.ts, scripts/sharedRoomDevPlugin.mjs</files>
  <read_first>src/app/types.ts, src/components/MinecraftPlayer.tsx, src/components/room-view/useRoomViewInteractions.ts, src/app/hooks/useSharedRoomRuntime.ts, .planning/phases/02-live-presence-and-co-op-consistency/02-RESEARCH.md</read_first>
  <action>Extend `src/app/types.ts` so the room runtime can surface a richer local presence snapshot instead of only the current simplified `PlayerInteractionStatus`. Update `src/components/MinecraftPlayer.tsx` to expose exact `onTransformChange(position, facingY)` output for the local actor and a remote-read-only mode that does not own local collision or movement authority. Update `src/components/room-view/useRoomViewInteractions.ts` to publish activity/pose state that maps cleanly to `idle`, `walking`, `sit`, `lie`, and `use_pc`. Create `src/app/hooks/useSharedRoomPresence.ts` to publish local presence on an interval, poll room presence on a separate interval, and derive freshness states without touching canonical room revisions. Extend `scripts/sharedRoomDevPlugin.mjs` with a top-level `presenceByRoom` collection and the exact presence endpoints, ensuring presence writes never increment or mutate shared-room `revision`.</action>
  <verify>cmd /c npm test -- tests/sharedRoomPresence.test.ts tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/components/MinecraftPlayer.tsx contains "onTransformChange"
    - src/app/hooks/useSharedRoomPresence.ts contains "export function useSharedRoomPresence"
    - scripts/sharedRoomDevPlugin.mjs contains "presenceByRoom"
    - scripts/sharedRoomDevPlugin.mjs contains "/api/dev/shared-room/presence/leave"
  </acceptance_criteria>
  <done>The live room can publish compact local presence snapshots through a dedicated dev backend without spamming full room documents.</done>
</task>

<task type="auto">
  <name>Task 3: Render the remote partner avatar and add presence regression coverage</name>
  <files>src/components/RoomView.tsx, src/App.tsx, tests/sharedRoomPresence.test.ts</files>
  <read_first>src/components/RoomView.tsx, src/App.tsx, src/app/hooks/useSharedRoomPresence.ts, src/components/MinecraftPlayer.tsx, .planning/phases/02-live-presence-and-co-op-consistency/02-UI-SPEC.md</read_first>
  <action>Update `src/components/RoomView.tsx` to accept `remotePresence` and render one full remote `MinecraftPlayer` using interpolated movement, facing, the partner's current skin/profile appearance, and synced interaction poses. The remote actor must never feed authority back into canonical room state. Update `src/App.tsx` to connect `useSharedRoomPresence` with the existing shared-room runtime so presence only starts once a canonical room is loaded. Add `tests/sharedRoomPresence.test.ts` that proves presence updates stay separate from room commits, remote partner snapshots carry position plus facing plus activity, and room revision does not change when presence changes.</action>
  <verify>cmd /c npm test -- tests/sharedRoomPresence.test.ts</verify>
  <acceptance_criteria>
    - src/components/RoomView.tsx contains "remotePresence"
    - src/components/RoomView.tsx contains "MinecraftPlayer"
    - src/App.tsx contains "useSharedRoomPresence"
    - tests/sharedRoomPresence.test.ts contains "presence updates do not mutate the room revision"
  </acceptance_criteria>
  <done>The live room shows the partner as a real in-room avatar backed by a separate presence channel and automated regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- tests/sharedRoomPresence.test.ts tests/sharedRoomRuntime.test.ts
- [ ] cmd /c npm run build
- [ ] Presence transport stays separate from canonical room commit payloads and does not mutate shared-room `revision`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Each partner can see the other partner's avatar position and facing through a lightweight presence channel instead of full room commits
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-presence-and-co-op-consistency/02-01-SUMMARY.md`
</output>
