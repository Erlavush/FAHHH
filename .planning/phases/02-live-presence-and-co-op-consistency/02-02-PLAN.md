---
phase: 02-live-presence-and-co-op-consistency
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/lib/sharedRoomStore.ts
  - src/lib/sharedRoomClient.ts
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/app/hooks/useSharedRoomPresence.ts
  - src/app/components/SharedRoomStatusStrip.tsx
  - src/App.tsx
  - src/styles.css
  - scripts/sharedRoomDevPlugin.mjs
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomPresenceUx.test.ts
autonomous: true
requirements:
  - PRES-02
user_setup: []
must_haves:
  truths:
    - "Development builds auto-enter one deterministic shared room instead of flashing the pairing shell."
    - "Shipped builds still keep the real pairing and shared-room status surfaces, but join/leave/reconnect states stay subtle and non-blocking."
    - "Presence freshness, not canonical room commits, drives partner waiting, joined, reconnecting, and returned states."
  artifacts:
    - path: "src/lib/sharedRoomStore.ts"
      provides: "Shared-room store with deterministic development bootstrap support"
      min_lines: 60
      contains: "bootstrapDevSharedRoom"
    - path: "src/app/hooks/useSharedRoomRuntime.ts"
      provides: "Runtime bootstrap path with dev auto-enter and hidden temporary UI surfaces"
      min_lines: 120
      contains: "devBypassActive"
    - path: "src/app/components/SharedRoomStatusStrip.tsx"
      provides: "Subtle partner presence status surface for shipped builds"
      min_lines: 50
      contains: "Partner reconnecting"
    - path: "scripts/sharedRoomDevPlugin.mjs"
      provides: "Dev bootstrap endpoint that ensures one predictable room for development"
      min_lines: 200
      contains: "/api/dev/shared-room/dev-bootstrap"
    - path: "tests/sharedRoomPresenceUx.test.ts"
      provides: "Presence-status and dev-bypass regression coverage"
      contains: "auto-enters the dev shared room"
  key_links:
    - from: "src/app/hooks/useSharedRoomRuntime.ts"
      to: "src/lib/sharedRoomStore.ts"
      via: "dev bootstrap and canonical room load"
      pattern: "bootstrapDevSharedRoom|loadSharedRoom"
    - from: "src/App.tsx"
      to: "src/app/components/SharedRoomStatusStrip.tsx"
      via: "partner presence status rendering and dev hide rules"
      pattern: "SharedRoomStatusStrip"
    - from: "scripts/sharedRoomDevPlugin.mjs"
      to: "src/lib/sharedRoomClient.ts"
      via: "development bootstrap fetch route"
      pattern: "dev-bootstrap"
---

<objective>
Add the join, leave, reconnect, and development-bypass shell behavior around Phase 2 live presence.

Purpose: Let development enter a shared room immediately while shipped builds still communicate partner presence clearly and quietly.
Output: Deterministic dev bootstrap, runtime dev bypass, subtle presence status updates, hidden temporary dev surfaces, and regression tests for presence UX.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-presence-and-co-op-consistency/02-CONTEXT.md
@.planning/phases/02-live-presence-and-co-op-consistency/02-RESEARCH.md
@.planning/phases/02-live-presence-and-co-op-consistency/02-UI-SPEC.md
@src/App.tsx
@src/app/hooks/useSharedRoomRuntime.ts
@src/app/hooks/useSharedRoomPresence.ts
@src/app/components/SharedRoomStatusStrip.tsx
@scripts/sharedRoomDevPlugin.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic development bootstrap for the shared room</name>
  <files>src/lib/sharedRoomStore.ts, src/lib/sharedRoomClient.ts, scripts/sharedRoomDevPlugin.mjs</files>
  <read_first>src/lib/sharedRoomStore.ts, src/lib/sharedRoomClient.ts, src/lib/sharedRoomSession.ts, scripts/sharedRoomDevPlugin.mjs, .planning/phases/02-live-presence-and-co-op-consistency/02-CONTEXT.md</read_first>
  <action>Extend `src/lib/sharedRoomStore.ts` and `src/lib/sharedRoomClient.ts` with exact `bootstrapDevSharedRoom` request/response support aimed at `/api/dev/shared-room/dev-bootstrap`. Extend `scripts/sharedRoomDevPlugin.mjs` with a development-only bootstrap endpoint that ensures one predictable room id `dev-shared-room`, one stable invite code `DEVROOM`, and a reusable two-member room lifecycle for browser-local testing. The endpoint must create the room from the current canonical source room only if missing, rejoin the same player when revisiting, join a second player when available, and return a 409 for a third distinct profile instead of silently replacing an existing member.</action>
  <verify>cmd /c npm test -- tests/sharedRoomRuntime.test.ts tests/sharedRoomStore.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomStore.ts contains "bootstrapDevSharedRoom"
    - src/lib/sharedRoomClient.ts contains "/api/dev/shared-room/dev-bootstrap"
    - scripts/sharedRoomDevPlugin.mjs contains "dev-shared-room"
    - scripts/sharedRoomDevPlugin.mjs contains "DEVROOM"
  </acceptance_criteria>
  <done>Development has a deterministic shared-room bootstrap path instead of relying on a visible invite flow during iteration.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-enter the dev room and hide temporary player-facing surfaces during development</name>
  <files>src/app/hooks/useSharedRoomRuntime.ts, src/App.tsx</files>
  <read_first>src/app/hooks/useSharedRoomRuntime.ts, src/App.tsx, src/lib/sharedRoomSession.ts, .planning/phases/02-live-presence-and-co-op-consistency/02-UI-SPEC.md</read_first>
  <action>Update `src/app/hooks/useSharedRoomRuntime.ts` so `import.meta.env.DEV` activates a true bootstrap path rather than merely hiding components. When dev bypass is active and no usable session exists, call `bootstrapDevSharedRoom`, load the canonical room, and expose an exact `devBypassActive` flag. Update `src/App.tsx` so development builds suppress `SharedRoomEntryShell` and `SharedRoomStatusStrip` while bypass is active, without flashing those surfaces first. Keep the real player-facing pairing flow in code for shipped builds and preserve Phase 1 blocking overlays for true canonical-room failures.</action>
  <verify>cmd /c npm test -- tests/sharedRoomRuntime.test.ts tests/sharedRoomPresenceUx.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useSharedRoomRuntime.ts contains "import.meta.env.DEV"
    - src/app/hooks/useSharedRoomRuntime.ts contains "devBypassActive"
    - src/App.tsx contains "devBypassActive"
    - tests/sharedRoomPresenceUx.test.ts contains "auto-enters the dev shared room"
  </acceptance_criteria>
  <done>Development goes straight into the live room without the temporary invite shell or shared-room strip getting in the way.</done>
</task>

<task type="auto">
  <name>Task 3: Add subtle join, leave, and reconnect presence status UX</name>
  <files>src/app/hooks/useSharedRoomPresence.ts, src/app/components/SharedRoomStatusStrip.tsx, src/styles.css, tests/sharedRoomPresenceUx.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomPresence.ts, src/app/components/SharedRoomStatusStrip.tsx, src/styles.css, .planning/phases/02-live-presence-and-co-op-consistency/02-UI-SPEC.md</read_first>
  <action>Update `src/app/hooks/useSharedRoomPresence.ts` to derive room-presence freshness states that map to the exact copy `Waiting for partner`, `Partner joined`, `Partner reconnecting`, `Partner is back`, and `Together now`. Update `src/app/components/SharedRoomStatusStrip.tsx` so shipped builds render a compact presence-mode status surface near the toolbar once a room is active, while development bypass keeps that strip hidden. Extend `src/styles.css` with dedicated presence-strip styles using the Phase 2 UI colors `#89B7D8`, `#6FAF83`, and `#D9B07C` without introducing a dashboard-like HUD. Add `tests/sharedRoomPresenceUx.test.ts` that proves the room stays usable while the partner is absent, reconnecting is non-blocking, and the status copy derives from presence freshness rather than room commit state.</action>
  <verify>cmd /c npm test -- tests/sharedRoomPresenceUx.test.ts tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/app/components/SharedRoomStatusStrip.tsx contains "Waiting for partner"
    - src/app/components/SharedRoomStatusStrip.tsx contains "Partner reconnecting"
    - src/styles.css contains ".shared-room-status--presence"
    - tests/sharedRoomPresenceUx.test.ts contains "keeps the room usable while the partner reconnects"
  </acceptance_criteria>
  <done>Join, leave, and reconnect states are visible and reliable without blocking the room or showing distracting development-only surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- tests/sharedRoomPresenceUx.test.ts tests/sharedRoomRuntime.test.ts tests/sharedRoomStore.test.ts
- [ ] cmd /c npm run build
- [ ] Development builds auto-enter the shared room without flashing the invite shell, while shipped builds still expose subtle partner-status UX
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Each partner can see when the other partner joins, reconnects, or leaves, and development bypass hides the temporary pairing/status surfaces during iteration
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-presence-and-co-op-consistency/02-02-SUMMARY.md`
</output>
