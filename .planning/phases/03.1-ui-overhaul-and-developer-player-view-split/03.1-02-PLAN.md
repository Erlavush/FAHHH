---
phase: 03.1-ui-overhaul-and-developer-player-view-split
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/app/shellViewModel.ts
  - src/app/components/PlayerClockChip.tsx
  - src/app/components/PlayerCompanionCard.tsx
  - src/app/components/PlayerProgressStack.tsx
  - src/app/components/PlayerActionDock.tsx
  - src/app/components/PlayerRoomDetailsSheet.tsx
  - src/app/components/InventoryPanel.tsx
  - src/App.tsx
  - src/styles.css
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - SHELL-01
user_setup: []
must_haves:
  truths:
    - "Player View is the shipped room-first shell and no longer reuses the old top-left debug/control cluster."
    - "Build mode, inventory, settings, partner status, ritual status, and PC minigame stay reachable from Player View."
    - "Secondary actions like room refresh, invite code, grid snap, and skin import move out of persistent HUD chrome."
  artifacts:
    - path: "src/app/components/PlayerClockChip.tsx"
      provides: "Dedicated top-left time chip for the player shell"
      min_lines: 25
      contains: "className=\"player-clock-chip\""
    - path: "src/app/components/PlayerCompanionCard.tsx"
      provides: "Top-right partner and ritual status surface"
      min_lines: 60
      contains: "className=\"player-companion-card\""
    - path: "src/app/components/PlayerActionDock.tsx"
      provides: "Bottom-center player action dock"
      min_lines: 50
      contains: "className=\"player-action-dock\""
    - path: "src/app/components/PlayerRoomDetailsSheet.tsx"
      provides: "Secondary player details/settings surface"
      min_lines: 70
      contains: "Refresh room state"
    - path: "src/app/components/InventoryPanel.tsx"
      provides: "Drawer-safe inventory surface that preserves current catalog logic"
      contains: "Shared Inventory"
    - path: "tests/shellViewModel.test.ts"
      provides: "Player HUD and copy regression coverage"
      contains: "returns player companion card copy"
  key_links:
    - from: "src/app/shellViewModel.ts"
      to: "src/App.tsx"
      via: "player HUD copy and action state derivation"
      pattern: "getPlayerCompanionCardState|getPlayerActionDockState|getPlayerRoomDetailsState"
    - from: "src/App.tsx"
      to: "src/app/components/PlayerRoomShell.tsx"
      via: "slot composition for player HUD zones"
      pattern: "PlayerRoomShell"
    - from: "src/App.tsx"
      to: "src/app/components/InventoryPanel.tsx"
      via: "player inventory drawer mounting"
      pattern: "InventoryPanel"
---

<objective>
Rebuild the shipped player-facing room shell so time, progression, build mode, inventory, companion status, and room details appear in deliberate room-edge zones instead of the old debug-heavy overlay stack.

Purpose: Fulfill `SHELL-01` with a player shell that looks and behaves like the intended shipped game while preserving all current room, build, inventory, settings, and shared-room flows.
Output: New player HUD components, player-safe details/settings surfaces, a drawer-based inventory presentation, and `App.tsx` wired to render the player contract from `03.1-UI-SPEC.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-RESEARCH.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-01-PLAN.md
@src/App.tsx
@src/app/shellViewModel.ts
@src/app/components/InventoryPanel.tsx
@src/app/components/SceneToolbar.tsx
@src/app/components/SharedRoomStatusStrip.tsx
@src/app/components/SharedRoomEntryShell.tsx
@src/app/components/SharedRoomBlockingOverlay.tsx
@src/components/PcMinigameOverlay.tsx
@src/styles.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure player-shell view models and copy contracts before rebuilding the HUD</name>
  <files>src/app/shellViewModel.ts, tests/shellViewModel.test.ts</files>
  <read_first>src/app/shellViewModel.ts, src/App.tsx, src/app/components/SharedRoomStatusStrip.tsx, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-RESEARCH.md</read_first>
  <behavior>
    - Test 1: player companion-card mapping returns the exact titles `Waiting for partner`, `Together now`, `Reconnecting`, `Daily ritual pending`, and `Daily ritual complete` for the supported presence and ritual states
    - Test 2: room-details mapping exposes invite code and `Refresh room state` only as secondary details actions, not primary dock actions
    - Test 3: action-dock mapping keeps `Enter Build Mode` and `Open Inventory` as primary player actions and never emits `Grid Snap`, `Import Minecraft Skin`, `Preview Studio`, or `Dev Panel`
  </behavior>
  <action>Extend `src/app/shellViewModel.ts` with exact exports `getPlayerCompanionCardState(...)`, `getPlayerActionDockState(...)`, and `getPlayerRoomDetailsState(...)`. Keep these helpers pure and responsible only for labels, tones, visibility flags, and section ordering. Add tests in `tests/shellViewModel.test.ts` first so player-facing copy and action contracts are locked before any JSX extraction begins.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/app/shellViewModel.ts contains "getPlayerCompanionCardState"
    - src/app/shellViewModel.ts contains "getPlayerActionDockState"
    - src/app/shellViewModel.ts contains "getPlayerRoomDetailsState"
    - src/app/shellViewModel.ts contains "Refresh room state"
    - tests/shellViewModel.test.ts contains "returns player companion card copy"
    - tests/shellViewModel.test.ts contains "does not expose developer actions in the player dock"
  </acceptance_criteria>
  <done>The player HUD behavior is defined in testable pure helpers instead of being buried inside ad hoc `App.tsx` JSX branches.</done>
</task>

<task type="auto">
  <name>Task 2: Extract deliberate player HUD components and mount them through `PlayerRoomShell`</name>
  <files>src/app/components/PlayerClockChip.tsx, src/app/components/PlayerCompanionCard.tsx, src/app/components/PlayerProgressStack.tsx, src/app/components/PlayerActionDock.tsx, src/app/components/PlayerRoomDetailsSheet.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, src/app/components/PlayerRoomShell.tsx, src/app/components/SceneToolbar.tsx, src/app/components/SharedRoomStatusStrip.tsx, src/components/PcMinigameOverlay.tsx, src/app/shellViewModel.ts</read_first>
  <action>Create `src/app/components/PlayerClockChip.tsx`, `PlayerCompanionCard.tsx`, `PlayerProgressStack.tsx`, `PlayerActionDock.tsx`, and `PlayerRoomDetailsSheet.tsx` with exact root class names `player-clock-chip`, `player-companion-card`, `player-progress-stack`, `player-action-dock`, and `player-room-details-sheet`. Update `src/App.tsx` so the player branch renders these surfaces through `PlayerRoomShell` slots: time top-left, companion/details top-right, progression bottom-left, action dock bottom-center, and full-screen overlays layered separately. Remove `SceneToolbar` and `SharedRoomStatusStrip` from the player branch entirely. Keep `SharedRoomEntryShell`, `SharedRoomBlockingOverlay`, `PcMinigameOverlay`, `EditDock`, and `PlacementActions` behavior intact, and preserve all existing build, interaction, and shared-room state ownership in `App.tsx`.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/PlayerClockChip.tsx contains "className=\"player-clock-chip\""
    - src/app/components/PlayerCompanionCard.tsx contains "className=\"player-companion-card\""
    - src/app/components/PlayerProgressStack.tsx contains "className=\"player-progress-stack\""
    - src/app/components/PlayerActionDock.tsx contains "className=\"player-action-dock\""
    - src/app/components/PlayerRoomDetailsSheet.tsx contains "className=\"player-room-details-sheet\""
    - src/App.tsx contains "PlayerClockChip"
    - src/App.tsx contains "PlayerCompanionCard"
    - src/App.tsx does not contain "<SceneToolbar"
    - src/App.tsx does not contain "<SharedRoomStatusStrip"
  </acceptance_criteria>
  <done>The shipped player shell is now composed from explicit room-first HUD surfaces instead of a mixed toolbar/status/debug stack.</done>
</task>

<task type="auto">
  <name>Task 3: Convert inventory and secondary player controls into drawer and sheet surfaces</name>
  <files>src/app/components/InventoryPanel.tsx, src/app/components/PlayerRoomDetailsSheet.tsx, src/App.tsx, src/styles.css</files>
  <read_first>src/app/components/InventoryPanel.tsx, src/App.tsx, src/styles.css, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md</read_first>
  <action>Keep the current `InventoryPanel` buy/place/sell/store and pet-catalog behavior, but adapt it to a player drawer presentation that opens from the left on desktop and collapses into a bottom sheet on narrow screens. Add the supporting sheet/drawer controls to `PlayerRoomDetailsSheet.tsx` so `Grid Snap`, `Import Minecraft Skin`, invite code visibility, and `Refresh room state` live there as secondary actions rather than persistent HUD buttons. Update `src/styles.css` to implement the player shell visual contract: warm parchment surfaces, deep ink secondary cards, terracotta accent only for active/primary states, no developer panel styling, and responsive drawer behavior at `<= 900px`.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/InventoryPanel.tsx contains "spawn-panel"
    - src/App.tsx contains "player-room-details-sheet"
    - src/styles.css contains ".player-clock-chip"
    - src/styles.css contains ".player-companion-card"
    - src/styles.css contains ".player-progress-stack"
    - src/styles.css contains ".player-action-dock"
    - src/styles.css contains ".player-room-details-sheet"
    - src/styles.css contains "@media (max-width: 900px)"
  </acceptance_criteria>
  <done>Player View keeps the existing gameplay flows but presents them in clean room-edge surfaces, drawers, and sheets instead of one noisy overlay cluster.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts
- [ ] cmd /c npm run build
- [ ] Player View no longer mounts `SceneToolbar` or `SharedRoomStatusStrip`, while build mode, inventory, settings/details, and PC minigame remain fully reachable
</verification>

<success_criteria>
The player-facing shell now reads as the shipped game: room-first, aesthetic, and structured, while all former top-left clutter is redistributed into clear zones and secondary sheets without losing current functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-02-SUMMARY.md`
</output>
