---
phase: 03.1-ui-overhaul-and-developer-player-view-split
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/app/shellViewModel.ts
  - src/app/components/DeveloperWorkspaceShell.tsx
  - src/app/components/DeveloperWorkspaceHeader.tsx
  - src/app/components/DeveloperWorkspaceRail.tsx
  - src/app/components/DeveloperSessionPanel.tsx
  - src/app/components/InventoryPanel.tsx
  - src/app/components/DevPanel.tsx
  - src/app/components/PerformanceMonitor.tsx
  - src/components/FurniturePreviewStudio.tsx
  - src/App.tsx
  - src/styles.css
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - SHELL-02
  - DEVX-01
user_setup: []
must_haves:
  truths:
    - "Developer-only tooling no longer renders through the shipped player shell."
    - "Developer View keeps one-click access to room runtime, inventory tooling, Preview Studio, Mob Lab, world controls, and session diagnostics."
    - "Preview Studio and Mob Lab run inside the developer workspace stage instead of floating above the player shell."
  artifacts:
    - path: "src/app/components/DeveloperWorkspaceHeader.tsx"
      provides: "Developer-only header with room/session summary and quick actions"
      min_lines: 40
      contains: "className=\"developer-workspace-header\""
    - path: "src/app/components/DeveloperWorkspaceRail.tsx"
      provides: "Explicit developer navigation rail"
      min_lines: 50
      contains: "Preview Studio"
    - path: "src/app/components/DeveloperSessionPanel.tsx"
      provides: "Developer shared-session diagnostics surface"
      min_lines: 60
      contains: "className=\"developer-session-panel\""
    - path: "src/components/FurniturePreviewStudio.tsx"
      provides: "Workspace-safe embedded presentation path"
      contains: "preview-studio--workspace"
    - path: "src/app/components/PerformanceMonitor.tsx"
      provides: "Developer-only performance monitor shell"
      contains: "className=\"performance-monitor\""
    - path: "tests/shellViewModel.test.ts"
      provides: "Developer workspace tab and session regression coverage"
      contains: "returns developer workspace tabs in contract order"
  key_links:
    - from: "src/app/shellViewModel.ts"
      to: "src/app/components/DeveloperWorkspaceRail.tsx"
      via: "tab metadata and workspace labels"
      pattern: "Room|Inventory|Preview Studio|Mob Lab|World|Session"
    - from: "src/App.tsx"
      to: "src/components/FurniturePreviewStudio.tsx"
      via: "developer workspace stage composition"
      pattern: "preview_studio|mob_lab"
    - from: "src/App.tsx"
      to: "src/app/components/DevPanel.tsx"
      via: "developer-only inspector mounting"
      pattern: "DevPanel"
---

<objective>
Reorganize development-time tooling into a dedicated developer workspace so debug/session controls, Preview Studio, furniture preview flows, Mob Lab, and performance readouts are fast to reach without polluting the shipped player shell.

Purpose: Fulfill `SHELL-02` and `DEVX-01` by building a tool-first workspace around the existing room runtime and authoring surfaces instead of continuing to mount those tools over Player View.
Output: Developer header/rail/session components, developer-only inspector and utility surfaces, embedded Preview Studio and Mob Lab stage rendering, and `App.tsx` routing that keeps all tooling behind the developer branch.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-RESEARCH.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-01-PLAN.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-02-PLAN.md
@src/App.tsx
@src/app/shellViewModel.ts
@src/app/components/DeveloperWorkspaceShell.tsx
@src/app/components/InventoryPanel.tsx
@src/app/components/DevPanel.tsx
@src/app/components/PerformanceMonitor.tsx
@src/components/FurniturePreviewStudio.tsx
@src/styles.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock developer workspace navigation and session copy in pure helpers first</name>
  <files>src/app/shellViewModel.ts, tests/shellViewModel.test.ts</files>
  <read_first>src/app/shellViewModel.ts, src/App.tsx, src/app/components/PerformanceMonitor.tsx, src/components/FurniturePreviewStudio.tsx, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: developer workspace tabs appear in the exact contract order `Room`, `Inventory`, `Preview Studio`, `Mob Lab`, `World`, `Session`
    - Test 2: developer session summary mapping exposes room ID, invite code, member count, presence title, and sync status for the workspace panel only
    - Test 3: developer quick-action mapping keeps `Refresh room state`, `Reset camera`, and `Reset sandbox` in the workspace but never leaks them into the player dock helper
  </behavior>
  <action>Extend `src/app/shellViewModel.ts` with exact exports `getDeveloperWorkspaceTabState(...)`, `getDeveloperSessionPanelState(...)`, and `getDeveloperQuickActions(...)`. Keep these helpers pure and responsible for tab order, labels, badges, and session copy so the workspace contract stays testable without a React integration harness. Add tests in `tests/shellViewModel.test.ts` first and ensure developer-only actions remain isolated from the player-shell helpers introduced in Plan 02.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/app/shellViewModel.ts contains "getDeveloperWorkspaceTabState"
    - src/app/shellViewModel.ts contains "getDeveloperSessionPanelState"
    - src/app/shellViewModel.ts contains "getDeveloperQuickActions"
    - src/app/shellViewModel.ts contains "Preview Studio"
    - tests/shellViewModel.test.ts contains "returns developer workspace tabs in contract order"
    - tests/shellViewModel.test.ts contains "keeps developer quick actions out of the player dock"
  </acceptance_criteria>
  <done>The developer workspace contract is encoded in pure helpers instead of being scattered across future JSX branches.</done>
</task>

<task type="auto">
  <name>Task 2: Build the developer workspace chrome and move tooling surfaces behind it</name>
  <files>src/app/components/DeveloperWorkspaceShell.tsx, src/app/components/DeveloperWorkspaceHeader.tsx, src/app/components/DeveloperWorkspaceRail.tsx, src/app/components/DeveloperSessionPanel.tsx, src/app/components/DevPanel.tsx, src/app/components/PerformanceMonitor.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, src/app/components/DeveloperWorkspaceShell.tsx, src/app/components/DevPanel.tsx, src/app/components/PerformanceMonitor.tsx, src/app/shellViewModel.ts</read_first>
  <action>Create `DeveloperWorkspaceHeader.tsx`, `DeveloperWorkspaceRail.tsx`, and `DeveloperSessionPanel.tsx` with exact root class names `developer-workspace-header`, `developer-workspace-rail`, and `developer-session-panel`. Update `DeveloperWorkspaceShell.tsx` so it supports header, rail, stage, inspector, and utility regions with active-tab rendering. Update `src/App.tsx` so `DevPanel`, `PerformanceMonitor`, room IDs, invite codes, shared-session diagnostics, reset actions, and raw world/debug controls render only when `shellViewMode === "developer"` and `import.meta.env.DEV` is true. Preserve one source of truth for all room/build/preview/session state and keep the room runtime visible in the default `Room` developer tab.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/DeveloperWorkspaceHeader.tsx contains "className=\"developer-workspace-header\""
    - src/app/components/DeveloperWorkspaceRail.tsx contains "className=\"developer-workspace-rail\""
    - src/app/components/DeveloperSessionPanel.tsx contains "className=\"developer-session-panel\""
    - src/app/components/PerformanceMonitor.tsx contains "className=\"performance-monitor\""
    - src/App.tsx contains "DeveloperWorkspaceHeader"
    - src/App.tsx contains "DeveloperWorkspaceRail"
    - src/App.tsx contains "DeveloperSessionPanel"
    - src/App.tsx does not mount "DevPanel" in the player shell branch
  </acceptance_criteria>
  <done>Development-time controls now live in a coherent workbench shell instead of sharing space with the shipped HUD.</done>
</task>

<task type="auto">
  <name>Task 3: Embed Preview Studio and Mob Lab into the developer stage instead of floating over the room shell</name>
  <files>src/components/FurniturePreviewStudio.tsx, src/app/components/InventoryPanel.tsx, src/App.tsx, src/styles.css</files>
  <read_first>src/components/FurniturePreviewStudio.tsx, src/app/components/InventoryPanel.tsx, src/App.tsx, src/styles.css, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md</read_first>
  <action>Update `src/components/FurniturePreviewStudio.tsx` to support an embedded developer-workspace presentation with an exact modifier class `preview-studio--workspace` while preserving the existing furniture and mob-lab logic, selected furniture type, selected mob preset, and persisted Mob Lab state. In `src/App.tsx`, route `openPreviewStudio` and `openMobPreviewStudio` to `shellViewMode="developer"` plus the correct `developerWorkspaceTab` so these tools become explicit authoring surfaces instead of player overlays. Reuse `InventoryPanel` as the developer inventory tab surface where appropriate instead of duplicating inventory logic. Update `src/styles.css` for the developer contract: darker neutral panels, mono utility text, left rail, right inspector, optional utility strip, responsive inspector collapse at `<= 1200px`, and no inheritance of player parchment styling.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/components/FurniturePreviewStudio.tsx contains "preview-studio--workspace"
    - src/App.tsx contains "developerWorkspaceTab"
    - src/App.tsx contains "preview_studio"
    - src/App.tsx contains "mob_lab"
    - src/styles.css contains ".developer-workspace-header"
    - src/styles.css contains ".developer-workspace-rail"
    - src/styles.css contains ".developer-session-panel"
    - src/styles.css contains ".performance-monitor"
    - src/styles.css contains "@media (max-width: 1200px)"
  </acceptance_criteria>
  <done>Preview Studio, Mob Lab, and related tooling are explicit developer workspace surfaces, keeping iteration fast without leaking authoring chrome into the player shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts
- [ ] cmd /c npm run build
- [ ] Developer-only controls, session diagnostics, Preview Studio, and Mob Lab render only in Developer View, while the player branch stays free of authoring surfaces
</verification>

<success_criteria>
Developer View becomes a real workbench: tool-first, fast to iterate in, and clearly separate from the shipped player shell while still sharing the same underlying room runtime and preview state.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-03-SUMMARY.md`
</output>
