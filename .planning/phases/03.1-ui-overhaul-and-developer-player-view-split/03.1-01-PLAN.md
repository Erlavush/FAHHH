---
phase: 03.1-ui-overhaul-and-developer-player-view-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/types.ts
  - src/lib/devWorldSettings.ts
  - src/app/shellViewModel.ts
  - src/app/components/ViewModeSwitch.tsx
  - src/app/components/PlayerRoomShell.tsx
  - src/app/components/DeveloperWorkspaceShell.tsx
  - src/App.tsx
  - src/styles.css
  - tests/devWorldSettings.test.ts
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - DEVX-01
user_setup: []
must_haves:
  truths:
    - "The app has one explicit shell view mode with separate player and developer layout branches."
    - "Switching between player and developer views does not duplicate or reset room runtime state."
    - "Developer-only surfaces are no longer mounted through the player shell branch by default."
  artifacts:
    - path: "src/app/types.ts"
      provides: "Typed shell mode and workspace tab unions"
      min_lines: 40
      contains: "export type AppShellViewMode = \"player\" | \"developer\""
    - path: "src/lib/devWorldSettings.ts"
      provides: "Persisted shell view mode and developer workspace tab"
      min_lines: 120
      contains: "shellViewMode?: AppShellViewMode"
    - path: "src/app/components/PlayerRoomShell.tsx"
      provides: "Room-first player shell container"
      min_lines: 40
      contains: "className=\"player-room-shell\""
    - path: "src/app/components/DeveloperWorkspaceShell.tsx"
      provides: "Developer workspace container"
      min_lines: 50
      contains: "className=\"developer-workspace-shell\""
    - path: "tests/devWorldSettings.test.ts"
      provides: "Shell view persistence regression coverage"
      contains: "loads shell view mode and developer workspace tab"
  key_links:
    - from: "src/lib/devWorldSettings.ts"
      to: "src/App.tsx"
      via: "persisted shell view mode and developer workspace tab state"
      pattern: "shellViewMode|developerWorkspaceTab"
    - from: "src/App.tsx"
      to: "src/app/components/PlayerRoomShell.tsx"
      via: "player branch layout composition"
      pattern: "PlayerRoomShell"
    - from: "src/App.tsx"
      to: "src/app/components/DeveloperWorkspaceShell.tsx"
      via: "developer branch layout composition"
      pattern: "DeveloperWorkspaceShell"
---

<objective>
Establish the shell architecture for Phase 03.1 by adding explicit player/developer view state, persistent workspace preferences, and dedicated shell container components.

Purpose: Fix the structural ownership problem first so later plans can redesign the player HUD and developer workbench without layering more surfaces onto the current overlay stack.
Output: Persisted shell mode and workspace tab state, pure shell view-model helpers, extracted player/developer shell containers, and `App.tsx` routed through those containers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-RESEARCH.md
@src/App.tsx
@src/lib/devWorldSettings.ts
@src/app/types.ts
@src/app/components/SceneToolbar.tsx
@src/app/components/SharedRoomStatusStrip.tsx
@src/app/components/DevPanel.tsx
@src/app/components/PerformanceMonitor.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit shell mode, developer tab, and testable shell-state helpers</name>
  <files>src/app/types.ts, src/lib/devWorldSettings.ts, src/app/shellViewModel.ts, tests/devWorldSettings.test.ts, tests/shellViewModel.test.ts</files>
  <read_first>src/app/types.ts, src/lib/devWorldSettings.ts, src/App.tsx, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `loadPersistedWorldSettings` preserves valid `shellViewMode` and `developerWorkspaceTab` values and drops unknown values back to `undefined`
    - Test 2: first-time default helper returns `developer` in dev and `player` in non-dev without mutating room state
    - Test 3: the shell view-model helper returns the exact developer tab labels `Room`, `Inventory`, `Preview Studio`, `Mob Lab`, `World`, and `Session`
  </behavior>
  <action>Update `src/app/types.ts` with the exact exports `AppShellViewMode = "player" | "developer"` and `DeveloperWorkspaceTab = "room" | "inventory" | "preview_studio" | "mob_lab" | "world" | "session"`. Extend `src/lib/devWorldSettings.ts` to sanitize, load, and save the exact optional fields `shellViewMode?: AppShellViewMode`, `developerWorkspaceTab?: DeveloperWorkspaceTab`, and `playerCompanionCardExpanded?: boolean` while preserving all existing world/debug/build settings unchanged. Create `src/app/shellViewModel.ts` with exact exports `getDefaultShellViewMode(isDev: boolean, persistedMode?: AppShellViewMode)`, `getDeveloperWorkspaceTabs()`, and `isDeveloperSurfaceVisible(shellViewMode, isDev)`. Add `tests/devWorldSettings.test.ts` and `tests/shellViewModel.test.ts` first so persistence and tab metadata are covered before wiring UI components.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/devWorldSettings.test.ts tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/app/types.ts contains "export type AppShellViewMode = \"player\" | \"developer\""
    - src/app/types.ts contains "export type DeveloperWorkspaceTab ="
    - src/lib/devWorldSettings.ts contains "shellViewMode?: AppShellViewMode"
    - src/lib/devWorldSettings.ts contains "developerWorkspaceTab?: DeveloperWorkspaceTab"
    - src/app/shellViewModel.ts contains "Preview Studio"
    - tests/devWorldSettings.test.ts contains "loads shell view mode and developer workspace tab"
  </acceptance_criteria>
  <done>The shell split has typed, persisted, and testable state instead of being an ad hoc JSX branch.</done>
</task>

<task type="auto">
  <name>Task 2: Extract player and developer shell containers and route `App.tsx` through them</name>
  <files>src/app/components/ViewModeSwitch.tsx, src/app/components/PlayerRoomShell.tsx, src/app/components/DeveloperWorkspaceShell.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, src/app/components/SceneToolbar.tsx, src/app/components/SharedRoomStatusStrip.tsx, src/app/components/InventoryPanel.tsx, src/app/components/DevPanel.tsx, src/app/components/PerformanceMonitor.tsx, src/app/shellViewModel.ts</read_first>
  <action>Create `src/app/components/ViewModeSwitch.tsx` as a dev-only segmented switch with exact visible labels `Player View` and `Developer View`. Create `src/app/components/PlayerRoomShell.tsx` that accepts slots for the room stage, top-left, top-right, bottom-left, bottom-center, drawer, and overlays and renders a root container with `className="player-room-shell"`. Create `src/app/components/DeveloperWorkspaceShell.tsx` that accepts header, rail, stage, inspector, and utility slots and renders a root container with `className="developer-workspace-shell"`. Update `src/App.tsx` so it owns `shellViewMode`, `developerWorkspaceTab`, and `playerCompanionCardExpanded` from `devWorldSettings`, renders `ViewModeSwitch` only when `import.meta.env.DEV` is true, and mounts `RoomView`, entry overlays, and blocking overlays through the correct shell container instead of the current shared overlay stack. Keep one source of truth for `buildModeEnabled`, `catalogOpen`, preview state, shared-room runtime state, and room mutations; do not duplicate these per shell branch.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/ViewModeSwitch.tsx contains "Player View"
    - src/app/components/ViewModeSwitch.tsx contains "Developer View"
    - src/app/components/PlayerRoomShell.tsx contains "className=\"player-room-shell\""
    - src/app/components/DeveloperWorkspaceShell.tsx contains "className=\"developer-workspace-shell\""
    - src/App.tsx contains "shellViewMode"
    - src/App.tsx contains "PlayerRoomShell"
    - src/App.tsx contains "DeveloperWorkspaceShell"
  </acceptance_criteria>
  <done>The app shell now has explicit player and developer composition roots, making the later visual rebuilds safe and targeted.</done>
</task>

<task type="auto">
  <name>Task 3: Replace legacy overlay-offset CSS with shell layout foundations</name>
  <files>src/styles.css, src/App.tsx</files>
  <read_first>src/styles.css, src/App.tsx, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md</read_first>
  <action>Update `src/styles.css` to add exact shell layout classes `.player-room-shell`, `.developer-workspace-shell`, `.app-shell__stage`, `.app-shell__overlay-layer`, and `.developer-workspace-shell__inspector`. Remove dependence on the legacy sibling-offset layout rules that shift `.scene-toolbar`, `.spawn-panel`, `.dev-panel`, and `.scene-note` based on `.shared-room-status`. Keep `RoomView`, `EditDock`, `PlacementActions`, and `PcMinigameOverlay` layering intact by anchoring the shared room stage to `.app-shell__stage` and the HUD/layout chrome to explicit overlay containers. Do not finalize player styling yet; this task only lays down the shell-level geometry and z-index contracts needed by Plans 02 and 03.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/styles.css contains ".player-room-shell"
    - src/styles.css contains ".developer-workspace-shell"
    - src/styles.css contains ".app-shell__stage"
    - src/styles.css contains ".app-shell__overlay-layer"
    - src/styles.css contains ".developer-workspace-shell__inspector"
  </acceptance_criteria>
  <done>The repo has shell-level layout primitives instead of one overlay stack controlled by sibling offset hacks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/devWorldSettings.test.ts tests/shellViewModel.test.ts
- [ ] cmd /c npm run build
- [ ] `App.tsx` routes through explicit player and developer shell containers, and shell mode plus developer tab state persist through `devWorldSettings.ts`
</verification>

<success_criteria>
The shell split is architectural instead of cosmetic: the app can now render the same room runtime through two explicit layout branches with persisted view-mode preferences.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-01-SUMMARY.md`
</output>
