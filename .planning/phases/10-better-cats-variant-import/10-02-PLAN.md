---
phase: 10-better-cats-variant-import
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/mobLab.ts
  - src/lib/mobLabState.ts
  - src/components/mob-lab/GlbMobPreviewActor.tsx
  - tests/mobLabState.test.ts
autonomous: true
requirements:
  - PETS-05
user_setup: []
must_haves:
  truths:
    - "Better Cats presets can render distinct coats because the GLB actor honors preset-provided variant texture data."
    - "The current Better Cats fixups remain intact: ghost meshes stay hidden and the tail hierarchy correction still happens."
    - "Curated cat variants are previewable through the existing imported-preset and Mob Lab path before live-room adoption uses them."
  artifacts:
    - path: "src/lib/mobLab.ts"
      provides: "Variant-aware imported preset schema and curated Better Cats preset registration"
      contains: "variantId"
    - path: "src/components/mob-lab/GlbMobPreviewActor.tsx"
      provides: "Better Cats texture override seam that preserves existing mesh filters and tail fix"
      contains: "tailBase.attach(tailTip)"
    - path: "tests/mobLabState.test.ts"
      provides: "Validation coverage for any new Better Cats preset metadata"
      contains: "variantId"
---

<objective>
Make the imported-preset and GLB rendering path truly variant-aware so curated Better Cats coats preview and render correctly.

Purpose: carry curated Better Cats metadata through the preset schema and teach the Better Cats GLB actor to apply variant texture overrides without regressing the known export fixes.
Output: variant-aware imported preset schema, registered Better Cats presets, and a GLB renderer that can show visually distinct curated cat coats.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-better-cats-variant-import/10-CONTEXT.md
@.planning/phases/10-better-cats-variant-import/10-RESEARCH.md
@src/lib/catVariants.ts
@src/lib/mobLab.ts
@src/lib/mobLabState.ts
@src/lib/mob-presets/better_cat_glb.json
@src/components/mob-lab/GlbMobPreviewActor.tsx
@docs/MOB_LAB.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend imported preset data for curated Better Cats variants</name>
  <files>src/lib/mobLab.ts, src/lib/mobLabState.ts, tests/mobLabState.test.ts</files>
  <read_first>src/lib/mobLab.ts, src/lib/mobLabState.ts, src/lib/catVariants.ts, tests/mobLabState.test.ts, .planning/phases/10-better-cats-variant-import/10-RESEARCH.md</read_first>
  <action>Extend `ImportedMobPreset` in `src/lib/mobLab.ts` with the exact optional field `variantId?: BetterCatVariantId` and register the curated Better Cats preset files from Plan 10-01 in `DEFAULT_IMPORTED_MOB_PRESETS`. Update `src/lib/mobLabState.ts` validation and migration so `variantId` survives export/import and browser-local persistence. Add or expand `tests/mobLabState.test.ts` first so the new variant metadata is validated before renderer work begins.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/mobLabState.test.ts</verify>
  <acceptance_criteria>
    - src/lib/mobLab.ts contains "variantId?: BetterCatVariantId"
    - src/lib/mobLab.ts contains at least one curated Better Cats preset import beyond `better_cat_glb`
    - src/lib/mobLabState.ts contains "variantId"
    - tests/mobLabState.test.ts contains "variantId"
  </acceptance_criteria>
  <done>The imported-preset pipeline can now describe curated Better Cats looks as first-class preset metadata.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply curated coat textures inside the Better Cats GLB actor without regressing known fixups</name>
  <files>src/components/mob-lab/GlbMobPreviewActor.tsx</files>
  <read_first>src/components/mob-lab/GlbMobPreviewActor.tsx, src/lib/catVariants.ts, src/lib/mobLab.ts, .planning/todos/pet-system/import-better-cat-variants.md, .planning/phases/10-better-cats-variant-import/10-CONTEXT.md</read_first>
  <action>Update `src/components/mob-lab/GlbMobPreviewActor.tsx` so Better Cats presets apply their curated `textureSrc` and any safe overlay data from `src/lib/catVariants.ts` to the cloned GLB scene. Preserve the exact current Better Cats cleanup behavior: continue hiding `thin`, `fluff`, `bobtail`, `small_leg`, and ghost ear meshes, keep `ear4` and `ear5`, and keep the manual `tailBase.attach(tailTip)` hierarchy correction. If the coat override is unavailable, fall back safely to the existing embedded-material behavior instead of crashing or rendering broken materials.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/components/mob-lab/GlbMobPreviewActor.tsx contains a texture or material override path keyed by preset metadata
    - src/components/mob-lab/GlbMobPreviewActor.tsx still contains "tailBase.attach(tailTip)"
    - src/components/mob-lab/GlbMobPreviewActor.tsx still contains the Better Cats ghost-mesh filtering branch for ears and variant meshes
  </acceptance_criteria>
  <done>Curated Better Cats presets now render as distinct cats instead of all sharing the same look.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/mobLabState.test.ts
- [ ] cmd /c npm run build
- [ ] Curated Better Cats presets preview with distinct coats in Mob Lab or the live imported actor path
</verification>

<success_criteria>
Curated Better Cats presets can now survive preset persistence and render as distinct looks through the existing GLB actor seam.
</success_criteria>

<output>
After completion, create `.planning/phases/10-better-cats-variant-import/10-02-SUMMARY.md`
</output>
