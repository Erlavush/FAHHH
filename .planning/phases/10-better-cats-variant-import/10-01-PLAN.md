---
phase: 10-better-cats-variant-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/import_better_cat_variants.mjs
  - public/textures/cats/better-cats/
  - src/lib/catVariants.ts
  - src/lib/mob-presets/
  - tests/catVariants.test.ts
autonomous: true
requirements:
  - PETS-05
user_setup: []
must_haves:
  truths:
    - "The Better Cats download is translated into repo-owned textures and preset metadata, so the game no longer depends on the external Downloads path at runtime."
    - "A curated first-wave Better Cats variant set exists as stable ids and labels instead of one-off hard-coded strings spread across the app."
    - "The imported variant source of truth is explicit enough that more cat coats can be added later without re-inventing the pipeline."
  artifacts:
    - path: "scripts/import_better_cat_variants.mjs"
      provides: "Repeatable import path from the Better Cats pack into repo-owned assets"
      contains: "Better Cats v4.0 1.21.10"
    - path: "src/lib/catVariants.ts"
      provides: "Curated Better Cats variant ids, labels, and asset metadata"
      contains: "export const BETTER_CAT_VARIANTS"
    - path: "tests/catVariants.test.ts"
      provides: "Regression coverage for curated variant metadata and preset references"
      contains: "describes the curated Better Cats launch set"
---

<objective>
Create a curated, repo-owned Better Cats asset and metadata foundation before runtime or catalog work begins.

Purpose: copy the chosen Better Cats coats out of the external download, define stable variant ids and labels, and generate or author the preset metadata that later plans will use in Mob Lab and the live room.
Output: import script, curated manifest, checked-in textures, and curated Better Cats preset definitions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pet-system/import-better-cat-variants.md
@.planning/phases/10-better-cats-variant-import/10-CONTEXT.md
@.planning/phases/10-better-cats-variant-import/10-RESEARCH.md
@docs/MOB_LAB.md
@src/lib/mob-presets/better_cat_glb.json
@src/lib/mobLab.ts
@scripts/generate_minecraft_vanilla_cat_rig.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a repeatable Better Cats import script and curated variant manifest</name>
  <files>scripts/import_better_cat_variants.mjs, src/lib/catVariants.ts, tests/catVariants.test.ts</files>
  <read_first>scripts/generate_minecraft_vanilla_cat_rig.mjs, .planning/todos/pet-system/import-better-cat-variants.md, .planning/phases/10-better-cats-variant-import/10-CONTEXT.md, .planning/phases/10-better-cats-variant-import/10-RESEARCH.md, src/lib/mobLab.ts</read_first>
  <action>Create `scripts/import_better_cat_variants.mjs` as a repeatable importer with the default source path `C:\Users\user\Downloads\Better Cats v4.0 1.21.10`. The script must copy the curated first-wave coat textures `tabby`, `red`, `calico`, `siamese`, `british_shorthair`, `ragdoll`, `black`, and `white` from `assets/minecraft/textures/entity/cat/` into repo-owned files under `public/textures/cats/better-cats/`. In `src/lib/catVariants.ts`, add the exact exported source-of-truth collections `export type BetterCatVariantId = ...` and `export const BETTER_CAT_VARIANTS = [...]` with stable ids, labels, coat texture paths, and optional overlay references. Add `tests/catVariants.test.ts` first so the curated launch set and file-path references are locked before runtime wiring lands.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/catVariants.test.ts</verify>
  <acceptance_criteria>
    - scripts/import_better_cat_variants.mjs contains "Better Cats v4.0 1.21.10"
    - src/lib/catVariants.ts contains "export const BETTER_CAT_VARIANTS"
    - src/lib/catVariants.ts contains "british_shorthair"
    - src/lib/catVariants.ts contains "siamese"
    - tests/catVariants.test.ts contains "describes the curated Better Cats launch set"
  </acceptance_criteria>
  <done>The repo now has a stable, repeatable Better Cats variant foundation instead of one external download folder.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate curated Better Cats preset definitions from the manifest</name>
  <files>src/lib/mob-presets/, src/lib/catVariants.ts, tests/catVariants.test.ts</files>
  <read_first>src/lib/mob-presets/better_cat_glb.json, src/lib/catVariants.ts, src/lib/mobLab.ts, .planning/phases/10-better-cats-variant-import/10-RESEARCH.md</read_first>
  <action>Use the curated manifest to create checked-in Better Cats preset definitions under `src/lib/mob-presets/` for the first-wave variant set. Each curated preset must reuse the existing `sourceMobPath` `/models/better_cat.glb`, point `textureSrc` at the copied coat texture in `public/textures/cats/better-cats/`, carry a stable `variantId`, and use user-facing labels like `Better Cat - Siamese` and `Better Cat - Calico`. Keep physics, stage, and locomotion aligned with the current `better_cat_glb.json` baseline unless the imported look proves a specific value must differ.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/catVariants.test.ts</verify>
  <acceptance_criteria>
    - src/lib/mob-presets/ contains a preset file for the curated Better Cats launch set
    - at least one generated Better Cats preset contains '"variantId": "siamese"'
    - at least one generated Better Cats preset contains '"sourceMobPath": "/models/better_cat.glb"'
    - at least one generated Better Cats preset contains '"textureSrc": "/textures/cats/better-cats/'
  </acceptance_criteria>
  <done>The curated Better Cats looks now exist as concrete imported presets ready for preview and runtime wiring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/catVariants.test.ts
- [ ] Curated Better Cats coats live under repo-owned `public/` paths
- [ ] Curated preset metadata exists for the first-wave launch set
</verification>

<success_criteria>
Phase 10 now has a repeatable Better Cats import foundation and curated preset library to build on.
</success_criteria>

<output>
After completion, create `.planning/phases/10-better-cats-variant-import/10-01-SUMMARY.md`
</output>
