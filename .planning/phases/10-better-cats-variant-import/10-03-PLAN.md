---
phase: 10-better-cats-variant-import
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/lib/pets.ts
  - src/lib/devLocalState.ts
  - src/app/hooks/useAppRoomActions.ts
  - src/App.tsx
  - src/components/ui/InventoryPanel.tsx
  - tests/pets.test.ts
  - tests/devLocalState.test.ts
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - PETS-05
user_setup: []
must_haves:
  truths:
    - "The local room can adopt curated Better Cats looks without creating a separate gameplay species per breed."
    - "Owned cats keep their chosen Better Cats preset or variant across sandbox persistence and legacy cats normalize safely."
    - "The Phase 09 cat-care and multi-cat loop remains intact while the adoption catalog becomes visually richer."
  artifacts:
    - path: "src/lib/pets.ts"
      provides: "Cat catalog data and owned-pet creation helpers that can target curated Better Cats presets"
      contains: "minecraft_cat"
    - path: "src/app/hooks/useAppRoomActions.ts"
      provides: "Local cat adoption flow that accepts the selected curated Better Cats preset"
      contains: "presetId"
    - path: "src/lib/devLocalState.ts"
      provides: "Persistence normalization for legacy cats and curated Better Cats presets"
      contains: "better_cat_glb"
    - path: "tests/devLocalState.test.ts"
      provides: "Regression coverage for persisted curated Better Cats cats"
      contains: "curated Better Cats"
---

<objective>
Wire curated Better Cats variants into the live sandbox adoption and persistence loop.

Purpose: let the player choose distinct imported cat looks in the current room runtime, persist those choices safely, and keep the Phase 09 cat-care showcase loop working.
Output: curated cat catalog entries, variant-aware local adoption, persistence-safe owned cats, and minimal shell/catalog wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-better-cats-variant-import/10-CONTEXT.md
@.planning/phases/10-better-cats-variant-import/10-RESEARCH.md
@src/lib/catVariants.ts
@src/lib/pets.ts
@src/lib/devLocalState.ts
@src/app/hooks/useAppRoomActions.ts
@src/App.tsx
@src/components/ui/InventoryPanel.tsx
@tests/pets.test.ts
@tests/devLocalState.test.ts
@tests/shellViewModel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a curated Better Cats catalog model to the gameplay pet domain</name>
  <files>src/lib/pets.ts, tests/pets.test.ts</files>
  <read_first>src/lib/pets.ts, src/lib/catVariants.ts, tests/pets.test.ts, .planning/phases/10-better-cats-variant-import/10-RESEARCH.md</read_first>
  <action>Keep `minecraft_cat` as the gameplay species in `src/lib/pets.ts`, but add a curated Better Cats catalog collection that exposes distinct adoptable entries keyed by preset id and user-facing label. Update owned-pet creation helpers so a local sandbox cat can be created with the selected curated Better Cats preset instead of always hard-coding `better_cat_glb`. Add or expand `tests/pets.test.ts` first so the curated launch set, default fallback preset, and cat creation behavior are locked before app wiring lands.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/pets.test.ts</verify>
  <acceptance_criteria>
    - src/lib/pets.ts contains a curated Better Cats catalog export
    - src/lib/pets.ts contains at least one curated Better Cats preset id beyond `better_cat_glb`
    - tests/pets.test.ts contains a case covering the curated Better Cats catalog
  </acceptance_criteria>
  <done>The gameplay pet domain can now describe multiple adoptable Better Cats looks without multiplying behavior logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire curated Better Cats adoption, persistence, and minimal store surfacing</name>
  <files>src/lib/devLocalState.ts, src/app/hooks/useAppRoomActions.ts, src/App.tsx, src/components/ui/InventoryPanel.tsx, tests/devLocalState.test.ts, tests/shellViewModel.test.ts</files>
  <read_first>src/lib/devLocalState.ts, src/app/hooks/useAppRoomActions.ts, src/App.tsx, src/components/ui/InventoryPanel.tsx, tests/devLocalState.test.ts, .planning/phases/10-better-cats-variant-import/10-CONTEXT.md</read_first>
  <action>Update the local sandbox adoption flow so the player buys a specific curated Better Cats catalog entry and the created owned pet keeps that preset id through local persistence. Normalize legacy cats that only know `better_cat_glb` to the default curated baseline without deleting them. Keep the hosted shared-room branch on the current shared cat contract unless there is a no-risk compatibility extension. In `src/components/ui/InventoryPanel.tsx` and `src/App.tsx`, add only the minimal data and prop wiring required to show multiple adoptable cat looks; do not perform a broad UI redesign. Add regression coverage for curated Better Cats persistence and any new shell-facing summary data.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/pets.test.ts tests/devLocalState.test.ts tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useAppRoomActions.ts contains the selected curated cat preset or catalog entry in the local buy flow
    - src/lib/devLocalState.ts contains normalization for legacy `better_cat_glb` cats
    - tests/devLocalState.test.ts contains "curated Better Cats"
    - src/components/ui/InventoryPanel.tsx contains multiple curated cat store entries or data-driven rendering for them
  </acceptance_criteria>
  <done>The player can now adopt and keep visually distinct Better Cats cats inside the current Phase 09 sandbox loop.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/pets.test.ts tests/devLocalState.test.ts tests/shellViewModel.test.ts
- [ ] cmd /c npm run build
- [ ] Local sandbox can own visually distinct Better Cats cats without regressing the Phase 09 care loop
</verification>

<success_criteria>
The local cat-sanctuary runtime now exposes curated Better Cats variants as adoptable, persistence-safe cats.
</success_criteria>

<output>
After completion, create `.planning/phases/10-better-cats-variant-import/10-03-SUMMARY.md`
</output>
