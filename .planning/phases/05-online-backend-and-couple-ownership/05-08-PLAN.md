---
phase: 05-online-backend-and-couple-ownership
plan: 08
type: execute
gap_closure: true
wave: 2
depends_on:
  - 07
files_modified:
  - src/lib/furnitureRegistry.ts
  - src/lib/furnitureInteractions.ts
  - src/components/room-view/useRoomViewInteractions.ts
  - src/components/MinecraftPlayer.tsx
  - tests/furnitureInteractions.test.ts
  - tests/sharedRoomPresenceUx.test.ts
autonomous: true
requirements:
  - ROOM-05
user_setup: []
must_haves:
  truths:
    - "If both partners lie on the same bed, the two avatars must land on clearly separated mattress sides rather than partially overlapping."
    - "Shared presence must continue to carry the chosen bed slot so both clients mirror the same side."
    - "Single-player bed use and standing up still work normally after the pose retune."
  artifacts:
    - path: "src/lib/furnitureRegistry.ts"
      provides: "Bed slot offsets and pose offsets retuned for visible side separation"
      contains: "interactionSecondaryOffset|interactionSecondaryPoseOffset"
    - path: "src/lib/furnitureInteractions.ts"
      provides: "Slot-aware bed targeting remains stable while using the tuned offsets"
      contains: "slotId"
    - path: "tests/furnitureInteractions.test.ts"
      provides: "Bed slot and separation regression coverage"
      contains: "bed"
  key_links:
    - from: "src/lib/furnitureRegistry.ts"
      to: "src/lib/furnitureInteractions.ts"
      via: "retuned offsets feed the occupancy-aware slot selector"
      pattern: "interactionOffset|interactionSecondaryOffset"
    - from: "src/lib/furnitureInteractions.ts"
      to: "src/components/room-view/useRoomViewInteractions.ts"
      via: "selected slot remains mirrored through shared presence"
      pattern: "slotId"
---

<objective>
Close the remaining Phase 05 bed-sharing gap where partners still overlap visually after the occupancy-aware slot patch.

Purpose: make the two bed slots read as real left/right positions on the rendered bed, not just logical slot IDs in presence state.
Output: retuned bed offsets and pose offsets, slot-aware pose mirroring that stays visually separate, and regression coverage for two-partner bed use.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-online-backend-and-couple-ownership/05-UAT.md
@.planning/debug/phase-05-bed-side-overlap.md
@src/lib/furnitureRegistry.ts
@src/lib/furnitureInteractions.ts
@src/components/room-view/useRoomViewInteractions.ts
@src/components/MinecraftPlayer.tsx
@tests/furnitureInteractions.test.ts
@tests/sharedRoomPresenceUx.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-tune bed slot offsets and pose offsets so both sides are visibly separate</name>
  <files>src/lib/furnitureRegistry.ts, src/lib/furnitureInteractions.ts, tests/furnitureInteractions.test.ts</files>
  <read_first>src/lib/furnitureRegistry.ts, src/lib/furnitureInteractions.ts, .planning/debug/phase-05-bed-side-overlap.md, tests/furnitureInteractions.test.ts</read_first>
  <behavior>
    - Test 1: the primary and secondary bed slots resolve to distinct rendered positions rather than nearly the same mattress lane
    - Test 2: full-bed occupancy still refuses a third claim predictably
    - Test 3: the new offsets do not break single-player bed use
  </behavior>
  <action>Retune the bed definition's interaction offsets and pose offsets to match the actual rendered bed width, and keep the slot-aware selector stable on top of those tuned values. Add tests first so slot separation is asserted before the registry constants change.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/furnitureInteractions.test.ts</verify>
  <acceptance_criteria>
    - the bed registry exposes clearly separated primary and secondary lie offsets
    - slot-aware interaction selection still works for first-slot, second-slot, and full-bed cases
    - tests/furnitureInteractions.test.ts protects the tuned bed-slot contract
  </acceptance_criteria>
  <done>The bed offers two visibly distinct lie targets instead of two nearly overlapping ones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify rendered bed poses mirror the chosen slot cleanly on both clients</name>
  <files>src/components/room-view/useRoomViewInteractions.ts, src/components/MinecraftPlayer.tsx, tests/sharedRoomPresenceUx.test.ts</files>
  <read_first>src/components/room-view/useRoomViewInteractions.ts, src/components/MinecraftPlayer.tsx, .planning/debug/phase-05-bed-side-overlap.md, tests/sharedRoomPresenceUx.test.ts</read_first>
  <behavior>
    - Test 1: both clients render the same chosen bed slot and do not collapse into the same visual pose
    - Test 2: standing up frees the slot and later reuse still lands on the correct side
    - Test 3: pose offsets remain correct for local and remote lying avatars
  </behavior>
  <action>Audit the final lie pose application path so the tuned slot offsets survive through shared presence into the actual `MinecraftPlayer` lie pose. Add runtime coverage first, then adjust pose application if needed so mirrored slots stay visually separate on both clients.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresenceUx.test.ts tests/furnitureInteractions.test.ts</verify>
  <acceptance_criteria>
    - shared presence continues to preserve the chosen bed slot
    - the final local and remote lie poses stay visibly separated on the mattress
    - tests/sharedRoomPresenceUx.test.ts protects slot mirroring and reuse
  </acceptance_criteria>
  <done>Two-player bed sharing finally reads as side-by-side instead of stacked avatars.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/furnitureInteractions.test.ts tests/sharedRoomPresenceUx.test.ts
- [ ] Bed slots are visually separated in the rendered bed pose, not only logically distinct
- [ ] Leaving the bed frees the slot for reuse
</verification>

<success_criteria>
The shared bed now behaves like a real two-person bed: one slot per partner, visibly separated, mirrored correctly on both clients.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-08-SUMMARY.md`
</output>
