---
phase: 05-online-backend-and-couple-ownership
plan: 05
type: execute
gap_closure: true
wave: 2
depends_on:
  - 04
files_modified:
  - src/lib/sharedPresenceTypes.ts
  - src/lib/sharedPresenceStore.ts
  - src/lib/sharedPresenceClient.ts
  - src/lib/firebasePresenceStore.ts
  - scripts/sharedRoomDevPlugin.mjs
  - src/app/hooks/useSharedRoomPresence.ts
  - src/components/RoomView.tsx
  - src/components/MinecraftPlayer.tsx
  - src/lib/sharedRoomPet.ts
  - src/components/room-view/RoomPetActor.tsx
  - tests/sharedRoomPresence.test.ts
  - tests/sharedRoomPresenceUx.test.ts
  - tests/sharedRoomPet.test.ts
autonomous: true
requirements:
  - ROOM-04
  - ROOM-05
user_setup: []
must_haves:
  truths:
    - "The shared cat must render from one authoritative live state so both partners see the same location and behavior."
    - "Remote partner movement must feel continuous enough to read as walking rather than one-second snap increments."
    - "Live pet and movement sync remain outside canonical room revision churn."
  artifacts:
    - path: "src/lib/sharedPresenceTypes.ts"
      provides: "Ephemeral live-state contract for smoother remote actors and shared pet state"
      min_lines: 60
      contains: "export interface SharedPresenceSnapshot"
    - path: "src/components/room-view/RoomPetActor.tsx"
      provides: "Shared-pet actor driven from authoritative live motion state"
      min_lines: 120
      contains: "RoomPetActor"
    - path: "src/components/MinecraftPlayer.tsx"
      provides: "Improved remote-motion interpolation or presentation path"
      min_lines: 300
      contains: "mode === \"remote\""
    - path: "tests/sharedRoomPet.test.ts"
      provides: "Shared-pet sync coverage"
      contains: "shared"
  key_links:
    - from: "src/lib/sharedPresenceTypes.ts"
      to: "src/app/hooks/useSharedRoomPresence.ts"
      via: "live motion payload is published and consumed through the presence seam"
      pattern: "SharedPresenceSnapshot"
    - from: "src/app/hooks/useSharedRoomPresence.ts"
      to: "src/components/RoomView.tsx"
      via: "remote actor and pet live state feed the room runtime"
      pattern: "remotePresence"
    - from: "src/components/RoomView.tsx"
      to: "src/components/MinecraftPlayer.tsx"
      via: "remote partner render consumes richer movement state"
      pattern: "mode=\"remote\""
---

<objective>
Close the Phase 05 UAT gaps around the shared cat diverging across clients and remote partner movement reading as snap-forward teleporting.

Purpose: keep canonical room revisions authoritative for committed room state while introducing enough ephemeral live-state fidelity that both partners see the same cat behavior and smoother partner locomotion.
Output: richer ephemeral sync contract, shared cat live-state source of truth, improved remote movement interpolation/presentation, and regression coverage that protects room revision boundaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-online-backend-and-couple-ownership/05-UAT.md
@.planning/debug/phase-05-shared-cat-diverges.md
@.planning/debug/phase-05-remote-avatar-snaps.md
@src/lib/sharedPresenceTypes.ts
@src/app/hooks/useSharedRoomPresence.ts
@src/components/RoomView.tsx
@src/components/MinecraftPlayer.tsx
@src/lib/sharedRoomPet.ts
@src/components/room-view/RoomPetActor.tsx
@tests/sharedRoomPresence.test.ts
@tests/sharedRoomPresenceUx.test.ts
@tests/sharedRoomPet.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared ephemeral motion contract for remote actors and the shared cat</name>
  <files>src/lib/sharedPresenceTypes.ts, src/lib/sharedPresenceStore.ts, src/lib/sharedPresenceClient.ts, src/lib/firebasePresenceStore.ts, scripts/sharedRoomDevPlugin.mjs, tests/sharedRoomPresence.test.ts, tests/sharedRoomPet.test.ts</files>
  <read_first>src/lib/sharedPresenceTypes.ts, src/lib/sharedPresenceStore.ts, src/lib/firebasePresenceStore.ts, scripts/sharedRoomDevPlugin.mjs, .planning/debug/phase-05-shared-cat-diverges.md, .planning/debug/phase-05-remote-avatar-snaps.md, tests/sharedRoomPresence.test.ts, tests/sharedRoomPet.test.ts</read_first>
  <behavior>
    - Test 1: shared live-state updates can carry enough motion information for remote avatars without mutating canonical room revisions
    - Test 2: the shared cat publishes or loads one authoritative live motion snapshot instead of each client inventing its own wander path
    - Test 3: presence and pet live state remain ephemeral and do not bump room revision counts
  </behavior>
  <action>Extend the ephemeral live-state seam so it can carry the minimum motion data needed for smooth remote actors and one shared cat simulation. Do not push this into canonical room commits; keep it behind the presence/live-state channel. Update the dev plugin and Firebase presence adapter together so both backends support the richer live-state payload, then add tests first in `tests/sharedRoomPresence.test.ts` and `tests/sharedRoomPet.test.ts` to lock the no-room-revision-churn rule before runtime consumers change.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresence.test.ts tests/sharedRoomPet.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedPresenceTypes.ts contains richer ephemeral live-state fields beyond bare position/facing timestamps
    - src/lib/firebasePresenceStore.ts and scripts/sharedRoomDevPlugin.mjs both support the extended live-state contract
    - tests/sharedRoomPresence.test.ts proves live-state updates do not mutate canonical room revisions
  </acceptance_criteria>
  <done>The runtime has a single ephemeral channel that can drive better remote locomotion and one shared cat state without polluting room commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive RoomView, MinecraftPlayer, and the cat actor from the shared live-state contract</name>
  <files>src/app/hooks/useSharedRoomPresence.ts, src/components/RoomView.tsx, src/components/MinecraftPlayer.tsx, src/lib/sharedRoomPet.ts, src/components/room-view/RoomPetActor.tsx, tests/sharedRoomPresenceUx.test.ts, tests/sharedRoomPet.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomPresence.ts, src/components/RoomView.tsx, src/components/MinecraftPlayer.tsx, src/lib/sharedRoomPet.ts, src/components/room-view/RoomPetActor.tsx, .planning/debug/phase-05-shared-cat-diverges.md, .planning/debug/phase-05-remote-avatar-snaps.md, tests/sharedRoomPresenceUx.test.ts, tests/sharedRoomPet.test.ts</read_first>
  <behavior>
    - Test 1: remote players render with buffered motion or interpolation that reads as walking between updates rather than jump-forward snapping
    - Test 2: both clients render the shared cat from the same live motion source rather than independent local wandering
    - Test 3: if live-state updates pause briefly, the last known state degrades gracefully without immediate visible jitter
  </behavior>
  <action>Update `useSharedRoomPresence`, `RoomView`, `MinecraftPlayer`, and `RoomPetActor` so remote partner motion and shared-cat motion are both driven from the shared ephemeral contract instead of per-client reconstruction alone. Preserve the existing player/developer shell split and keep canonical room state untouched. Add or expand tests first so partner motion smoothness and cat-state convergence are protected before tuning interpolation or actor behavior.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresenceUx.test.ts tests/sharedRoomPet.test.ts</verify>
  <acceptance_criteria>
    - src/components/MinecraftPlayer.tsx contains a richer remote-motion presentation path than simple target snapping
    - src/components/room-view/RoomPetActor.tsx no longer derives authoritative cat wandering purely from local refs
    - tests/sharedRoomPet.test.ts contains cross-client shared-cat motion coverage
  </acceptance_criteria>
  <done>Remote movement and shared cat behavior now feel like one live room rather than two separate local simulations.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresence.test.ts tests/sharedRoomPresenceUx.test.ts tests/sharedRoomPet.test.ts
- [ ] Shared cat live state stays outside canonical room revision churn
- [ ] Remote partner movement is visibly smoother than the prior snap-forward behavior
</verification>

<success_criteria>
The couple room now has one shared live actor layer: the cat no longer diverges per browser, and remote partner movement reads as deliberate locomotion instead of sparse teleport steps.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-05-SUMMARY.md`
</output>
