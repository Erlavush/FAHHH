---
phase: 05-online-backend-and-couple-ownership
plan: 07
type: execute
gap_closure: true
wave: 1
depends_on:
  - 05
files_modified:
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/App.tsx
  - src/app/hooks/useSharedRoomPresence.ts
  - src/components/MinecraftPlayer.tsx
  - src/components/room-view/RoomPetActor.tsx
  - src/components/RoomView.tsx
  - src/lib/sharedRoomPet.ts
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomPet.test.ts
  - tests/sharedRoomPresenceUx.test.ts
autonomous: true
requirements:
  - ROOM-04
  - ROOM-05
user_setup: []
must_haves:
  truths:
    - "A first-time shared cat adoption must appear on the passive browser without requiring that browser to click adopt or manually reload."
    - "Remote partner and shared cat movement must feel substantially smoother than the current snap-stepping presentation."
    - "Canonical shared-pet ownership stays in the room document, while live actor playback stays on the ephemeral presence seam."
  artifacts:
    - path: "src/app/hooks/useSharedRoomRuntime.ts"
      provides: "Passive canonical room refresh or subscription path for shared-pet adoption visibility"
      contains: "loadHostedRoom|loadLegacyRoom|reloadRoom"
    - path: "src/components/MinecraftPlayer.tsx"
      provides: "Buffered remote actor playback instead of direct target smoothing"
      contains: "mode === \"remote\""
    - path: "src/components/room-view/RoomPetActor.tsx"
      provides: "Shared cat playback that follows the same smoother replica path"
      contains: "sharedLiveState"
    - path: "tests/sharedRoomPet.test.ts"
      provides: "Cross-client adoption visibility and shared cat playback coverage"
      contains: "shared"
  key_links:
    - from: "src/app/hooks/useSharedRoomRuntime.ts"
      to: "src/App.tsx"
      via: "canonical shared-pet adoption becomes visible without local interaction on the passive browser"
      pattern: "runtimeSnapshot?.sharedPet"
    - from: "src/app/hooks/useSharedRoomPresence.ts"
      to: "src/components/MinecraftPlayer.tsx"
      via: "buffered motion playback for partner replicas"
      pattern: "motion|remoteMotion"
    - from: "src/app/hooks/useSharedRoomPresence.ts"
      to: "src/components/room-view/RoomPetActor.tsx"
      via: "shared cat live-state playback uses the same smoother replica contract"
      pattern: "sharedPetState|sharedLiveState"
---

<objective>
Close the remaining Phase 05 gaps around passive-browser cat adoption visibility and snap-like live actor playback.

Purpose: make the shared pet appear as soon as one partner adopts it, and make both partner and pet motion feel closer to a real multiplayer actor instead of sparse step-wise teleporting.
Output: passive canonical shared-pet refresh, buffered replica playback for partner and cat motion, and regression coverage for first-adoption visibility plus smoother live actor presentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-online-backend-and-couple-ownership/05-UAT.md
@.planning/debug/phase-05-shared-cat-diverges.md
@.planning/debug/phase-05-remote-avatar-snaps.md
@src/app/hooks/useSharedRoomRuntime.ts
@src/App.tsx
@src/app/hooks/useSharedRoomPresence.ts
@src/components/MinecraftPlayer.tsx
@src/components/room-view/RoomPetActor.tsx
@src/components/RoomView.tsx
@tests/sharedRoomRuntime.test.ts
@tests/sharedRoomPet.test.ts
@tests/sharedRoomPresenceUx.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface newly adopted shared pets on passive browsers without local interaction</name>
  <files>src/app/hooks/useSharedRoomRuntime.ts, src/App.tsx, src/lib/sharedRoomPet.ts, tests/sharedRoomRuntime.test.ts, tests/sharedRoomPet.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomRuntime.ts, src/App.tsx, src/lib/sharedRoomPet.ts, .planning/debug/phase-05-shared-cat-diverges.md, tests/sharedRoomRuntime.test.ts, tests/sharedRoomPet.test.ts</read_first>
  <behavior>
    - Test 1: when browser A adopts the shared cat, browser B learns about the new canonical shared pet without clicking adopt or forcing a local conflict/reload path
    - Test 2: the passive browser still treats shared-pet existence as canonical room state rather than inventing a local-only cat
    - Test 3: the room runtime does not regress normal shared-room loading or conflict recovery
  </behavior>
  <action>Extend the room-runtime seam so passive browsers notice canonical shared-pet creation promptly. Prefer a lightweight canonical-room refresh/subscription path keyed off room revision or shared-pet presence rather than another ad hoc local trigger. Keep shared-pet ownership authoritative in the room document and add tests first so first-adoption visibility is locked before runtime plumbing changes ship.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts tests/sharedRoomPet.test.ts</verify>
  <acceptance_criteria>
    - passive browsers render the newly adopted shared cat without their own adopt action
    - shared-pet creation still comes from canonical room state, not only the ephemeral presence channel
    - tests/sharedRoomPet.test.ts covers first-adoption visibility across clients
  </acceptance_criteria>
  <done>A shared cat appears for both partners as soon as one of them adopts it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace snap-stepping with buffered playback for remote partners and the shared cat</name>
  <files>src/app/hooks/useSharedRoomPresence.ts, src/components/MinecraftPlayer.tsx, src/components/room-view/RoomPetActor.tsx, src/components/RoomView.tsx, tests/sharedRoomPresenceUx.test.ts, tests/sharedRoomPet.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomPresence.ts, src/components/MinecraftPlayer.tsx, src/components/room-view/RoomPetActor.tsx, src/components/RoomView.tsx, .planning/debug/phase-05-remote-avatar-snaps.md, tests/sharedRoomPresenceUx.test.ts, tests/sharedRoomPet.test.ts</read_first>
  <behavior>
    - Test 1: remote partner playback uses buffered interpolation/dead reckoning rather than visible snap-forward steps
    - Test 2: shared-cat playback uses the same smoother replica timeline and no longer reads as buggy teleporting
    - Test 3: playback tuning remains performant on localhost and does not regress presence correctness
  </behavior>
  <action>Upgrade the live actor render path from direct target smoothing to buffered snapshot playback with short-horizon dead reckoning. Apply the same playback strategy to both partner and cat replicas so their motion feels coherent and closer to a Minecraft-style multiplayer actor. Add tests first for the new playback helpers and live-state consumers before tuning the actual render loop.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresenceUx.test.ts tests/sharedRoomPet.test.ts</verify>
  <acceptance_criteria>
    - src/components/MinecraftPlayer.tsx no longer relies on a single latest-target lerp as the primary remote motion path
    - src/components/room-view/RoomPetActor.tsx replays shared cat motion from a smoother buffered replica path
    - tests/sharedRoomPresenceUx.test.ts and tests/sharedRoomPet.test.ts protect the new playback contract
  </acceptance_criteria>
  <done>Partner and cat replicas feel like one smoother multiplayer room rather than two clients chasing sparse snapshots.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts tests/sharedRoomPet.test.ts tests/sharedRoomPresenceUx.test.ts
- [ ] Passive browser sees a newly adopted shared cat without local interaction
- [ ] Partner and cat motion are visibly smoother than the current snap-stepping behavior
</verification>

<success_criteria>
The shared cat now appears for both browsers immediately after adoption, and both partner and pet replicas feel materially smoother than the current snap-like localhost presentation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-07-SUMMARY.md`
</output>
