---
phase: 05-online-backend-and-couple-ownership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - src/lib/sharedBackendConfig.ts
  - src/lib/firebaseApp.ts
  - src/lib/firebaseAuth.ts
  - src/lib/sharedRoomTypes.ts
  - src/lib/sharedRoomOwnership.ts
  - src/lib/sharedRoomOwnershipStore.ts
  - src/lib/sharedRoomSession.ts
  - tests/firebaseAuth.test.ts
  - tests/sharedRoomOwnership.test.ts
  - tests/sharedRoomSession.test.ts
  - tests/sharedRoomTypes.test.ts
autonomous: true
requirements:
  - PAIR-02
  - PAIR-03
user_setup: []
must_haves:
  truths:
    - "Firebase-authenticated identity, not browser-local UUID generation, is the canonical player identity for hosted pairing."
    - "Couple linking has a typed pending-link handshake with explicit two-party confirmation and one-room-per-account enforcement."
    - "Browser-local session data is preserved only as convenience cache and can no longer silently create a second identity."
  artifacts:
    - path: "src/lib/firebaseAuth.ts"
      provides: "Google sign-in/auth-session adapter for the app shell"
      min_lines: 60
      contains: "export async function signInWithGoogle"
    - path: "src/lib/sharedRoomOwnershipStore.ts"
      provides: "Hosted ownership/bootstrap store contract"
      min_lines: 40
      contains: "export interface SharedRoomOwnershipStore"
    - path: "src/lib/sharedRoomOwnership.ts"
      provides: "Pure pair-link and membership rule helpers"
      min_lines: 80
      contains: "export function isPendingPairLinkReady"
    - path: "tests/sharedRoomOwnership.test.ts"
      provides: "Pair-link rule coverage"
      contains: "rejects a third member or already-paired account"
  key_links:
    - from: "src/lib/firebaseAuth.ts"
      to: "src/lib/sharedRoomSession.ts"
      via: "authenticated user mapped into cached display-name and last-known-room helpers"
      pattern: "toSharedPlayerProfile|saveSharedRoomSession"
    - from: "src/lib/sharedRoomOwnershipStore.ts"
      to: "src/lib/sharedRoomOwnership.ts"
      via: "bootstrap and pending-link results validated by pure ownership helpers"
      pattern: "SharedRoomBootstrapState|SharedPendingPairLink"
    - from: "src/lib/sharedRoomTypes.ts"
      to: "src/lib/sharedRoomOwnershipStore.ts"
      via: "typed ownership and bootstrap payloads"
      pattern: "SharedRoomMembership|SharedPendingPairLink"
---

<objective>
Define the auth and ownership contract for Phase 05 before any hosted adapter work begins.

Purpose: Fulfill the identity and enforcement half of `PAIR-02` and `PAIR-03` by replacing local-only identity assumptions with a typed Google-auth + pair-link contract that later plans can implement.
Output: Environment-driven backend config, Firebase auth helpers, typed ownership/bootstrap payloads, pure pair-link rules, and downgraded local session cache semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md
@.planning/phases/05-online-backend-and-couple-ownership/05-RESEARCH.md
@src/lib/sharedRoomTypes.ts
@src/lib/sharedRoomSession.ts
@src/app/hooks/useSharedRoomRuntime.ts
@tests/sharedRoomSession.test.ts
@tests/sharedRoomTypes.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit backend config and Firebase Google-auth helpers first</name>
  <files>.env.example, src/lib/sharedBackendConfig.ts, src/lib/firebaseApp.ts, src/lib/firebaseAuth.ts, tests/firebaseAuth.test.ts</files>
  <read_first>.env.example, package.json, .planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md, .planning/phases/05-online-backend-and-couple-ownership/05-RESEARCH.md, src/lib/sharedRoomSession.ts</read_first>
  <behavior>
    - Test 1: backend config parsing recognizes the exact modes `firebase` and `dev` and falls back to `dev` when config is incomplete
    - Test 2: Firebase auth helper maps an authenticated user into `SharedPlayerProfile` using the exact `uid` as `playerId`
    - Test 3: Google sign-in helper uses the Google provider path and exposes explicit sign-in / sign-out / auth-state subscription functions
  </behavior>
  <action>Update `.env.example` to include the exact optional keys `VITE_SHARED_BACKEND=firebase` and `VITE_FIREBASE_USE_EMULATORS=false` alongside the existing Firebase config keys. Create `src/lib/sharedBackendConfig.ts` with the exact exports `getSharedBackendMode(): "dev" | "firebase"`, `getFirebaseConfig()`, and `shouldUseFirebaseEmulators()`. Create `src/lib/firebaseApp.ts` with the exact export `getFirebaseApp()` that lazily initializes the Firebase app from `getFirebaseConfig()`. Create `src/lib/firebaseAuth.ts` with the exact exports `subscribeToSharedAuth`, `signInWithGoogle`, `signOutSharedAuth`, and `toSharedPlayerProfile(user, fallbackDisplayName?)`. `toSharedPlayerProfile` must set `playerId` to Firebase `uid`, prefer `displayName`, then `email`, then fallback text. Add `tests/firebaseAuth.test.ts` first so the provider path, profile mapping, and backend-mode parsing are locked before store work starts.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/firebaseAuth.test.ts</verify>
  <acceptance_criteria>
    - .env.example contains "VITE_SHARED_BACKEND="
    - .env.example contains "VITE_FIREBASE_USE_EMULATORS="
    - src/lib/sharedBackendConfig.ts contains "export function getSharedBackendMode"
    - src/lib/firebaseApp.ts contains "export function getFirebaseApp"
    - src/lib/firebaseAuth.ts contains "export async function signInWithGoogle"
    - src/lib/firebaseAuth.ts contains "export function subscribeToSharedAuth"
    - tests/firebaseAuth.test.ts contains "maps Firebase uid to the shared player profile id"
  </acceptance_criteria>
  <done>The repo now has one explicit backend/auth configuration surface instead of Firebase placeholders plus local-only identity assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the hosted ownership, bootstrap, and pending-link contract as pure domain types and rules</name>
  <files>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomOwnership.ts, src/lib/sharedRoomOwnershipStore.ts, tests/sharedRoomOwnership.test.ts, tests/sharedRoomTypes.test.ts</files>
  <read_first>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomValidation.ts, .planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md, .planning/phases/05-online-backend-and-couple-ownership/05-RESEARCH.md, tests/sharedRoomTypes.test.ts</read_first>
  <behavior>
    - Test 1: pair-link helpers reject self-linking, already-paired accounts, and any attempt to produce more than two members
    - Test 2: a pending pair link becomes ready only when both participant ids are present and both confirmation flags are true before expiry
    - Test 3: bootstrap state can represent exactly `needs_linking`, `pending_link`, and `paired_room`
  </behavior>
  <action>Extend `src/lib/sharedRoomTypes.ts` with the exact exports `SharedRoomMembership`, `SharedPendingPairLink`, `SharedRoomBootstrapState`, and `SharedRoomBootstrapKind = "needs_linking" | "pending_link" | "paired_room"`. `SharedRoomMembership` must include `playerId`, `roomId`, `partnerPlayerId`, `pairCode`, and `pairedAt`. `SharedPendingPairLink` must include `pendingLinkId`, `playerIds`, `submittedByPlayerId`, `targetPairCode`, `confirmationsByPlayerId`, and `expiresAt`. Create `src/lib/sharedRoomOwnership.ts` with the exact exports `normalizePairCode(code: string): string`, `isPendingPairLinkReady(link, nowIso)`, `rejectInvalidPairParticipants(playerIds)`, and `deriveSharedRoomBootstrapState(input)`. Create `src/lib/sharedRoomOwnershipStore.ts` with the exact interface `SharedRoomOwnershipStore` exposing `loadBootstrapState`, `submitPairCode`, `confirmPairLink`, and `cancelPairLink`. Add `tests/sharedRoomOwnership.test.ts` and expand `tests/sharedRoomTypes.test.ts` so the exclusivity, two-party confirmation, and bootstrap-state rules are covered before Firebase persistence is added.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomOwnership.test.ts tests/sharedRoomTypes.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomTypes.ts contains "export type SharedRoomBootstrapKind = \"needs_linking\" | \"pending_link\" | \"paired_room\""
    - src/lib/sharedRoomTypes.ts contains "export interface SharedRoomMembership"
    - src/lib/sharedRoomTypes.ts contains "export interface SharedPendingPairLink"
    - src/lib/sharedRoomOwnership.ts contains "export function isPendingPairLinkReady"
    - src/lib/sharedRoomOwnershipStore.ts contains "export interface SharedRoomOwnershipStore"
    - tests/sharedRoomOwnership.test.ts contains "rejects a third member or already-paired account"
  </acceptance_criteria>
  <done>The hosted ownership problem is now described in types and pure rules instead of being implied by the old create/join dev flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Downgrade local session to convenience cache and prepare runtime-facing identity helpers</name>
  <files>src/lib/sharedRoomSession.ts, tests/sharedRoomSession.test.ts, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/lib/sharedRoomSession.ts, src/app/hooks/useSharedRoomRuntime.ts, src/lib/firebaseAuth.ts, .planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md, tests/sharedRoomSession.test.ts, tests/sharedRoomRuntime.test.ts</read_first>
  <behavior>
    - Test 1: shared-room session helpers preserve last-known room metadata without generating a new canonical identity when auth is available
    - Test 2: cached display-name fallback survives sign-out and later auth re-entry without inventing a new `playerId`
    - Test 3: clearing cached session removes room convenience state but does not alter auth-derived profile mapping
  </behavior>
  <action>Update `src/lib/sharedRoomSession.ts` so `loadOrCreateSharedPlayerProfile` accepts the exact optional second argument `{ canonicalPlayerId?: string | null }` and, when `canonicalPlayerId` is provided, stores that value as `playerId` instead of generating a UUID. Keep the exact exports `loadSharedRoomSession`, `saveSharedRoomSession`, and `clearSharedRoomSession`, but treat them as last-known room cache only: room id, invite code, partner id, and revision may be cached locally, yet they must no longer be described or tested as the source of truth for identity. Add `saveSharedPlayerProfile` coverage for auth-derived ids, expand `tests/sharedRoomSession.test.ts` for canonical auth identity hydration, and update `tests/sharedRoomRuntime.test.ts` helper setup so the runtime test fixtures can seed auth-derived player ids without depending on browser-generated UUIDs.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomSession.test.ts tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomSession.ts contains "canonicalPlayerId"
    - src/lib/sharedRoomSession.ts contains "loadOrCreateSharedPlayerProfile"
    - tests/sharedRoomSession.test.ts contains "hydrates the shared profile from a canonical auth id"
    - tests/sharedRoomRuntime.test.ts contains "saveSharedPlayerProfile"
  </acceptance_criteria>
  <done>Local storage is now only a convenience cache; the hosted auth contract can safely take over canonical identity in later plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/firebaseAuth.test.ts tests/sharedRoomOwnership.test.ts tests/sharedRoomSession.test.ts tests/sharedRoomTypes.test.ts
- [ ] The repo has one explicit backend mode and one explicit ownership/bootstrap contract for hosted pairing
- [ ] Local session helpers no longer imply that browser-generated ids are the canonical room owner identity
</verification>

<success_criteria>
Phase 05 now has a real auth and ownership contract to build against: Google-auth identity, pending-link handshake, and convenience-only local cache semantics are all explicit and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-01-SUMMARY.md`
</output>
