---
phase: 05-online-backend-and-couple-ownership
plan: 06
type: execute
gap_closure: true
wave: 3
depends_on:
  - 05
files_modified:
  - src/lib/furnitureInteractions.ts
  - src/app/hooks/useSharedRoomPresence.ts
  - src/components/room-view/useRoomViewInteractions.ts
  - tests/furnitureInteractions.test.ts
  - tests/sharedRoomPresenceUx.test.ts
autonomous: true
requirements:
  - ROOM-05
user_setup: []
must_haves:
  truths:
    - "If both partners lie on the same bed, each partner gets a distinct stable side instead of overlapping on one pose."
    - "The chosen bed side propagates through shared presence so both clients render the same slot."
    - "Single-player bed use still works when no partner slot is occupied."
  artifacts:
    - path: "src/lib/furnitureInteractions.ts"
      provides: "Occupancy-aware bed-side target selection"
      min_lines: 120
      contains: "getFurnitureInteractionTargets"
    - path: "src/components/room-view/useRoomViewInteractions.ts"
      provides: "Runtime selection of the correct bed slot before pose activation"
      min_lines: 250
      contains: "handleFurnitureInteractionCommand"
    - path: "tests/furnitureInteractions.test.ts"
      provides: "Two-partner bed-side regression coverage"
      contains: "bed"
  key_links:
    - from: "src/lib/furnitureInteractions.ts"
      to: "src/components/room-view/useRoomViewInteractions.ts"
      via: "runtime requests an occupancy-aware interaction target instead of always taking slot zero"
      pattern: "getFurnitureInteractionTarget"
    - from: "src/components/room-view/useRoomViewInteractions.ts"
      to: "src/app/hooks/useSharedRoomPresence.ts"
      via: "chosen lie slot flows into shared presence pose payloads"
      pattern: "playerInteractionPose|pose"
---

<objective>
Close the Phase 05 UAT gap where both partners overlap in the same lie pose when using one bed.

Purpose: preserve the existing bed interaction but make two-partner bed use feel intentional by assigning separate left/right sides and syncing that choice through shared presence.
Output: occupancy-aware bed target selection, stable shared lie slots, and regression coverage for single-player and two-player bed interactions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-online-backend-and-couple-ownership/05-UAT.md
@.planning/debug/phase-05-bed-side-overlap.md
@src/lib/furnitureRegistry.ts
@src/lib/furnitureInteractions.ts
@src/components/room-view/useRoomViewInteractions.ts
@src/app/hooks/useSharedRoomPresence.ts
@tests/furnitureInteractions.test.ts
@tests/sharedRoomPresenceUx.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make bed-target selection occupancy-aware instead of always taking slot zero</name>
  <files>src/lib/furnitureInteractions.ts, tests/furnitureInteractions.test.ts</files>
  <read_first>src/lib/furnitureRegistry.ts, src/lib/furnitureInteractions.ts, .planning/debug/phase-05-bed-side-overlap.md, tests/furnitureInteractions.test.ts</read_first>
  <behavior>
    - Test 1: the first bed user still gets the primary lie slot
    - Test 2: if one slot is already occupied, the second user gets the alternate slot
    - Test 3: when both slots are occupied, interaction selection fails predictably instead of reusing one slot silently
  </behavior>
  <action>Create an occupancy-aware bed-target selector inside `furnitureInteractions.ts` rather than always returning `getFurnitureInteractionTargets()[0]`. Reuse the existing primary and secondary bed offsets from the registry, but let the runtime request the correct slot based on current occupant pose information. Add tests first in `tests/furnitureInteractions.test.ts` so single-player, second-player, and full-bed behavior are explicit before runtime wiring changes.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/furnitureInteractions.test.ts</verify>
  <acceptance_criteria>
    - src/lib/furnitureInteractions.ts no longer resolves all bed interactions through unconditional slot-zero selection
    - tests/furnitureInteractions.test.ts contains coverage for first-slot, second-slot, and no-slot-left bed cases
  </acceptance_criteria>
  <done>Bed interaction targeting now exposes separate usable sides instead of one shared overlap point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the chosen bed slot through room interactions and shared presence</name>
  <files>src/components/room-view/useRoomViewInteractions.ts, src/app/hooks/useSharedRoomPresence.ts, tests/sharedRoomPresenceUx.test.ts</files>
  <read_first>src/components/room-view/useRoomViewInteractions.ts, src/app/hooks/useSharedRoomPresence.ts, .planning/debug/phase-05-bed-side-overlap.md, tests/sharedRoomPresenceUx.test.ts</read_first>
  <behavior>
    - Test 1: when a partner is already lying on one side, the local player targets the alternate side
    - Test 2: the remote partner receives the exact chosen lie pose and renders on the same side
    - Test 3: standing up frees that slot for later reuse
  </behavior>
  <action>Thread occupancy-aware bed targeting through `useRoomViewInteractions` so the active interaction chooses a bed side from current shared presence rather than defaulting to the first registry target. Ensure the chosen lie slot flows into shared presence pose payloads so both local and remote clients render the same side. Extend `tests/sharedRoomPresenceUx.test.ts` or add adjacent runtime coverage first so slot reuse and partner-side mirroring are protected before behavior ships.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresenceUx.test.ts tests/furnitureInteractions.test.ts</verify>
  <acceptance_criteria>
    - src/components/room-view/useRoomViewInteractions.ts uses occupancy-aware bed target selection
    - shared presence pose payloads preserve the selected bed side
    - tests/sharedRoomPresenceUx.test.ts contains bed-slot mirroring or slot-release coverage
  </acceptance_criteria>
  <done>Two-player bed use is now stable and visually side-by-side instead of overlapping.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/furnitureInteractions.test.ts tests/sharedRoomPresenceUx.test.ts
- [ ] Two partners can occupy distinct bed sides in one room
- [ ] Leaving the bed frees that side for reuse
</verification>

<success_criteria>
Bed interactions now respect two-person occupancy: one bed, two stable sides, no overlap.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-06-SUMMARY.md`
</output>
