---
phase: 05-online-backend-and-couple-ownership
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/app/hooks/useSharedRoomPresence.ts
  - src/app/components/SharedRoomEntryShell.tsx
  - src/app/components/SharedRoomBlockingOverlay.tsx
  - src/App.tsx
  - src/styles.css
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomPresenceUx.test.ts
  - tests/sharedRoomEntryShell.test.tsx
autonomous: true
requirements:
  - PAIR-02
  - ROOM-05
  - ROOM-04
user_setup: []
must_haves:
  truths:
    - "A signed-out player sees Google sign-in first, and a signed-in paired player skips linking and auto-loads the existing couple room."
    - "The first pair-link flow requires code submission plus explicit confirmation from both authenticated partners before the room becomes active."
    - "Once paired, either partner can leave and re-enter from another browser or device without losing canonical room state or violating the player/developer shell split."
  artifacts:
    - path: "src/app/hooks/useSharedRoomRuntime.ts"
      provides: "Auth-first hosted bootstrap and auto-room-load runtime"
      min_lines: 250
      contains: "loadBootstrapState"
    - path: "src/app/components/SharedRoomEntryShell.tsx"
      provides: "Google sign-in, code entry, confirm-link, and waiting states"
      min_lines: 140
      contains: "Continue with Google"
    - path: "tests/sharedRoomEntryShell.test.tsx"
      provides: "Entry-shell flow coverage"
      contains: "renders the confirm-link state after code submission"
    - path: "tests/sharedRoomRuntime.test.ts"
      provides: "Auto-reentry and hosted bootstrap coverage"
      contains: "loads the paired room automatically for an authenticated member"
  key_links:
    - from: "src/app/hooks/useSharedRoomRuntime.ts"
      to: "src/lib/firebaseAuth.ts"
      via: "auth state drives hosted bootstrap and room loading"
      pattern: "subscribeToSharedAuth|signInWithGoogle"
    - from: "src/app/hooks/useSharedRoomRuntime.ts"
      to: "src/lib/sharedRoomOwnershipStore.ts"
      via: "bootstrap, submit, confirm, and cancel link operations"
      pattern: "loadBootstrapState|submitPairCode|confirmPairLink"
    - from: "src/App.tsx"
      to: "src/app/components/SharedRoomEntryShell.tsx"
      via: "player-facing entry shell mounted only in Player View"
      pattern: "SharedRoomEntryShell"
---

<objective>
Swap the shipped player flow from local dev session entry to hosted Google sign-in, pair linking, and auto room re-entry.

Purpose: Fulfill `PAIR-02`, `ROOM-05`, and the user-facing half of `ROOM-04` by making the real hosted backend the default room entry path without polluting the shipped player shell with developer-only controls.
Output: Auth-first runtime bootstrap, Google sign-in and link-confirm entry shell, hosted room auto-reentry, recovery/error states, and browser-level regression coverage for the new flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md
@.planning/phases/05-online-backend-and-couple-ownership/05-RESEARCH.md
@.planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md
@src/App.tsx
@src/app/hooks/useSharedRoomRuntime.ts
@src/app/hooks/useSharedRoomPresence.ts
@src/app/components/SharedRoomEntryShell.tsx
@src/app/components/SharedRoomBlockingOverlay.tsx
@src/lib/sharedRoomSession.ts
@tests/sharedRoomRuntime.test.ts
@tests/sharedRoomPresenceUx.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the shared-room runtime auth-first and bootstrap paired-room state automatically</name>
  <files>src/app/hooks/useSharedRoomRuntime.ts, src/lib/sharedRoomSession.ts, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomRuntime.ts, src/lib/sharedRoomSession.ts, src/lib/firebaseAuth.ts, src/lib/sharedRoomOwnershipStore.ts, src/lib/sharedRoomStore.ts, .planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md, tests/sharedRoomRuntime.test.ts</read_first>
  <behavior>
    - Test 1: signed-in authenticated member with an existing membership record auto-loads the paired room without going through code entry
    - Test 2: signed-in unpaired member lands in `needs_linking` bootstrap state with their own reusable pair code available
    - Test 3: signing out clears cached room convenience state and returns the runtime to the signed-out bootstrap path
  </behavior>
  <action>Extend `src/app/hooks/useSharedRoomRuntime.ts` so it subscribes to `subscribeToSharedAuth`, maps the authenticated Firebase user through `toSharedPlayerProfile`, and then calls `loadBootstrapState` from the hosted ownership store before deciding whether to load a canonical room. Add the exact runtime bootstrap states `signed_out`, `needs_linking`, `pending_link`, `loading_room`, and `room_ready`. Keep `commitRoomState`, `commitRoomMutation`, and canonical conflict replay behavior unchanged once a room is active. Preserve `SharedRoomSession` local storage only as last-known-room cache and clear it on sign-out. Expand `tests/sharedRoomRuntime.test.ts` so hosted bootstrap and auto-reentry are covered before entry-shell UI changes land.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useSharedRoomRuntime.ts contains "signed_out"
    - src/app/hooks/useSharedRoomRuntime.ts contains "needs_linking"
    - src/app/hooks/useSharedRoomRuntime.ts contains "pending_link"
    - src/app/hooks/useSharedRoomRuntime.ts contains "loading_room"
    - src/app/hooks/useSharedRoomRuntime.ts contains "subscribeToSharedAuth"
    - tests/sharedRoomRuntime.test.ts contains "loads the paired room automatically for an authenticated member"
  </acceptance_criteria>
  <done>The runtime can now enter the app from real auth state instead of only from local create/join session assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the entry shell with Google sign-in, code entry, mutual confirm, and waiting states</name>
  <files>src/app/components/SharedRoomEntryShell.tsx, src/app/components/SharedRoomBlockingOverlay.tsx, src/styles.css, tests/sharedRoomEntryShell.test.tsx</files>
  <read_first>src/app/components/SharedRoomEntryShell.tsx, src/app/components/SharedRoomBlockingOverlay.tsx, .planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md, src/styles.css</read_first>
  <behavior>
    - Test 1: signed-out entry shell shows Google sign-in CTA before any pair-code controls
    - Test 2: after code submission, the shell can render a confirm-link state with both participant names and a confirm CTA
    - Test 3: pending-link waiting state shows that the player is waiting for their partner rather than falling back to create/join copy
  </behavior>
  <action>Rewrite `src/app/components/SharedRoomEntryShell.tsx` to render exact player-facing states for `signed_out`, `needs_linking`, and `pending_link`. Use the exact visible CTA labels `Continue with Google`, `Submit partner code`, and `Confirm couple room`. Use the exact waiting copy `Waiting for your partner to confirm`. Keep the existing stakes warning visible in the hosted flow and preserve the room-first aesthetic established by the player shell. Update `src/app/components/SharedRoomBlockingOverlay.tsx` so hosted bootstrap failures can render exact titles `Loading your room...` and `We couldn't verify your room right now.` Add `tests/sharedRoomEntryShell.test.tsx` first so the shell states are covered before `App.tsx` wiring.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomEntryShell.test.tsx</verify>
  <acceptance_criteria>
    - src/app/components/SharedRoomEntryShell.tsx contains "Continue with Google"
    - src/app/components/SharedRoomEntryShell.tsx contains "Submit partner code"
    - src/app/components/SharedRoomEntryShell.tsx contains "Confirm couple room"
    - src/app/components/SharedRoomEntryShell.tsx contains "Waiting for your partner to confirm"
    - src/app/components/SharedRoomBlockingOverlay.tsx contains "Loading your room..."
    - tests/sharedRoomEntryShell.test.tsx contains "renders the confirm-link state after code submission"
  </acceptance_criteria>
  <done>The shipped player shell now presents the actual hosted sign-in and linking flow instead of the old dev-era create/join UI.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the hosted flow through `App.tsx` and preserve player-shell and presence behavior across re-entry</name>
  <files>src/App.tsx, src/app/hooks/useSharedRoomPresence.ts, src/styles.css, tests/sharedRoomPresenceUx.test.ts, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/App.tsx, src/app/hooks/useSharedRoomPresence.ts, src/app/components/SharedRoomEntryShell.tsx, src/app/components/PlayerRoomShell.tsx, src/app/components/DeveloperWorkspaceShell.tsx, .planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md, .planning/phases/03.1-ui-overhaul-and-developer-player-view-split/03.1-UI-SPEC.md</read_first>
  <action>Update `src/App.tsx` so the player branch mounts `SharedRoomEntryShell` from hosted runtime bootstrap state instead of the old `createRoom` / `joinRoom` path. Wire the exact callbacks `onSignInWithGoogle`, `onSubmitPartnerCode`, `onConfirmPairLink`, `onCancelPairLink`, and `onSignOut`. Keep Developer View free of new auth/debug clutter; only Player View should surface the hosted entry flow. Update `src/app/hooks/useSharedRoomPresence.ts` so in-room presence still starts only after `room_ready`, while pending-link presence can run during `pending_link` without starting room presence early. Expand `tests/sharedRoomPresenceUx.test.ts` and `tests/sharedRoomRuntime.test.ts` so browser/device re-entry, partner-offline room entry, and stale-room verification failures are covered. Preserve the existing player/developer shell split and do not reintroduce temporary dev chrome into Player View.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts tests/sharedRoomPresenceUx.test.ts && cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/App.tsx contains "onSignInWithGoogle"
    - src/App.tsx contains "onSubmitPartnerCode"
    - src/App.tsx contains "onConfirmPairLink"
    - src/App.tsx contains "onSignOut"
    - src/app/hooks/useSharedRoomPresence.ts contains "pending_link"
    - tests/sharedRoomRuntime.test.ts contains "allows one partner to enter the paired room while the partner is offline"
    - tests/sharedRoomPresenceUx.test.ts contains "does not start room presence before the room is ready"
  </acceptance_criteria>
  <done>The hosted backend is now the default shipped player flow: auth, linking, auto-reentry, and room presence all behave correctly without breaking the player shell or developer workspace boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomEntryShell.test.tsx tests/sharedRoomRuntime.test.ts tests/sharedRoomPresenceUx.test.ts
- [ ] cmd /c npm run build
- [ ] Signed-out, unpaired, pending-link, paired-room, and partner-offline re-entry states all flow through Player View without reintroducing developer clutter
</verification>

<success_criteria>
Phase 05 finishes as a real hosted player flow: Google sign-in, trusted couple linking, automatic future room entry, and reconnect-safe canonical room loading all work through the existing room runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-03-SUMMARY.md`
</output>
