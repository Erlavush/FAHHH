---
phase: 05-online-backend-and-couple-ownership
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - src/lib/sharedBackendConfig.ts
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/app/hooks/useSharedRoomPresence.ts
  - src/app/shellViewModel.ts
  - src/app/components/SharedRoomEntryShell.tsx
  - scripts/sharedRoomDevPlugin.mjs
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomPresence.test.ts
  - tests/sharedRoomPresenceUx.test.ts
  - tests/sharedRoomEntryShell.test.tsx
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - PAIR-02
  - ROOM-05
user_setup: []
must_haves:
  truths:
    - "Local dev fallback must no longer masquerade as the hosted sign-in path; the player shell clearly explains whether it is in hosted mode or local dev room mode."
    - "If Firebase is requested but not configured, the runtime blocks or warns explicitly instead of silently auto-entering the room."
    - "When a partner disappears abruptly, reconnecting is a short-lived transition and then degrades to a partner-away state rather than staying stuck forever."
  artifacts:
    - path: "src/app/hooks/useSharedRoomRuntime.ts"
      provides: "Explicit runtime/backend mode and hosted-unavailable fallback handling"
      min_lines: 250
      contains: "devBypassActive"
    - path: "src/app/hooks/useSharedRoomPresence.ts"
      provides: "Bounded reconnect versus offline partner-away state machine"
      min_lines: 200
      contains: "derivePresenceFreshness"
    - path: "scripts/sharedRoomDevPlugin.mjs"
      provides: "Dev presence stale-record cleanup or pruning"
      min_lines: 500
      contains: "loadSharedRoomPresenceSnapshotInDatabase"
    - path: "tests/sharedRoomPresenceUx.test.ts"
      provides: "Reconnect timeout and partner-away UX coverage"
      contains: "keeps the room usable while the partner reconnects"
  key_links:
    - from: "src/lib/sharedBackendConfig.ts"
      to: "src/app/hooks/useSharedRoomRuntime.ts"
      via: "runtime chooses hosted entry or local dev fallback from backend mode"
      pattern: "getSharedBackendMode|devBypassActive"
    - from: "scripts/sharedRoomDevPlugin.mjs"
      to: "src/app/hooks/useSharedRoomPresence.ts"
      via: "stale presence pruning affects reconnect/offline freshness"
      pattern: "presenceByRoom|derivePresenceFreshness"
    - from: "src/app/hooks/useSharedRoomPresence.ts"
      to: "src/app/shellViewModel.ts"
      via: "partner freshness maps to player-facing companion copy"
      pattern: "Partner reconnecting|Waiting for partner"
---

<objective>
Close the Phase 05 UAT gaps around hosted-entry clarity and the partner status card staying stuck on `Reconnecting`.

Purpose: Keep the brownfield dev sandbox efficient while making it impossible to confuse local dev fallback with the hosted auth path, and make partner-away status degrade cleanly after short reconnect windows.
Output: explicit backend/fallback shell state, bounded reconnect semantics, stale dev presence cleanup, and regression coverage for hosted-unavailable plus partner-away UX.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-online-backend-and-couple-ownership/05-UAT.md
@.planning/debug/phase-05-hosted-entry-falls-back-to-dev.md
@.planning/debug/phase-05-presence-stuck-reconnecting.md
@src/lib/sharedBackendConfig.ts
@src/app/hooks/useSharedRoomRuntime.ts
@src/app/hooks/useSharedRoomPresence.ts
@src/app/shellViewModel.ts
@scripts/sharedRoomDevPlugin.mjs
@tests/sharedRoomRuntime.test.ts
@tests/sharedRoomPresenceUx.test.ts
@tests/sharedRoomPresence.test.ts
@tests/shellViewModel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make backend mode and hosted-unavailable fallback explicit in the player shell</name>
  <files>src/lib/sharedBackendConfig.ts, src/app/hooks/useSharedRoomRuntime.ts, src/app/components/SharedRoomEntryShell.tsx, tests/sharedRoomRuntime.test.ts, tests/sharedRoomEntryShell.test.tsx</files>
  <read_first>src/lib/sharedBackendConfig.ts, src/app/hooks/useSharedRoomRuntime.ts, src/app/components/SharedRoomEntryShell.tsx, .planning/phases/05-online-backend-and-couple-ownership/05-UAT.md, .planning/debug/phase-05-hosted-entry-falls-back-to-dev.md, tests/sharedRoomRuntime.test.ts, tests/sharedRoomEntryShell.test.tsx</read_first>
  <behavior>
    - Test 1: when Firebase is requested but config is incomplete, the runtime exposes an explicit hosted-unavailable state instead of silently bootstrapping into the room
    - Test 2: when the app is intentionally using the local dev fallback path, the player shell labels that state as local/dev rather than looking like hosted sign-in was skipped accidentally
    - Test 3: the existing hosted sign-in path still appears first once backend mode is truly `firebase`
  </behavior>
  <action>Extend backend-mode and runtime bootstrap handling so `useSharedRoomRuntime` can distinguish `hosted ready`, `hosted requested but unavailable`, and `local dev fallback` rather than collapsing everything into silent dev auto-entry. Preserve dev workflow speed, but add an explicit player-shell explanation or badge so local fallback is visibly different from hosted mode. If `VITE_SHARED_BACKEND=firebase` is set but Firebase env is incomplete, block or warn clearly instead of entering the room as if hosted auth had passed. Expand `tests/sharedRoomRuntime.test.ts` and `tests/sharedRoomEntryShell.test.tsx` first so the fallback-versus-hosted contract is locked before UI copy changes ship.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts tests/sharedRoomEntryShell.test.tsx</verify>
  <acceptance_criteria>
    - src/app/hooks/useSharedRoomRuntime.ts contains a distinct hosted-unavailable or fallback-visible bootstrap path
    - src/app/components/SharedRoomEntryShell.tsx contains dev/fallback-visible copy instead of only hosted and legacy create/join copy
    - tests/sharedRoomRuntime.test.ts contains coverage for incomplete Firebase config not silently auto-entering hosted UAT
  </acceptance_criteria>
  <done>The user can tell whether they are testing the real hosted path or the local dev fallback path before they interpret any room-entry behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Degrade stale partner presence from reconnecting to partner-away and prune stale dev presence</name>
  <files>scripts/sharedRoomDevPlugin.mjs, src/app/hooks/useSharedRoomPresence.ts, src/app/shellViewModel.ts, tests/sharedRoomPresence.test.ts, tests/sharedRoomPresenceUx.test.ts, tests/shellViewModel.test.ts</files>
  <read_first>scripts/sharedRoomDevPlugin.mjs, src/app/hooks/useSharedRoomPresence.ts, src/app/shellViewModel.ts, .planning/debug/phase-05-presence-stuck-reconnecting.md, tests/sharedRoomPresence.test.ts, tests/sharedRoomPresenceUx.test.ts, tests/shellViewModel.test.ts</read_first>
  <behavior>
    - Test 1: abruptly closing a browser no longer leaves a permanent reconnecting ghost in the dev presence snapshot
    - Test 2: short-lived stale partner presence still shows a reconnecting state, but long-stale partner presence degrades to a waiting/offline state
    - Test 3: the companion card copy reflects a softer partner-away label instead of permanent `Reconnecting`
  </behavior>
  <action>Update the dev presence store and `useSharedRoomPresence` freshness model so reconnecting remains a bounded transient state. Prune stale presence records on read or during periodic access in the dev plugin, then adjust the presence-state machine and shell view model so long-stale partner presence becomes a partner-away/waiting state rather than a permanent reconnect card. Keep the room usable during short reconnects. Add or extend tests first in `tests/sharedRoomPresence.test.ts`, `tests/sharedRoomPresenceUx.test.ts`, and `tests/shellViewModel.test.ts` so both the store cleanup and the player-facing copy are protected.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresence.test.ts tests/sharedRoomPresenceUx.test.ts tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - scripts/sharedRoomDevPlugin.mjs contains stale-presence pruning or TTL cleanup for `presenceByRoom`
    - src/app/hooks/useSharedRoomPresence.ts distinguishes reconnecting from long-term partner-away status
    - src/app/shellViewModel.ts no longer hardcodes permanent `Reconnecting` as the only stale-partner companion state
    - tests/sharedRoomPresenceUx.test.ts contains partner-away coverage after reconnect timeout
  </acceptance_criteria>
  <done>Partner presence transitions feel intentional: reconnecting is temporary, and offline partners settle into a calmer away state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts tests/sharedRoomEntryShell.test.tsx tests/sharedRoomPresence.test.ts tests/sharedRoomPresenceUx.test.ts tests/shellViewModel.test.ts
- [ ] Hosted-unavailable and local-dev fallback states are visually distinct from successful hosted sign-in
- [ ] Closing a dev browser eventually yields partner-away state rather than permanent reconnecting
</verification>

<success_criteria>
Phase 05 gap closure now tells the truth about backend mode and partner presence freshness: the dev fallback path is explicit, hosted sign-in is not silently skipped, and partner-away status no longer gets stuck as reconnecting forever.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-04-SUMMARY.md`
</output>
