---
phase: 05-online-backend-and-couple-ownership
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/lib/firebaseRoomStore.ts
  - src/lib/firebaseOwnershipStore.ts
  - src/lib/firebasePresenceStore.ts
  - src/lib/sharedRoomClient.ts
  - src/lib/sharedPresenceClient.ts
  - src/lib/sharedRoomStore.ts
  - src/lib/sharedPresenceStore.ts
  - src/lib/sharedRoomValidation.ts
  - tests/sharedRoomStore.test.ts
  - tests/sharedRoomPresence.test.ts
  - tests/sharedRoomEditLocks.test.ts
  - tests/firebaseOwnershipStore.test.ts
autonomous: true
requirements:
  - ROOM-04
  - PAIR-03
user_setup: []
must_haves:
  truths:
    - "Canonical room, progression, memories, and shared pet state load and commit through a hosted Firestore-backed adapter instead of the dev file store."
    - "Room presence, edit locks, and linking-screen presence use a separate hosted Realtime Database adapter instead of canonical room commits."
    - "Pair finalization enforces one room per account and two members per room through one trusted atomic path."
  artifacts:
    - path: "src/lib/firebaseRoomStore.ts"
      provides: "Firestore-backed canonical room adapter"
      min_lines: 120
      contains: "export function createFirebaseRoomStore"
    - path: "src/lib/firebaseOwnershipStore.ts"
      provides: "Hosted ownership/bootstrap adapter"
      min_lines: 140
      contains: "export function createFirebaseOwnershipStore"
    - path: "src/lib/firebasePresenceStore.ts"
      provides: "Realtime Database presence and lock adapter"
      min_lines: 140
      contains: "export function createFirebasePresenceStore"
    - path: "tests/firebaseOwnershipStore.test.ts"
      provides: "Pair finalization and membership enforcement regression coverage"
      contains: "creates the starter room and shared cat only after both confirmations"
  key_links:
    - from: "src/lib/firebaseOwnershipStore.ts"
      to: "src/lib/firebaseRoomStore.ts"
      via: "trusted pair finalization creates the canonical room document and membership records"
      pattern: "sharedRooms|roomMemberships|pendingPairLinks"
    - from: "src/lib/firebasePresenceStore.ts"
      to: "src/app/hooks/useSharedRoomPresence.ts"
      via: "room presence, edit locks, and link presence payloads"
      pattern: "roomPresence|roomLocks|pairLinkPresence"
    - from: "src/lib/sharedRoomClient.ts"
      to: "src/lib/firebaseRoomStore.ts"
      via: "backend mode selects the hosted room adapter"
      pattern: "createFirebaseRoomStore"
---

<objective>
Implement the hosted Firebase adapters behind the existing runtime seams.

Purpose: Fulfill `ROOM-04` and the enforcement half of `PAIR-03` by replacing the dev file-backed canonical room and presence adapters with hosted Firestore and Realtime Database implementations.
Output: Firestore-backed room and ownership stores, Realtime Database-backed presence and edit-lock store, backend selection factory wiring, and targeted adapter tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-online-backend-and-couple-ownership/05-CONTEXT.md
@.planning/phases/05-online-backend-and-couple-ownership/05-RESEARCH.md
@src/lib/sharedRoomStore.ts
@src/lib/sharedPresenceStore.ts
@src/lib/sharedRoomClient.ts
@src/lib/sharedPresenceClient.ts
@src/lib/sharedRoomValidation.ts
@src/lib/roomState.ts
@src/lib/sharedRoomSeed.ts
@src/lib/sharedProgression.ts
@src/lib/sharedRoomPet.ts
@src/lib/petPathing.ts
@src/app/constants.ts
@scripts/sharedRoomDevPlugin.mjs
@tests/sharedRoomStore.test.ts
@tests/sharedRoomPresence.test.ts
@tests/sharedRoomEditLocks.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Firestore ownership and canonical room adapters with atomic pair finalization</name>
  <files>src/lib/firebaseRoomStore.ts, src/lib/firebaseOwnershipStore.ts, src/lib/sharedRoomStore.ts, tests/sharedRoomStore.test.ts, tests/firebaseOwnershipStore.test.ts</files>
  <read_first>src/lib/sharedRoomStore.ts, src/lib/sharedRoomOwnershipStore.ts, src/lib/sharedRoomTypes.ts, src/lib/sharedRoomValidation.ts, src/lib/roomState.ts, src/lib/sharedRoomSeed.ts, src/lib/sharedProgression.ts, src/lib/sharedRoomPet.ts, src/lib/petPathing.ts, src/app/constants.ts, .planning/phases/05-online-backend-and-couple-ownership/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `loadBootstrapState` returns `needs_linking` with a reusable self pair code when the authenticated user has no membership record
    - Test 2: `confirmPairLink` refuses already-paired accounts and does not create a room until both confirmations are true
    - Test 3: successful pair finalization creates the canonical starter room, starter progression, shared cat, and membership docs in one atomic flow
  </behavior>
  <action>Create `src/lib/firebaseOwnershipStore.ts` with the exact export `createFirebaseOwnershipStore()` implementing `SharedRoomOwnershipStore` against the Firestore collections `roomMemberships`, `pendingPairLinks`, and `pairCodes`. `loadBootstrapState` must guarantee one reusable `pairCode` per authenticated player. `submitPairCode` must reject self-linking, normalize codes to uppercase, and create or update a pending-link document keyed by the two participant ids. `confirmPairLink` must finalize only when both `confirmationsByPlayerId` values are true and both players still lack membership docs. During finalization, create a new `sharedRooms/{roomId}` canonical document using `createSharedRoomSeed(roomId, createDefaultRoomState())`, `createInitialSharedRoomProgression`, and `createSharedRoomPetRecord(pickPetSpawnPosition(DEFAULT_PLAYER_POSITION, roomState.furniture), actorPlayerId, nowIso)`, then create two `roomMemberships/{playerId}` docs and close the pending-link record in the same atomic path. Create `src/lib/firebaseRoomStore.ts` with the exact export `createFirebaseRoomStore()` implementing `SharedRoomStore` load and commit against `sharedRooms/{roomId}` while preserving `revision`, `progression`, `frameMemories`, and `sharedPet`. Expand `src/lib/sharedRoomStore.ts` types only as needed to support the hosted adapter inputs, and add adapter tests first in `tests/firebaseOwnershipStore.test.ts` and `tests/sharedRoomStore.test.ts`.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/firebaseOwnershipStore.test.ts tests/sharedRoomStore.test.ts</verify>
  <acceptance_criteria>
    - src/lib/firebaseOwnershipStore.ts contains "roomMemberships"
    - src/lib/firebaseOwnershipStore.ts contains "pendingPairLinks"
    - src/lib/firebaseOwnershipStore.ts contains "pairCodes"
    - src/lib/firebaseOwnershipStore.ts contains "createSharedRoomPetRecord"
    - src/lib/firebaseRoomStore.ts contains "export function createFirebaseRoomStore"
    - tests/firebaseOwnershipStore.test.ts contains "creates the starter room and shared cat only after both confirmations"
    - tests/sharedRoomStore.test.ts contains "loads canonical room state from the hosted adapter"
  </acceptance_criteria>
  <done>The hosted backend can now create, look up, load, and commit canonical couple rooms without relying on the dev file store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Realtime Database presence, linking presence, and edit-lock adapter</name>
  <files>src/lib/firebasePresenceStore.ts, src/lib/sharedPresenceStore.ts, tests/sharedRoomPresence.test.ts, tests/sharedRoomEditLocks.test.ts</files>
  <read_first>src/lib/sharedPresenceStore.ts, src/lib/sharedPresenceTypes.ts, src/lib/sharedPresenceValidation.ts, src/app/hooks/useSharedRoomPresence.ts, .planning/phases/05-online-backend-and-couple-ownership/05-RESEARCH.md, tests/sharedRoomPresence.test.ts, tests/sharedRoomEditLocks.test.ts</read_first>
  <behavior>
    - Test 1: room presence writes and reads use a room-scoped RTDB path without mutating canonical room revision state
    - Test 2: edit-lock acquisition still rejects a partner conflict and honors existing TTL semantics
    - Test 3: linking presence can mark a participant active on a pending link and clear it on leave/disconnect
  </behavior>
  <action>Create `src/lib/firebasePresenceStore.ts` with the exact export `createFirebasePresenceStore()` implementing `SharedPresenceStore` against the Realtime Database roots `roomPresence`, `roomLocks`, and `pairLinkPresence`. Preserve the current room presence payload fields (`playerId`, `displayName`, `skinSrc`, `position`, `facingY`, `activity`, `pose`, `updatedAt`) and existing lock payload fields (`roomId`, `furnitureId`, `playerId`, `displayName`, `expiresAt`, `updatedAt`). Keep the exact five-second lock TTL behavior already implied by the dev adapter. Add explicit RTDB `onDisconnect` cleanup for presence nodes and any link-presence node written by the current client. Extend `src/lib/sharedPresenceStore.ts` only as needed to support the exact additional methods `upsertPairLinkPresence`, `loadPairLinkPresence`, and `leavePairLinkPresence`. Expand `tests/sharedRoomPresence.test.ts` and `tests/sharedRoomEditLocks.test.ts` first so presence, locks, and link-presence behavior remain separate from canonical room commits.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomPresence.test.ts tests/sharedRoomEditLocks.test.ts</verify>
  <acceptance_criteria>
    - src/lib/firebasePresenceStore.ts contains "roomPresence"
    - src/lib/firebasePresenceStore.ts contains "roomLocks"
    - src/lib/firebasePresenceStore.ts contains "pairLinkPresence"
    - src/lib/firebasePresenceStore.ts contains "onDisconnect"
    - src/lib/sharedPresenceStore.ts contains "upsertPairLinkPresence"
    - tests/sharedRoomPresence.test.ts contains "keeps room presence separate from canonical room state"
    - tests/sharedRoomEditLocks.test.ts contains "rejects a conflicting partner lock"
  </acceptance_criteria>
  <done>Hosted ephemeral state now has the correct data lifetime: presence, link presence, and edit locks live outside canonical room documents.</done>
</task>

<task type="auto">
  <name>Task 3: Wire backend selection so the runtime can switch from dev adapters to hosted adapters explicitly</name>
  <files>src/lib/sharedRoomClient.ts, src/lib/sharedPresenceClient.ts, src/lib/sharedRoomStore.ts, src/lib/sharedPresenceStore.ts, src/lib/sharedBackendConfig.ts</files>
  <read_first>src/lib/sharedRoomClient.ts, src/lib/sharedPresenceClient.ts, src/lib/sharedBackendConfig.ts, src/lib/firebaseRoomStore.ts, src/lib/firebaseOwnershipStore.ts, src/lib/firebasePresenceStore.ts</read_first>
  <action>Update `src/lib/sharedRoomClient.ts` so the default export path selects the exact adapter based on `getSharedBackendMode()`: when mode is `"firebase"`, export the hosted adapter created by `createFirebaseRoomStore()`; when mode is `"dev"`, keep using the current dev API-backed adapter. Update `src/lib/sharedPresenceClient.ts` with the same mode switch between `createFirebasePresenceStore()` and the current dev presence adapter. Do not delete the dev adapters in this phase; keep them as explicit fallback only. Ensure the mode switch is centralized in the client files and does not leak into `App.tsx` or `RoomView`. Keep the `SharedRoomStore` and `SharedPresenceStore` interfaces usable by the existing runtime hooks without any Firebase imports in shell code.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomClient.ts contains "getSharedBackendMode"
    - src/lib/sharedRoomClient.ts contains "createFirebaseRoomStore"
    - src/lib/sharedPresenceClient.ts contains "getSharedBackendMode"
    - src/lib/sharedPresenceClient.ts contains "createFirebasePresenceStore"
    - src/lib/sharedRoomClient.ts contains "\"firebase\""
    - src/lib/sharedPresenceClient.ts contains "\"dev\""
  </acceptance_criteria>
  <done>The live runtime can now depend on the same store interfaces while the backing implementation switches from dev-file to hosted Firebase explicitly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/firebaseOwnershipStore.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomPresence.test.ts tests/sharedRoomEditLocks.test.ts
- [ ] cmd /c npm run build
- [ ] Hosted adapters exist for canonical room data and ephemeral presence, and the default client exports can select them without changing `App.tsx`
</verification>

<success_criteria>
Phase 05 now has real hosted adapters: Firestore owns the canonical room, RTDB owns ephemeral presence, and pair finalization can create a trusted starter room with the shared cat atomically.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-backend-and-couple-ownership/05-02-SUMMARY.md`
</output>
