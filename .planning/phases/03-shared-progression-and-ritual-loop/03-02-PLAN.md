---
phase: 03-shared-progression-and-ritual-loop
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/lib/sharedProgression.ts
  - src/App.tsx
  - src/app/components/InventoryPanel.tsx
  - src/components/PcMinigameOverlay.tsx
  - tests/sharedProgression.test.ts
  - tests/pcMinigame.test.ts
  - tests/sharedRoomRuntime.test.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - RITL-01
user_setup: []
must_haves:
  truths:
    - "The active partner's personal wallet pays for shared-room purchases and receives shared-room sell refunds instead of a global shared coin scalar."
    - "The desk PC applies personal coins and XP immediately, records one ritual contribution per partner per room day, and grants the daily ritual bonus only when the second distinct partner finishes."
    - "The desk-PC result surface explains whether the run only saved a check-in or fully completed the daily ritual and advanced the streak."
  artifacts:
    - path: "src/lib/sharedProgression.ts"
      provides: "Pure personal wallet, desk-PC reward, ritual bonus, and streak mutation helpers"
      min_lines: 220
      contains: "export function applyDeskPcCompletionToProgression"
    - path: "src/App.tsx"
      provides: "Shared-room buy, sell, and desk-PC handlers routed through progression-aware mutations"
      min_lines: 900
      contains: "applyDeskPcCompletionToProgression"
    - path: "src/components/PcMinigameOverlay.tsx"
      provides: "Desk-PC result card that shows ritual contribution and daily bonus outcomes"
      min_lines: 300
      contains: "Daily ritual"
    - path: "tests/sharedProgression.test.ts"
      provides: "Regression coverage for desk-PC ritual completion and personal wallet mutations"
      contains: "awards the ritual bonus to both partners when the second partner completes the day"
    - path: "tests/sharedRoomRuntime.test.ts"
      provides: "Runtime coverage for shared-room desk-PC progression commits"
      contains: "commits desk pc progression and ritual state through the shared room runtime"
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/sharedProgression.ts"
      via: "buy, sell, and desk-PC mutation helpers"
      pattern: "applyPersonalWalletSpend|applyPersonalWalletRefund|applyDeskPcCompletionToProgression"
    - from: "src/App.tsx"
      to: "src/components/PcMinigameOverlay.tsx"
      via: "desk-PC result props for ritual and bonus feedback"
      pattern: "dailyRitualStatus|dailyRitualBonus"
    - from: "src/app/components/InventoryPanel.tsx"
      to: "src/App.tsx"
      via: "active-player wallet affordability and shared-room purchase copy"
      pattern: "playerCoins|Purchased furniture becomes shared room inventory."
---

<objective>
Connect the desk-PC earn loop, shared-room buy and sell flows, and the first daily ritual to the canonical progression model.

Purpose: Make the Phase 03 progression schema drive actual room economy and return-play behavior instead of remaining a passive UI layer.
Output: Pure progression mutation helpers for wallet spend/refund and desk-PC ritual completion, shared-room handlers that use those helpers, visible desk-PC ritual feedback, and regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-RESEARCH.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-01-PLAN.md
@src/lib/sharedProgression.ts
@src/lib/pcMinigame.ts
@src/App.tsx
@src/app/components/InventoryPanel.tsx
@src/components/PcMinigameOverlay.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure progression mutations for wallet spend, desk-PC rewards, and daily ritual closure</name>
  <files>src/lib/sharedProgression.ts, tests/sharedProgression.test.ts, tests/pcMinigame.test.ts</files>
  <read_first>.planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md, .planning/phases/03-shared-progression-and-ritual-loop/03-RESEARCH.md, src/lib/sharedProgression.ts, src/lib/pcMinigame.ts, tests/sharedProgression.test.ts</read_first>
  <behavior>
    - Test 1: `applyPersonalWalletSpend` subtracts the exact furniture cost from the acting player's wallet and rejects overspend without mutating any partner wallet
    - Test 2: `applyPersonalWalletRefund` adds the exact sell refund to the acting player's wallet and does not change couple ritual state
    - Test 3: `applyDeskPcCompletionToProgression` awards the actor `rewardCoins` plus `rewardCoins + 4` XP, stores the desk-PC history fields, records a same-day contribution once, and when the second distinct partner completes the same room day, awards both players `12` coins and `16` XP while marking the ritual complete
  </behavior>
  <action>Extend `src/lib/sharedProgression.ts` with exact exported constants `DESK_PC_XP_OFFSET = 4`, `DAILY_RITUAL_BONUS_COINS = 12`, and `DAILY_RITUAL_BONUS_XP = 16`. Add exact exports `applyPersonalWalletSpend(progression, actorPlayerId, cost, nowIso)`, `applyPersonalWalletRefund(progression, actorPlayerId, amount, nowIso)`, and `applyDeskPcCompletionToProgression({ progression, actorPlayerId, result, memberIds, nowIso })`. `applyDeskPcCompletionToProgression` must update the acting player's `deskPc` fields using `applyPcMinigameResult`, add personal coins and XP immediately, write the contribution to `progression.couple.ritual.completionsByPlayerId[actorPlayerId]`, ignore duplicate same-day contributions from the same actor, and when both member ids have a contribution for the current room day, set `completedAt`, `bonusAppliedAt`, `lastCompletedDayKey`, increment `streakCount`, update `longestStreakCount`, and award the exact daily bonus to both partner records. Add tests in `tests/sharedProgression.test.ts` first, and add or update `tests/pcMinigame.test.ts` only for any shared helper interop that remains in the pure desk-PC math boundary.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts tests/pcMinigame.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedProgression.ts contains "export function applyDeskPcCompletionToProgression"
    - src/lib/sharedProgression.ts contains "DAILY_RITUAL_BONUS_COINS = 12"
    - src/lib/sharedProgression.ts contains "DESK_PC_XP_OFFSET = 4"
    - tests/sharedProgression.test.ts contains "awards the ritual bonus to both partners when the second partner completes the day"
    - tests/sharedProgression.test.ts contains "does not let the same player complete the ritual twice in one room day"
  </acceptance_criteria>
  <done>The repo has deterministic pure helpers for personal spend/refund and desk-PC ritual progression that can be replayed safely after reloads.</done>
</task>

<task type="auto">
  <name>Task 2: Route shared-room buy, sell, and desk-PC handlers through the new personal progression model</name>
  <files>src/App.tsx, src/app/components/InventoryPanel.tsx</files>
  <read_first>src/App.tsx, src/app/components/InventoryPanel.tsx, src/lib/sharedProgression.ts, src/lib/economy.ts, .planning/phases/03-shared-progression-and-ritual-loop/03-01-PLAN.md</read_first>
  <action>Update `src/App.tsx` so shared-room mode no longer spends or refunds the old `playerCoinsRef` scalar directly. In shared-room mode, `handleBuyFurniture` must call `applyPersonalWalletSpend` with the active `session.playerId` and the exact registry price before appending shared `ownedFurniture`. `handleSellStoredFurniture` must call `applyPersonalWalletRefund` with the active `session.playerId` and the exact `getOwnedFurnitureSellPrice(nextSellItem)` before committing the updated room inventory. `handlePcMinigameComplete` must call `applyDeskPcCompletionToProgression` with the active `session.playerId`, the current room member ids, and the exact `PcMinigameResult`, then commit the updated `progression` together with the unchanged room state. Preserve the solo fallback path through local `playerCoins` and local `pcMinigameProgress` when `sharedRoomActive` is false. Update `src/app/components/InventoryPanel.tsx` so affordability, header wallet copy, and need-more messaging all reflect the active player's personal wallet in shared mode while keeping the exact helper line `Purchased furniture becomes shared room inventory.`</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/App.tsx contains "applyPersonalWalletSpend"
    - src/App.tsx contains "applyPersonalWalletRefund"
    - src/App.tsx contains "applyDeskPcCompletionToProgression"
    - src/app/components/InventoryPanel.tsx contains "Purchased furniture becomes shared room inventory."
    - src/app/components/InventoryPanel.tsx contains "Need"
  </acceptance_criteria>
  <done>Shared-room economy actions now actually use the personal wallet model that Phase 03 defines, while the room inventory stays couple-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Show desk-PC ritual progress and bonus outcomes in the room-facing result surface</name>
  <files>src/components/PcMinigameOverlay.tsx, src/App.tsx, tests/sharedRoomRuntime.test.ts, tests/sharedProgression.test.ts</files>
  <read_first>src/components/PcMinigameOverlay.tsx, src/App.tsx, src/lib/sharedProgression.ts, .planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: when only the local player has finished today's desk-PC ritual contribution, the result surface shows `Check-in saved. Waiting on partner.`
    - Test 2: when the second partner completes the same day, the result surface shows `Daily ritual complete` and the streak row reflects the incremented streak count
    - Test 3: shared-room runtime commits the updated ritual and reward state together so reload sees the same wallet, XP, and ritual values
  </behavior>
  <action>Update `src/components/PcMinigameOverlay.tsx` so the result card accepts exact props `dailyRitualStatus`, `dailyRitualBonusCoins`, `dailyRitualBonusXp`, and `streakCount`, then renders exact result rows labeled `Daily ritual`, `Daily bonus`, and `Streak`. When the actor only contributed their own same-day check-in, show the exact status text `Check-in saved. Waiting on partner.`. When the second partner completion closes the ritual, show the exact status text `Daily ritual complete` and the exact bonus string `+12 coins / +16 XP for both partners`. Update `src/App.tsx` to derive and pass those exact props from the shared-room progression mutation outcome and keep solo mode behavior unchanged. Add regression coverage in `tests/sharedRoomRuntime.test.ts` and `tests/sharedProgression.test.ts` proving shared-room desk-PC completion commits the updated ritual state and that the displayed status matches partial vs completed ritual outcomes.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/components/PcMinigameOverlay.tsx contains "Daily ritual"
    - src/components/PcMinigameOverlay.tsx contains "Check-in saved. Waiting on partner."
    - src/components/PcMinigameOverlay.tsx contains "Daily ritual complete"
    - src/components/PcMinigameOverlay.tsx contains "+12 coins / +16 XP for both partners"
    - tests/sharedRoomRuntime.test.ts contains "commits desk pc progression and ritual state through the shared room runtime"
  </acceptance_criteria>
  <done>The first daily ritual is visible where players already look, and desk-PC runs now explain whether they only saved a check-in or fully completed the couple ritual.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts tests/pcMinigame.test.ts tests/sharedRoomRuntime.test.ts
- [ ] cmd /c npm run build
- [ ] Shared-room buy and sell flows use the acting partner's personal wallet, desk-PC runs update personal wallet and XP, and the second same-day partner completion closes the ritual and advances the streak
</verification>

<success_criteria>
The new progression model is no longer passive: the acting partner spends and earns from a personal wallet, the desk PC can close one daily ritual for the couple, and the room-facing desk-PC UI makes that outcome readable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-progression-and-ritual-loop/03-02-SUMMARY.md`
</output>
