---
phase: 03-shared-progression-and-ritual-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sharedProgressionTypes.ts
  - src/lib/sharedProgression.ts
  - src/lib/sharedRoomTypes.ts
  - src/lib/sharedRoomStore.ts
  - src/lib/sharedRoomClient.ts
  - src/lib/sharedRoomValidation.ts
  - scripts/sharedRoomDevPlugin.mjs
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/App.tsx
  - src/app/components/SceneToolbar.tsx
  - src/app/components/SharedRoomStatusStrip.tsx
  - src/app/components/InventoryPanel.tsx
  - src/styles.css
  - tests/sharedProgression.test.ts
  - tests/sharedRoomValidation.test.ts
  - tests/sharedRoomStore.test.ts
  - tests/sharedRoomRuntime.test.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-03
user_setup: []
must_haves:
  truths:
    - "Each shared-room document carries per-player progression and couple ritual state instead of only a shared coin scalar."
    - "The active player can see a personal wallet and level bar in the toolbar while the shared-room status layer shows streak and today's ritual state."
    - "Legacy shared-room documents upgrade forward without duplicating or deleting room purchasing power."
  artifacts:
    - path: "src/lib/sharedProgressionTypes.ts"
      provides: "Canonical personal and couple progression schema"
      min_lines: 70
      contains: "export interface SharedRoomProgressionState"
    - path: "src/lib/sharedProgression.ts"
      provides: "Progression migration, selectors, and ritual view helpers"
      min_lines: 140
      contains: "export function createInitialSharedRoomProgression"
    - path: "src/lib/sharedRoomTypes.ts"
      provides: "Shared room document shape with canonical progression"
      min_lines: 40
      contains: "progression: SharedRoomProgressionState"
    - path: "src/app/components/SceneToolbar.tsx"
      provides: "Toolbar wallet and level surface"
      min_lines: 50
      contains: "Level"
    - path: "src/app/components/SharedRoomStatusStrip.tsx"
      provides: "Shared streak and ritual status surface"
      min_lines: 70
      contains: "Both partners need one desk check-in today."
    - path: "tests/sharedProgression.test.ts"
      provides: "Regression coverage for migration and progression selectors"
      contains: "splits legacy shared coins across the room members"
  key_links:
    - from: "src/lib/sharedRoomValidation.ts"
      to: "src/lib/sharedProgression.ts"
      via: "legacy sharedCoins upgrade and member normalization"
      pattern: "createInitialSharedRoomProgression|ensureSharedRoomProgressionMembers"
    - from: "src/app/hooks/useSharedRoomRuntime.ts"
      to: "src/App.tsx"
      via: "runtimeSnapshot progression adoption"
      pattern: "progression"
    - from: "src/App.tsx"
      to: "src/app/components/SceneToolbar.tsx"
      via: "active-player wallet and level props"
      pattern: "playerLevel|walletLabel|playerXp"
---

<objective>
Define the canonical shared progression model, migrate the shared-room document off top-level `sharedCoins`, and surface the new progression state in the existing shell.

Purpose: Establish one authoritative data contract for personal wallet/level progress plus shared streak/ritual state before any reward or ritual mutations are added.
Output: Progression schema and helpers, legacy migration support, runtime snapshot adoption, toolbar/status-strip UI surfaces, and regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-RESEARCH.md
@src/lib/sharedRoomTypes.ts
@src/lib/sharedRoomStore.ts
@src/lib/sharedRoomValidation.ts
@src/app/hooks/useSharedRoomRuntime.ts
@src/App.tsx
@src/app/components/SceneToolbar.tsx
@src/app/components/SharedRoomStatusStrip.tsx
@src/app/components/InventoryPanel.tsx
@scripts/sharedRoomDevPlugin.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical progression schema, migration math, and selectors</name>
  <files>src/lib/sharedProgressionTypes.ts, src/lib/sharedProgression.ts, tests/sharedProgression.test.ts</files>
  <read_first>.planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md, .planning/phases/03-shared-progression-and-ritual-loop/03-RESEARCH.md, src/lib/sharedRoomTypes.ts, src/lib/pcMinigame.ts, tests/sharedRoomValidation.test.ts</read_first>
  <behavior>
    - Test 1: legacy `sharedCoins` split evenly across current `memberIds`, with any odd remainder assigned to the creator and recorded in `migratedFromSharedCoins`
    - Test 2: `selectActivePlayerProgression` and `buildSharedRitualStatus` return the active-player wallet/level state plus the correct empty or waiting ritual banner
    - Test 3: `ensureSharedRoomProgressionMembers` adds missing member records without rewriting existing coins, XP, levels, or desk-PC progress
  </behavior>
  <action>Create `src/lib/sharedProgressionTypes.ts` with exact exported interfaces `SharedPlayerDeskPcProgress`, `SharedPlayerProgression`, `SharedCoupleRitualContribution`, `SharedCoupleRitualDay`, `SharedCoupleProgression`, and `SharedRoomProgressionState`. Create `src/lib/sharedProgression.ts` with exact exports `ROOM_LEVEL_XP_THRESHOLDS`, `toRoomDayKey(nowIso: string)`, `createInitialSharedRoomProgression(memberIds, members, legacySharedCoins, nowIso)`, `ensureSharedRoomProgressionMembers(progression, memberIds, members, nowIso)`, `selectActivePlayerProgression(progression, playerId)`, `selectPartnerPlayerProgression(progression, playerId)`, and `buildSharedRitualStatus(progression, session)`. Use level thresholds `[0, 20, 55, 95, 140, 190]` for Levels 1-6. Reuse the existing desk-PC lifetime fields by storing `bestScore`, `gamesPlayed`, `lastRewardCoins`, `lastScore`, `lastCompletedAt`, and `totalCoinsEarned` inside `SharedPlayerDeskPcProgress`. Keep the room inventory and `ownedFurniture` model unchanged per D-01 and D-14. Add `tests/sharedProgression.test.ts` first, then implement the helpers so the tests pass.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedProgressionTypes.ts contains "export interface SharedRoomProgressionState"
    - src/lib/sharedProgression.ts contains "export function createInitialSharedRoomProgression"
    - src/lib/sharedProgression.ts contains "export function buildSharedRitualStatus"
    - tests/sharedProgression.test.ts contains "splits legacy shared coins across the room members"
  </acceptance_criteria>
  <done>The repo has one typed progression contract with deterministic migration math and reusable selectors for the shell UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the shared-room document, validation, and dev store with canonical progression</name>
  <files>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomStore.ts, src/lib/sharedRoomClient.ts, src/lib/sharedRoomValidation.ts, scripts/sharedRoomDevPlugin.mjs, src/app/hooks/useSharedRoomRuntime.ts, tests/sharedRoomValidation.test.ts, tests/sharedRoomStore.test.ts, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomStore.ts, src/lib/sharedRoomClient.ts, src/lib/sharedRoomValidation.ts, scripts/sharedRoomDevPlugin.mjs, src/app/hooks/useSharedRoomRuntime.ts, .planning/phases/03-shared-progression-and-ritual-loop/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: legacy documents with top-level `sharedCoins` validate and normalize into a Phase 03 document with `progression`
    - Test 2: `bootstrapDevSharedRoomInDatabase` and `createSharedRoomInDatabase` seed `progression.players` for every member and preserve the shared-room `roomState`
    - Test 3: `createSharedRoomRuntimeSnapshot` exposes `progression` instead of `sharedCoins`
  </behavior>
  <action>Update `src/lib/sharedRoomTypes.ts` so `SharedRoomDocument` contains `progression: SharedRoomProgressionState` and no longer exposes top-level writable `sharedCoins`. Keep `CreateSharedRoomInput` and `BootstrapDevSharedRoomInput` `sharedCoins` fields as seed-only migration inputs for brownfield bootstrap. Update `src/lib/sharedRoomStore.ts` so `CommitSharedRoomStateInput` carries `progression: SharedRoomProgressionState`. Update `src/lib/sharedRoomClient.ts` and `src/app/hooks/useSharedRoomRuntime.ts` to fetch, validate, and expose `runtimeSnapshot.progression`. Update `src/lib/sharedRoomValidation.ts` to accept both legacy `sharedCoins` documents and Phase 03 `progression` documents, normalize them through `createInitialSharedRoomProgression` and `ensureSharedRoomProgressionMembers`, preserve `roomState.metadata.roomId === roomId`, and return only the Phase 03 shape. Update `scripts/sharedRoomDevPlugin.mjs` so `createSharedRoomInDatabase`, `bootstrapDevSharedRoomInDatabase`, `loadSharedRoomInDatabase`, and `commitSharedRoomStateInDatabase` seed or normalize `progression`, write migrated documents back to `.data/shared-room-dev-db.json`, and never keep both top-level writable `sharedCoins` and `progression` in the returned document. Add or extend `tests/sharedRoomValidation.test.ts`, `tests/sharedRoomStore.test.ts`, and `tests/sharedRoomRuntime.test.ts` to prove load-time migration, seeded progression members, and runtime snapshot adoption. Do not introduce a second save path or route progression through presence transport per D-14 and D-15.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomTypes.ts contains "progression: SharedRoomProgressionState"
    - src/lib/sharedRoomStore.ts contains "progression: SharedRoomProgressionState"
    - src/app/hooks/useSharedRoomRuntime.ts contains "progression: roomDocument.progression"
    - scripts/sharedRoomDevPlugin.mjs contains "createInitialSharedRoomProgression"
    - tests/sharedRoomValidation.test.ts contains "upgrades a legacy sharedCoins document into progression"
  </acceptance_criteria>
  <done>The canonical shared-room document, runtime snapshot, and dev store now carry progression as first-class authoritative state.</done>
</task>

<task type="auto">
  <name>Task 3: Surface personal wallet and shared ritual status in the existing room shell</name>
  <files>src/App.tsx, src/app/components/SceneToolbar.tsx, src/app/components/SharedRoomStatusStrip.tsx, src/app/components/InventoryPanel.tsx, src/styles.css</files>
  <read_first>src/App.tsx, src/app/components/SceneToolbar.tsx, src/app/components/SharedRoomStatusStrip.tsx, src/app/components/InventoryPanel.tsx, src/styles.css, .planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md</read_first>
  <action>Update `src/App.tsx` to derive exact `activePlayerProgression`, `partnerPlayerProgression`, `walletLabel`, `playerLevel`, `playerXp`, `playerXpNextLevel`, and `ritualStatus` values from `sharedRoomRuntime.runtimeSnapshot.progression` plus `sharedRoomRuntime.session.playerId`. Update `src/app/components/SceneToolbar.tsx` props to include `walletLabel`, `playerLevel`, `playerXp`, and `playerXpNextLevel`, and render the exact strings `Level {playerLevel}` and `XP {playerXp}/{playerXpNextLevel}` beside the personal wallet without creating a standalone dashboard, per D-10 through D-13. Update `src/app/components/SharedRoomStatusStrip.tsx` so normal presence mode renders streak and ritual copy driven by `buildSharedRitualStatus`, including the exact empty-state line `Both partners need one desk check-in today.` and the exact partial-state line `Your check-in is done. Waiting on partner.` while preserving save/reload override states. Update `src/app/components/InventoryPanel.tsx` header and helper copy to describe the active player's wallet and the exact rule `Purchased furniture becomes shared room inventory.` Update `src/styles.css` with compact classes such as `.scene-toolbar__progress` and `.shared-room-status__ritual-chip`, and split invite/share chrome from progression status so dev bypass can keep progression signals visible during local iteration. Do not add a dedicated progression dashboard or move status UI into debug tools.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/SceneToolbar.tsx contains "Level"
    - src/app/components/SceneToolbar.tsx contains "XP"
    - src/app/components/SharedRoomStatusStrip.tsx contains "Both partners need one desk check-in today."
    - src/app/components/InventoryPanel.tsx contains "Purchased furniture becomes shared room inventory."
    - src/styles.css contains ".scene-toolbar__progress"
  </acceptance_criteria>
  <done>The main room shell visibly reflects personal wallet/level state plus shared streak and ritual status without leaving the room-first UI language.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts
- [ ] cmd /c npm run build
- [ ] Shared-room documents load with `progression`, toolbar shows personal wallet and level, and the status strip shows streak plus today's ritual state
</verification>

<success_criteria>
Personal progression and shared ritual state exist as authoritative shared-room data, legacy documents migrate cleanly, and the room shell makes that state visible without introducing a separate dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-progression-and-ritual-loop/03-01-SUMMARY.md`
</output>
