---
phase: 03-shared-progression-and-ritual-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/lib/sharedRoomStore.ts
  - src/lib/sharedRoomClient.ts
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/lib/sharedProgression.ts
  - src/App.tsx
  - scripts/sharedRoomDevPlugin.mjs
  - tests/sharedRoomStore.test.ts
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedProgression.test.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - RITL-01
user_setup: []
must_haves:
  truths:
    - "Stale shared-room progression writes do not silently overwrite partner wallets, desk-PC history, or ritual state."
    - "Partial same-day ritual progress survives refresh, reconnect, and partner absence until the canonical room day rolls forward."
    - "Missing a full canonical room day resets the couple streak exactly once, while reloads within the same room day preserve current-day contributions."
  artifacts:
    - path: "src/app/hooks/useSharedRoomRuntime.ts"
      provides: "Conflict-aware shared-room mutation helper with reload-and-replay behavior"
      min_lines: 220
      contains: "commitRoomMutation"
    - path: "scripts/sharedRoomDevPlugin.mjs"
      provides: "Strict expectedRevision conflict handling for authoritative shared-room commits"
      min_lines: 280
      contains: "Shared room revision conflict."
    - path: "src/lib/sharedProgression.ts"
      provides: "Room-day rollover and streak reset helpers"
      min_lines: 260
      contains: "export function advanceRitualDayIfNeeded"
    - path: "tests/sharedRoomStore.test.ts"
      provides: "Regression coverage for stale progression commit conflicts"
      contains: "rejects stale progression commits with a 409"
    - path: "tests/sharedRoomRuntime.test.ts"
      provides: "Runtime coverage for reload-and-replay progression recovery"
      contains: "replays a progression mutation after a stale revision conflict"
  key_links:
    - from: "src/app/hooks/useSharedRoomRuntime.ts"
      to: "src/lib/sharedRoomStore.ts"
      via: "conflict-aware commit path for shared-room mutations"
      pattern: "commitRoomMutation|commitSharedRoomState"
    - from: "src/App.tsx"
      to: "src/app/hooks/useSharedRoomRuntime.ts"
      via: "shared-room buy, sell, and desk-PC flows use the conflict-aware mutation helper"
      pattern: "commitRoomMutation"
    - from: "src/lib/sharedProgression.ts"
      to: "scripts/sharedRoomDevPlugin.mjs"
      via: "load-time day rollover and streak normalization"
      pattern: "advanceRitualDayIfNeeded"
---

<objective>
Persist streak logic, refresh-safe ritual progress, and reconnect-safe progression updates without allowing stale snapshot overwrites.

Purpose: Harden Phase 03 so personal rewards, desk-PC ritual contributions, and couple streak state survive reloads and near-simultaneous actions predictably.
Output: Strict revision-conflict handling, a reload-and-replay shared-room mutation helper, room-day rollover and streak reset helpers, and regression coverage for refresh/reconnect safety.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-RESEARCH.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-01-PLAN.md
@.planning/phases/03-shared-progression-and-ritual-loop/03-02-PLAN.md
@src/lib/sharedRoomStore.ts
@src/lib/sharedRoomClient.ts
@src/app/hooks/useSharedRoomRuntime.ts
@src/lib/sharedProgression.ts
@scripts/sharedRoomDevPlugin.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce authoritative revision conflicts instead of blind stale progression overwrites</name>
  <files>src/lib/sharedRoomStore.ts, src/lib/sharedRoomClient.ts, scripts/sharedRoomDevPlugin.mjs, tests/sharedRoomStore.test.ts</files>
  <read_first>src/lib/sharedRoomStore.ts, src/lib/sharedRoomClient.ts, scripts/sharedRoomDevPlugin.mjs, tests/sharedRoomStore.test.ts, .planning/phases/03-shared-progression-and-ritual-loop/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: committing shared-room state with an `expectedRevision` older than the stored room revision returns a 409 and does not mutate room or progression state
    - Test 2: valid commits still persist updated room and progression data and increment revision once
    - Test 3: the client preserves the 409 status and exact message so runtime recovery can distinguish a stale conflict from a transport failure
  </behavior>
  <action>Update `scripts/sharedRoomDevPlugin.mjs` so `commitSharedRoomStateInDatabase` compares the incoming `expectedRevision` to the current stored room revision before writing. If they differ, throw a 409 with the exact message `Shared room revision conflict.` and leave the stored room unchanged. Keep successful commits incrementing revision by exactly one. Update `src/lib/sharedRoomClient.ts` and `src/lib/sharedRoomStore.ts` types so 409 conflict responses surface unchanged to callers instead of being collapsed into a generic network error. Add `tests/sharedRoomStore.test.ts` coverage that proves stale progression commits are rejected, successful commits still persist progression changes, and the 409 message is stable.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomStore.test.ts</verify>
  <acceptance_criteria>
    - scripts/sharedRoomDevPlugin.mjs contains "Shared room revision conflict."
    - tests/sharedRoomStore.test.ts contains "rejects stale progression commits with a 409"
    - src/lib/sharedRoomClient.ts contains "status === 409"
    - src/lib/sharedRoomStore.ts contains "expectedRevision"
  </acceptance_criteria>
  <done>The shared-room store now rejects stale progression writes instead of silently letting later snapshots erase earlier rewards.</done>
</task>

<task type="auto">
  <name>Task 2: Add a reload-and-replay mutation helper for shared-room progression updates</name>
  <files>src/app/hooks/useSharedRoomRuntime.ts, src/App.tsx, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomRuntime.ts, src/App.tsx, src/lib/sharedRoomStore.ts, tests/sharedRoomRuntime.test.ts, .planning/phases/03-shared-progression-and-ritual-loop/03-02-PLAN.md</read_first>
  <action>Extend `src/app/hooks/useSharedRoomRuntime.ts` with an exact helper `commitRoomMutation(reason, mutate)` where `mutate` receives the latest `SharedRoomRuntimeSnapshot` and returns `{ roomState, progression }`. `commitRoomMutation` must call the existing commit endpoint with the current `expectedRevision`, and when a 409 conflict returns, it must reload the latest room, re-run `mutate` once against that fresh snapshot, retry the commit once, and expose the existing status text `Reloading latest room...` during recovery. Update `src/App.tsx` so shared-room buy, sell, and desk-PC completion flows use `commitRoomMutation` instead of assembling commits from potentially stale local refs. Add `tests/sharedRoomRuntime.test.ts` proving a stale progression conflict triggers one reload and replay rather than dropping the mutation.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useSharedRoomRuntime.ts contains "commitRoomMutation"
    - src/app/hooks/useSharedRoomRuntime.ts contains "Reloading latest room..."
    - src/App.tsx contains "commitRoomMutation"
    - tests/sharedRoomRuntime.test.ts contains "replays a progression mutation after a stale revision conflict"
  </acceptance_criteria>
  <done>Progression-affecting shared-room actions recover from stale revisions by replaying the pure mutation against the latest authoritative room instead of silently losing rewards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist room-day rollover, streak reset, and reconnect-safe ritual progress</name>
  <files>src/lib/sharedProgression.ts, src/app/hooks/useSharedRoomRuntime.ts, scripts/sharedRoomDevPlugin.mjs, tests/sharedProgression.test.ts, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/lib/sharedProgression.ts, src/app/hooks/useSharedRoomRuntime.ts, scripts/sharedRoomDevPlugin.mjs, tests/sharedProgression.test.ts, .planning/phases/03-shared-progression-and-ritual-loop/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: refreshing after only one partner has completed today's ritual preserves that partner's contribution and keeps the ritual pending
    - Test 2: advancing past an unfinished ritual day clears the old contributions and resets `streakCount` to `0` exactly once
    - Test 3: advancing past a completed ritual day opens a new day while preserving `lastCompletedDayKey` and `longestStreakCount`
  </behavior>
  <action>Extend `src/lib/sharedProgression.ts` with the exact export `advanceRitualDayIfNeeded(progression, memberIds, members, nowIso)` that derives the current UTC room day via `toRoomDayKey(nowIso)`, ensures missing member records exist, clears stale `progression.couple.ritual.completionsByPlayerId` when the room day advances, and if the prior room day was not fully completed, resets `streakCount` to `0` without touching `longestStreakCount`. Call `advanceRitualDayIfNeeded` during authoritative shared-room load normalization in `scripts/sharedRoomDevPlugin.mjs` and before runtime adoption in `src/app/hooks/useSharedRoomRuntime.ts` so refresh and reconnect show the same ritual state. Add regression coverage in `tests/sharedProgression.test.ts` and `tests/sharedRoomRuntime.test.ts` proving partial same-day progress survives reload, missing one full room day resets the streak, and completed ritual history remains intact across reconnect.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedProgression.test.ts tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedProgression.ts contains "export function advanceRitualDayIfNeeded"
    - tests/sharedProgression.test.ts contains "preserves a partial ritual contribution across a reload in the same room day"
    - tests/sharedProgression.test.ts contains "resets the streak after an unfinished day is missed"
    - tests/sharedRoomRuntime.test.ts contains "hydrates the same partial ritual progress after reload"
  </acceptance_criteria>
  <done>The couple streak and current ritual day now survive refresh and reconnect correctly, and missing a full room day resets the streak in a deterministic way.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts tests/sharedProgression.test.ts
- [ ] cmd /c npm run build
- [ ] Stale progression writes return 409 conflicts, shared-room runtime replays progression mutations after reload, and same-day ritual progress survives refresh while missed days reset the streak deterministically
</verification>

<success_criteria>
Shared progression is now reconnect-safe: stale writes do not silently erase rewards, same-day ritual progress survives reload, and streak rollover/reset rules remain deterministic across partner absence and refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-progression-and-ritual-loop/03-03-SUMMARY.md`
</output>
