---
phase: 04-memories-pets-and-breakup-stakes
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/lib/sharedRoomTypes.ts
  - src/lib/sharedRoomStore.ts
  - src/lib/sharedRoomValidation.ts
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/lib/sharedRoomPet.ts
  - src/lib/pets.ts
  - src/App.tsx
  - src/app/components/InventoryPanel.tsx
  - src/components/RoomView.tsx
  - src/styles.css
  - tests/sharedRoomValidation.test.ts
  - tests/sharedRoomStore.test.ts
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomPet.test.ts
autonomous: true
requirements:
  - PETS-01
user_setup: []
must_haves:
  truths:
    - "The shared-room path owns exactly one canonical curated cat record and does not depend on browser-local sandbox pet ownership."
    - "The live room shows the shared cat through the existing `RoomPetActor` runtime without pulling in Mob Lab persistence."
    - "Player View can adopt the shared cat from shipped UI, while developer-only authoring shortcuts stay in Developer View."
  artifacts:
    - path: "src/lib/sharedRoomTypes.ts"
      provides: "Typed shared-room pet record on the canonical document"
      min_lines: 80
      contains: "export interface SharedRoomPetRecord"
    - path: "src/lib/sharedRoomPet.ts"
      provides: "Pure helper for shared cat creation and runtime adaptation"
      min_lines: 50
      contains: "export function createSharedRoomPetRecord"
    - path: "src/app/components/InventoryPanel.tsx"
      provides: "Shared-room pet adoption UI in the shipped inventory flow"
      min_lines: 200
      contains: "Shared Companion"
    - path: "tests/sharedRoomPet.test.ts"
      provides: "Curated shared-pet helper coverage"
      contains: "creates the curated shared-room cat"
    - path: "tests/sharedRoomStore.test.ts"
      provides: "Shared-room pet persistence regression coverage"
      contains: "persists shared pet adoption on commit"
  key_links:
    - from: "src/lib/sharedRoomValidation.ts"
      to: "src/app/hooks/useSharedRoomRuntime.ts"
      via: "normalized sharedPet field in the canonical runtime snapshot"
      pattern: "sharedPet"
    - from: "src/lib/sharedRoomPet.ts"
      to: "src/App.tsx"
      via: "shared pet record adapted to runtime room pets"
      pattern: "toRuntimeOwnedPet"
    - from: "src/App.tsx"
      to: "src/app/components/InventoryPanel.tsx"
      via: "player-facing shared pet adoption action"
      pattern: "adopt_shared_pet|Shared Companion"
---

<objective>
Promote one curated cat into the shared-room runtime by storing it canonically, rendering it through the existing room pet path, and exposing a shipped player adoption flow that does not break Mob Lab boundaries.

Purpose: Fulfill `PETS-01` while keeping Mob Lab explicit and developer-only.
Output: Canonical shared-pet schema, helper functions for the curated cat, player-shell adoption UI, and `App.tsx` integration that derives live shared-room pets from the shared-room document instead of local sandbox state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-CONTEXT.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-RESEARCH.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-01-PLAN.md
@src/App.tsx
@src/lib/sharedRoomTypes.ts
@src/lib/sharedRoomStore.ts
@src/lib/sharedRoomValidation.ts
@src/lib/pets.ts
@src/lib/petPathing.ts
@src/components/RoomView.tsx
@src/components/room-view/RoomPetActor.tsx
@src/app/components/InventoryPanel.tsx
@src/lib/mobLab.ts
@docs/MOB_LAB.md
@tests/sharedRoomStore.test.ts
@tests/sharedRoomRuntime.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one canonical shared-pet record for the curated cat before wiring UI</name>
  <files>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomStore.ts, src/lib/sharedRoomValidation.ts, src/app/hooks/useSharedRoomRuntime.ts, src/lib/sharedRoomPet.ts, tests/sharedRoomValidation.test.ts, tests/sharedRoomStore.test.ts, tests/sharedRoomRuntime.test.ts, tests/sharedRoomPet.test.ts</files>
  <read_first>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomStore.ts, src/lib/sharedRoomValidation.ts, src/app/hooks/useSharedRoomRuntime.ts, src/lib/pets.ts, src/lib/mobLab.ts, docs/MOB_LAB.md, .planning/phases/04-memories-pets-and-breakup-stakes/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `validateSharedRoomDocument` defaults missing `sharedPet` to `null` and accepts the curated cat record only when it references `minecraft_cat` and `better_cat_glb`
    - Test 2: `createSharedRoomPetRecord` creates the exact curated shared-room cat payload with canonical `spawnPosition`
    - Test 3: shared-room store commit and runtime replay preserve `sharedPet` across save, reload, and stale revision retry
  </behavior>
  <action>Update `src/lib/sharedRoomTypes.ts` with the exact interface `SharedRoomPetRecord` containing `id`, `type`, `presetId`, `spawnPosition`, `adoptedAt`, `adoptedByPlayerId`, and `displayName`. Add `sharedPet: SharedRoomPetRecord | null` to `SharedRoomDocument`. Extend `src/lib/sharedRoomStore.ts` `CommitSharedRoomStateInput` and `src/app/hooks/useSharedRoomRuntime.ts` runtime snapshot and mutation result typing with the exact `sharedPet` field. Create `src/lib/sharedRoomPet.ts` with exact exports `createSharedRoomPetRecord(spawnPosition, actorPlayerId, nowIso)` and `toRuntimeOwnedPet(sharedPet)`; hard-code the first record to `type: "minecraft_cat"` and `presetId: "better_cat_glb"`. Update validation so older documents normalize to `sharedPet: null`. Add tests first so shared-pet shape, defaults, store persistence, and replay behavior are locked before UI changes begin.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts tests/sharedRoomPet.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomTypes.ts contains "export interface SharedRoomPetRecord"
    - src/lib/sharedRoomTypes.ts contains "sharedPet: SharedRoomPetRecord | null"
    - src/lib/sharedRoomPet.ts contains "export function createSharedRoomPetRecord"
    - src/lib/sharedRoomPet.ts contains "presetId: \"better_cat_glb\""
    - src/lib/sharedRoomValidation.ts contains "sharedPet"
    - tests/sharedRoomPet.test.ts contains "creates the curated shared-room cat"
    - tests/sharedRoomStore.test.ts contains "persists shared pet adoption on commit"
  </acceptance_criteria>
  <done>The shared-room document can now carry one canonical curated cat record safely.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the live shared-room cat from canonical state instead of local sandbox pets</name>
  <files>src/App.tsx, src/components/RoomView.tsx, src/lib/sharedRoomPet.ts</files>
  <read_first>src/App.tsx, src/components/RoomView.tsx, src/lib/sharedRoomPet.ts, src/lib/pets.ts, src/lib/petPathing.ts, src/components/room-view/RoomPetActor.tsx</read_first>
  <action>Update `src/App.tsx` so it derives an exact `displayedPets` array: use local `ownedPets` only when `sharedRoomActive` is false, and when `sharedRoomActive` is true adapt `sharedRoomRuntime.runtimeSnapshot?.sharedPet` through `toRuntimeOwnedPet(sharedPet)` into a single-entry pets array or `[]`. Keep `RoomView` unchanged as the one live runtime owner by passing `pets={displayedPets}`. Add a canonical adoption handler that calls `sharedRoomRuntime.commitRoomMutation("adopt_shared_pet", ...)`, uses `pickPetSpawnPosition(playerPosition, liveFurniturePlacements)` for the stored spawn position, and no-ops when a shared pet already exists. Do not reuse local sandbox `setOwnedPets` for shared-room adoption.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/App.tsx contains "const displayedPets"
    - src/App.tsx contains "sharedRoomRuntime.runtimeSnapshot?.sharedPet"
    - src/App.tsx contains "commitRoomMutation(\"adopt_shared_pet\""
    - src/App.tsx contains "pets={displayedPets}"
    - src/lib/sharedRoomPet.ts contains "export function toRuntimeOwnedPet"
  </acceptance_criteria>
  <done>The shared-room cat now comes from canonical shared state while the local sandbox pet path stays intact for non-shared mode.</done>
</task>

<task type="auto">
  <name>Task 3: Expose curated shared-pet adoption in Player View without leaking Mob Lab into shipped UI</name>
  <files>src/app/components/InventoryPanel.tsx, src/App.tsx, src/styles.css</files>
  <read_first>src/app/components/InventoryPanel.tsx, src/App.tsx, src/app/components/PlayerRoomShell.tsx, src/styles.css, .planning/phases/04-memories-pets-and-breakup-stakes/04-CONTEXT.md</read_first>
  <action>Update `src/app/components/InventoryPanel.tsx` so the pet section supports the exact modes `sandbox` and `shared_room`. In `sandbox` mode, preserve the existing temporary pet-store behavior and developer authoring shortcuts. In `shared_room` mode, replace the generic pet-shop copy with one curated card labeled `Shared Companion`, hide the `Open Mob Lab` preview shortcut from Player View, and use the exact primary action label `Adopt for the room` until the cat exists. Update `src/App.tsx` to pass `petCatalogMode={sharedRoomActive ? "shared_room" : "sandbox"}` and keep `showAuthoringActions={developerSurfaceVisible}` so only Developer View exposes authoring shortcuts. Update `src/styles.css` so the shared-room pet card reads like shipped player UI rather than a debug catalog block.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/InventoryPanel.tsx contains "petCatalogMode"
    - src/app/components/InventoryPanel.tsx contains "\"sandbox\" | \"shared_room\""
    - src/app/components/InventoryPanel.tsx contains "Shared Companion"
    - src/app/components/InventoryPanel.tsx contains "Adopt for the room"
    - src/App.tsx contains "petCatalogMode={sharedRoomActive ? \"shared_room\" : \"sandbox\"}"
    - src/styles.css contains ".spawn-card"
  </acceptance_criteria>
  <done>Player View can adopt the curated shared cat from the shipped inventory flow, while Mob Lab remains explicit developer tooling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts tests/sharedRoomPet.test.ts
- [ ] cmd /c npm run build
- [ ] Shared-room mode renders at most one canonical cat from `sharedPet`, local sandbox mode still uses local `ownedPets`, and Player View never exposes Mob Lab as a default pet-adoption path
</verification>

<success_criteria>
The room now has one deliberate live companion: a shared cat that persists canonically and respects the existing Mob Lab authoring boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/04-memories-pets-and-breakup-stakes/04-02-SUMMARY.md`
</output>
