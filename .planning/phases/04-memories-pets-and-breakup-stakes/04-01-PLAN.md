---
phase: 04-memories-pets-and-breakup-stakes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sharedRoomTypes.ts
  - src/lib/sharedRoomStore.ts
  - src/lib/sharedRoomValidation.ts
  - src/app/hooks/useSharedRoomRuntime.ts
  - src/lib/sharedRoomMemories.ts
  - src/lib/memoryFrameImage.ts
  - src/components/StarterFurnitureModels.tsx
  - src/components/RoomView.tsx
  - src/components/room-view/FurnitureVisual.tsx
  - src/components/room-view/RoomFurnitureActor.tsx
  - src/components/room-view/RoomFurnitureLayer.tsx
  - src/components/room-view/RoomSelectedFurnitureLayer.tsx
  - src/components/room-view/RoomMemoryFrameProxy.tsx
  - src/app/components/MemoryFrameDialog.tsx
  - src/App.tsx
  - src/styles.css
  - tests/sharedRoomValidation.test.ts
  - tests/sharedRoomStore.test.ts
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomMemories.test.ts
autonomous: true
requirements:
  - MEMR-01
user_setup: []
must_haves:
  truths:
    - "A placed `wall_frame` can hold one canonical shared image and one optional canonical caption."
    - "Frame-memory records survive reload and shared-room conflict replay because they commit through the same authoritative mutation path as room edits."
    - "Removing or storing a frame cannot leave orphan memory records attached to missing furniture ids."
  artifacts:
    - path: "src/lib/sharedRoomTypes.ts"
      provides: "Typed canonical frame-memory record on the shared-room document"
      min_lines: 60
      contains: "export interface SharedRoomFrameMemory"
    - path: "src/lib/sharedRoomMemories.ts"
      provides: "Pure frame-memory pruning and sanitization helpers"
      min_lines: 60
      contains: "export function pruneSharedRoomFrameMemories"
    - path: "src/app/components/MemoryFrameDialog.tsx"
      provides: "Player-shell frame editor"
      min_lines: 80
      contains: "Upload shared photo"
    - path: "src/components/room-view/RoomMemoryFrameProxy.tsx"
      provides: "Non-build-mode wall-frame interaction bridge"
      min_lines: 30
      contains: "Open shared memory"
    - path: "tests/sharedRoomMemories.test.ts"
      provides: "Frame-memory domain regression coverage"
      contains: "prunes frame memories for missing wall frames"
  key_links:
    - from: "src/lib/sharedRoomValidation.ts"
      to: "src/app/hooks/useSharedRoomRuntime.ts"
      via: "normalized frameMemories field inside the canonical runtime snapshot"
      pattern: "frameMemories"
    - from: "src/components/RoomView.tsx"
      to: "src/app/components/MemoryFrameDialog.tsx"
      via: "wall-frame inspect callback into the player shell"
      pattern: "onOpenMemoryFrame"
    - from: "src/App.tsx"
      to: "src/app/hooks/useSharedRoomRuntime.ts"
      via: "memory save and clear mutations"
      pattern: "save_memory_frame|clear_memory_frame"
---

<objective>
Add one shared memory object to the live shared room by reusing the existing `wall_frame` placement path, rendering its personalized content in the 3D room, and editing it from the shipped player shell.

Purpose: Fulfill `MEMR-01` without creating a second placement system or leaking memory data into local-only persistence.
Output: Canonical frame-memory schema, pruning and image-processing helpers, room render support for personalized frames, and a player-shell editor that saves through the authoritative shared-room runtime.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-CONTEXT.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-RESEARCH.md
@src/App.tsx
@src/app/hooks/useSharedRoomRuntime.ts
@src/lib/sharedRoomTypes.ts
@src/lib/sharedRoomStore.ts
@src/lib/sharedRoomValidation.ts
@src/lib/roomState.ts
@src/lib/furnitureRegistry.ts
@src/components/RoomView.tsx
@src/components/StarterFurnitureModels.tsx
@src/components/room-view/FurnitureVisual.tsx
@tests/sharedRoomStore.test.ts
@tests/sharedRoomRuntime.test.ts
@tests/sharedRoomValidation.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the shared-room schema and mutation path for canonical frame memories first</name>
  <files>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomStore.ts, src/lib/sharedRoomValidation.ts, src/app/hooks/useSharedRoomRuntime.ts, src/lib/sharedRoomMemories.ts, tests/sharedRoomValidation.test.ts, tests/sharedRoomStore.test.ts, tests/sharedRoomRuntime.test.ts, tests/sharedRoomMemories.test.ts</files>
  <read_first>src/lib/sharedRoomTypes.ts, src/lib/sharedRoomStore.ts, src/lib/sharedRoomValidation.ts, src/app/hooks/useSharedRoomRuntime.ts, src/lib/roomState.ts, .planning/phases/04-memories-pets-and-breakup-stakes/04-CONTEXT.md, .planning/phases/04-memories-pets-and-breakup-stakes/04-RESEARCH.md, tests/sharedRoomStore.test.ts, tests/sharedRoomRuntime.test.ts</read_first>
  <behavior>
    - Test 1: `validateSharedRoomDocument` defaults missing `frameMemories` to `{}` and preserves valid memory records for placed `wall_frame` ids
    - Test 2: `pruneSharedRoomFrameMemories` removes records whose furniture ids are no longer placed `wall_frame` items
    - Test 3: shared-room store commit and runtime replay keep `frameMemories` intact across load, save, and stale-revision retry
  </behavior>
  <action>Update `src/lib/sharedRoomTypes.ts` with the exact interface `SharedRoomFrameMemory` containing `furnitureId`, `imageDataUrl`, `caption`, `updatedAt`, and `updatedByPlayerId`, and add `frameMemories: Record<string, SharedRoomFrameMemory>` to `SharedRoomDocument`. Extend `src/lib/sharedRoomStore.ts` `CommitSharedRoomStateInput` with the exact field `frameMemories`. Create `src/lib/sharedRoomMemories.ts` with exact exports `sanitizeMemoryFrameCaption(caption: string | null): string | null`, `pruneSharedRoomFrameMemories(frameMemories, roomState)`, and `upsertSharedRoomFrameMemory(frameMemories, memory)`. Update `src/lib/sharedRoomValidation.ts` so older documents normalize to `frameMemories: {}` and every validation path prunes frame-memory records against placed `wall_frame` ids before returning the document. Extend `src/app/hooks/useSharedRoomRuntime.ts` so `SharedRoomRuntimeSnapshot`, `SharedRoomMutation`, `commitRoomState`, and `commitRoomMutation` all carry `frameMemories` through the canonical mutation path. Add or update tests first so validation, store commit, runtime replay, and pruning behavior are locked before UI work begins.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts tests/sharedRoomMemories.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomTypes.ts contains "export interface SharedRoomFrameMemory"
    - src/lib/sharedRoomTypes.ts contains "frameMemories: Record<string, SharedRoomFrameMemory>"
    - src/lib/sharedRoomStore.ts contains "frameMemories:"
    - src/lib/sharedRoomValidation.ts contains "frameMemories"
    - src/lib/sharedRoomMemories.ts contains "export function pruneSharedRoomFrameMemories"
    - tests/sharedRoomMemories.test.ts contains "prunes frame memories for missing wall frames"
    - tests/sharedRoomStore.test.ts contains "persists frame memories on commit"
  </acceptance_criteria>
  <done>The shared-room boundary can now carry, normalize, and replay one frame-memory map safely.</done>
</task>

<task type="auto">
  <name>Task 2: Render personalized wall-frame content in the live room and expose a player-shell inspect hook</name>
  <files>src/components/StarterFurnitureModels.tsx, src/components/RoomView.tsx, src/components/room-view/FurnitureVisual.tsx, src/components/room-view/RoomFurnitureActor.tsx, src/components/room-view/RoomFurnitureLayer.tsx, src/components/room-view/RoomSelectedFurnitureLayer.tsx, src/components/room-view/RoomMemoryFrameProxy.tsx</files>
  <read_first>src/components/StarterFurnitureModels.tsx, src/components/RoomView.tsx, src/components/room-view/FurnitureVisual.tsx, src/components/room-view/RoomFurnitureActor.tsx, src/components/room-view/RoomInteractionProxy.tsx, src/lib/furnitureRegistry.ts, src/lib/sharedRoomTypes.ts</read_first>
  <action>Extend `WallFrameModel` in `src/components/StarterFurnitureModels.tsx` with the exact optional props `memoryImageSrc?: string | null` and `memoryCaption?: string | null`, and render the existing art plane as a personalized image surface when `memoryImageSrc` is present while keeping the placeholder frame appearance when it is absent. Thread `frameMemories` through `RoomView`, `RoomFurnitureLayer`, `RoomSelectedFurnitureLayer`, `RoomFurnitureActor`, and `FurnitureVisual`, with `FurnitureVisual` looking up the memory record for each `wall_frame`. Create `src/components/room-view/RoomMemoryFrameProxy.tsx` as a non-build-mode interaction proxy that appears only for `wall_frame` items and exposes the exact visible hint text `Open shared memory`. Update `RoomView.tsx` with an exact callback prop `onOpenMemoryFrame(furnitureId: string)` so clicking a placed frame in Player View opens the editor path instead of doing nothing.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/components/StarterFurnitureModels.tsx contains "memoryImageSrc?: string | null"
    - src/components/StarterFurnitureModels.tsx contains "memoryCaption?: string | null"
    - src/components/RoomView.tsx contains "onOpenMemoryFrame"
    - src/components/room-view/RoomMemoryFrameProxy.tsx contains "Open shared memory"
    - src/components/room-view/FurnitureVisual.tsx contains "frameMemory"
    - src/components/room-view/RoomFurnitureActor.tsx contains "onOpenMemoryFrame"
  </acceptance_criteria>
  <done>The 3D room can now show canonical frame content and hand wall-frame interaction back to the shipped player shell.</done>
</task>

<task type="auto">
  <name>Task 3: Add a player-shell memory editor with canonical save and clear actions</name>
  <files>src/lib/memoryFrameImage.ts, src/app/components/MemoryFrameDialog.tsx, src/App.tsx, src/styles.css</files>
  <read_first>src/App.tsx, src/app/components/PlayerRoomShell.tsx, src/app/components/PlayerRoomDetailsSheet.tsx, src/lib/sharedRoomMemories.ts, src/app/hooks/useSharedRoomRuntime.ts, src/styles.css, .planning/phases/04-memories-pets-and-breakup-stakes/04-RESEARCH.md</read_first>
  <action>Create `src/lib/memoryFrameImage.ts` with an exact export `prepareMemoryFrameImage(file: File): Promise<string>` that resizes and encodes one uploaded image into a canonical data URL for the room document. Create `src/app/components/MemoryFrameDialog.tsx` with exact visible labels `Upload shared photo`, `Caption`, `Save memory`, and `Clear memory`. Update `src/App.tsx` to own `openMemoryFrameId`, dialog open state, draft caption state, and upload handling; when saving, call `sharedRoomRuntime.commitRoomMutation("save_memory_frame", ...)` to upsert `frameMemories[furnitureId]`, and when clearing, call `sharedRoomRuntime.commitRoomMutation("clear_memory_frame", ...)` to remove that record. Mount the dialog only in Player View and keep Developer View unchanged. Update `src/styles.css` with exact root selectors `.memory-frame-dialog`, `.memory-frame-dialog__panel`, and `.memory-frame-dialog__image-preview` so the editor reads as shipped player UI rather than a dev utility pane.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/lib/memoryFrameImage.ts contains "export async function prepareMemoryFrameImage"
    - src/app/components/MemoryFrameDialog.tsx contains "Upload shared photo"
    - src/app/components/MemoryFrameDialog.tsx contains "Save memory"
    - src/app/components/MemoryFrameDialog.tsx contains "Clear memory"
    - src/App.tsx contains "commitRoomMutation(\"save_memory_frame\""
    - src/App.tsx contains "commitRoomMutation(\"clear_memory_frame\""
    - src/styles.css contains ".memory-frame-dialog"
    - src/styles.css contains ".memory-frame-dialog__image-preview"
  </acceptance_criteria>
  <done>The player can open a placed wall frame, upload one shared image, add a short caption, and save or clear it canonically.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomValidation.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts tests/sharedRoomMemories.test.ts
- [ ] cmd /c npm run build
- [ ] A placed `wall_frame` can be opened from Player View, saved canonically, and reloaded with the same image/caption while stale frame-memory records are pruned automatically
</verification>

<success_criteria>
The room now has one real shared memory slice: the existing `wall_frame` becomes a canonical personalized object instead of placeholder decor.
</success_criteria>

<output>
After completion, create `.planning/phases/04-memories-pets-and-breakup-stakes/04-01-SUMMARY.md`
</output>
