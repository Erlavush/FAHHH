---
phase: 04-memories-pets-and-breakup-stakes
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/lib/sharedRoomReset.ts
  - src/app/components/SharedRoomEntryShell.tsx
  - src/app/components/PlayerRoomDetailsSheet.tsx
  - src/app/components/BreakupResetDialog.tsx
  - src/app/shellViewModel.ts
  - src/App.tsx
  - src/styles.css
  - tests/sharedRoomRuntime.test.ts
  - tests/sharedRoomStore.test.ts
  - tests/sharedRoomReset.test.ts
autonomous: true
requirements:
  - STAK-01
  - STAK-02
user_setup: []
must_haves:
  truths:
    - "The create/join shell explains the breakup consequence before the player commits to the shared-room loop."
    - "The active shared-room danger zone requires explicit confirmation before resetting the canonical room."
    - "A confirmed breakup reset clears shared progression, shared decor and inventory state, frame memories, and the shared cat while preserving local profile and authoring-tool data."
  artifacts:
    - path: "src/lib/sharedRoomReset.ts"
      provides: "Pure canonical breakup reset helper"
      min_lines: 50
      contains: "export function createBreakupResetMutation"
    - path: "src/app/components/SharedRoomEntryShell.tsx"
      provides: "Pre-commit stakes warning in the create/join flow"
      min_lines: 80
      contains: "Shared rooms can be reset if the relationship ends."
    - path: "src/app/components/BreakupResetDialog.tsx"
      provides: "Player-shell danger-zone confirmation dialog"
      min_lines: 70
      contains: "Type RESET to confirm"
    - path: "tests/sharedRoomReset.test.ts"
      provides: "Pure reset contract coverage"
      contains: "clears frame memories and the shared pet"
    - path: "src/App.tsx"
      provides: "Breakup reset mutation wiring"
      contains: "commitRoomMutation(\"breakup_reset\""
  key_links:
    - from: "src/lib/sharedRoomReset.ts"
      to: "src/App.tsx"
      via: "one pure breakup reset mutation committed through the shared room runtime"
      pattern: "createBreakupResetMutation|breakup_reset"
    - from: "src/app/components/SharedRoomEntryShell.tsx"
      to: "src/App.tsx"
      via: "pre-commit acknowledgment gating create and join actions"
      pattern: "stakesAcknowledged"
    - from: "src/app/components/PlayerRoomDetailsSheet.tsx"
      to: "src/app/components/BreakupResetDialog.tsx"
      via: "danger-zone entry point in the shipped player shell"
      pattern: "Break up and reset room"
---

<objective>
Add the breakup-stakes loop by warning the player before room commitment, exposing a danger-zone reset surface in Player View, and committing one canonical fresh-baseline mutation when the reset is confirmed.

Purpose: Fulfill `STAK-01` and `STAK-02` while clearing all relationship-tied shared-room state introduced across Phases 1-4.
Output: Stakes warning in the shared-room entry shell, a player-shell breakup confirmation dialog, and a pure reset helper committed through the authoritative shared-room mutation path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-CONTEXT.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-RESEARCH.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-01-PLAN.md
@.planning/phases/04-memories-pets-and-breakup-stakes/04-02-PLAN.md
@src/App.tsx
@src/app/components/SharedRoomEntryShell.tsx
@src/app/components/PlayerRoomDetailsSheet.tsx
@src/app/shellViewModel.ts
@src/app/hooks/useSharedRoomRuntime.ts
@src/lib/sharedProgression.ts
@src/lib/sharedRoomSeed.ts
@src/lib/roomState.ts
@tests/sharedRoomRuntime.test.ts
@tests/sharedRoomStore.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a pure breakup reset helper that rebuilds the full shared-room baseline</name>
  <files>src/lib/sharedRoomReset.ts, tests/sharedRoomReset.test.ts, tests/sharedRoomStore.test.ts, tests/sharedRoomRuntime.test.ts</files>
  <read_first>src/app/hooks/useSharedRoomRuntime.ts, src/lib/sharedProgression.ts, src/lib/sharedRoomSeed.ts, src/lib/roomState.ts, src/lib/sharedRoomTypes.ts, .planning/phases/04-memories-pets-and-breakup-stakes/04-RESEARCH.md, tests/sharedRoomRuntime.test.ts</read_first>
  <behavior>
    - Test 1: `createBreakupResetMutation` keeps the same `roomId`, `memberIds`, and `members` while replacing the room contents with the fresh shared starter baseline
    - Test 2: the reset clears progression carryover, `frameMemories`, and `sharedPet`
    - Test 3: runtime commit and replay converge on the reset snapshot after a revision conflict
  </behavior>
  <action>Create `src/lib/sharedRoomReset.ts` with the exact export `createBreakupResetMutation(snapshot, actorPlayerId, nowIso)` that returns `roomState`, `progression`, `frameMemories`, and `sharedPet`. Build the next room state from `createSharedRoomSeed(snapshot.roomId, createDefaultRoomState())`, rebuild progression with `createInitialSharedRoomProgression(snapshot.memberIds, snapshot.members, 0, nowIso)`, set `frameMemories` to `{}`, and set `sharedPet` to `null`. Add tests first so the reset helper proves it preserves room membership and room identity while clearing relationship-tied shared data, and extend shared-room runtime or store tests where needed to cover reset replay behavior.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/sharedRoomReset.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts</verify>
  <acceptance_criteria>
    - src/lib/sharedRoomReset.ts contains "export function createBreakupResetMutation"
    - src/lib/sharedRoomReset.ts contains "createSharedRoomSeed"
    - src/lib/sharedRoomReset.ts contains "createInitialSharedRoomProgression"
    - tests/sharedRoomReset.test.ts contains "clears frame memories and the shared pet"
    - tests/sharedRoomRuntime.test.ts contains "replays breakup reset after a stale revision conflict"
  </acceptance_criteria>
  <done>The breakup contract is now encoded in one pure helper instead of scattered field clearing.</done>
</task>

<task type="auto">
  <name>Task 2: Add breakup-stakes disclosure to the shared-room create and join shell</name>
  <files>src/app/components/SharedRoomEntryShell.tsx, src/App.tsx, src/styles.css</files>
  <read_first>src/app/components/SharedRoomEntryShell.tsx, src/App.tsx, src/app/components/SharedRoomBlockingOverlay.tsx, src/styles.css, .planning/phases/04-memories-pets-and-breakup-stakes/04-CONTEXT.md</read_first>
  <action>Update `src/app/components/SharedRoomEntryShell.tsx` so the create/join flow includes a stakes callout with the exact copy `Shared rooms can be reset if the relationship ends.` and `This clears shared progression, room decor, memories, and your shared pet.` Add an internal acknowledgment checkbox with the exact label `I understand the shared room can be reset later` and keep both `Create Shared Room` and `Join with Code` disabled until it is checked. Update `src/App.tsx` only as needed to keep the existing create/join handlers intact. Add supporting styles under the exact selectors `.shared-room-entry__stakes` and `.shared-room-entry__acknowledgment` so the warning reads as intentional shipped copy rather than an inline debug note.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/SharedRoomEntryShell.tsx contains "Shared rooms can be reset if the relationship ends."
    - src/app/components/SharedRoomEntryShell.tsx contains "This clears shared progression, room decor, memories, and your shared pet."
    - src/app/components/SharedRoomEntryShell.tsx contains "I understand the shared room can be reset later"
    - src/styles.css contains ".shared-room-entry__stakes"
    - src/styles.css contains ".shared-room-entry__acknowledgment"
  </acceptance_criteria>
  <done>The player is warned about breakup consequences before ever entering the shared-room loop.</done>
</task>

<task type="auto">
  <name>Task 3: Add a player-shell danger zone and explicit breakup confirmation dialog</name>
  <files>src/app/components/PlayerRoomDetailsSheet.tsx, src/app/components/BreakupResetDialog.tsx, src/app/shellViewModel.ts, src/App.tsx, src/styles.css</files>
  <read_first>src/app/components/PlayerRoomDetailsSheet.tsx, src/app/shellViewModel.ts, src/App.tsx, src/lib/sharedRoomReset.ts, src/app/hooks/useSharedRoomRuntime.ts, src/styles.css, .planning/phases/04-memories-pets-and-breakup-stakes/04-CONTEXT.md</read_first>
  <action>Create `src/app/components/BreakupResetDialog.tsx` with the exact title `Break up and reset room`, the exact confirmation prompt `Type RESET to confirm`, and a destructive action button labeled `Reset shared room`. Extend `src/app/shellViewModel.ts` and `PlayerRoomDetailsSheet.tsx` so Player View exposes a dedicated danger-zone entry labeled `Break up and reset room` only when `sharedRoomActive` is true. Update `src/App.tsx` to own dialog-open state, discard unconfirmed local draft edits before reset when necessary, and on confirm call `sharedRoomRuntime.commitRoomMutation("breakup_reset", ...)` using `createBreakupResetMutation(...)`. Keep this entry out of `ViewModeSwitch`, `DeveloperWorkspaceHeader`, and `DeveloperSessionPanel`; it belongs only in the shipped player shell. Update `src/styles.css` with exact selectors `.player-room-details-sheet__danger-zone`, `.breakup-reset-dialog`, and `.breakup-reset-dialog__confirm-input` so the danger zone reads as intentional and separate from routine actions.</action>
  <verify>cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/components/BreakupResetDialog.tsx contains "Break up and reset room"
    - src/app/components/BreakupResetDialog.tsx contains "Type RESET to confirm"
    - src/app/components/BreakupResetDialog.tsx contains "Reset shared room"
    - src/app/components/PlayerRoomDetailsSheet.tsx contains "Break up and reset room"
    - src/App.tsx contains "commitRoomMutation(\"breakup_reset\""
    - src/styles.css contains ".player-room-details-sheet__danger-zone"
    - src/styles.css contains ".breakup-reset-dialog"
  </acceptance_criteria>
  <done>The player gets an explicit danger-zone breakup flow that clears canonical shared-room state only after deliberate confirmation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/sharedRoomReset.test.ts tests/sharedRoomStore.test.ts tests/sharedRoomRuntime.test.ts
- [ ] cmd /c npm run build
- [ ] Shared-room entry requires acknowledgment of breakup stakes before create/join, and a confirmed player-shell breakup reset returns the shared room to the intended fresh baseline while preserving non-shared local data
</verification>

<success_criteria>
The shared room now has real emotional stakes: players are warned before commitment, and an explicit danger-zone reset clears the shared relationship state exactly as designed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-memories-pets-and-breakup-stakes/04-03-SUMMARY.md`
</output>
