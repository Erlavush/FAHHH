---
phase: 07-memory-collection-and-shared-pet-depth
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - src/lib/sharedRoomTypes.ts
  - src/lib/sharedRoomPet.ts
  - tests/sharedRoomPet.test.ts
---

# Plan 07-02: Shared Pet Depth

**Goal:** Upgrade the shared pet schema and logic to support multiple cats, variants, and richer care/behavior state.

## Context
Phase 07 aims to deepen personalization. Task 2 focuses on pets, specifically moving from a single cat with hardcoded defaults to a roster of distinct cats with persisted names, personalities, and care needs.

## Tasks

1.  **Schema Update:**
    *   Update `SharedRoomPetRecord` in `src/lib/sharedRoomTypes.ts` to include `displayName`, `behaviorProfileId`, and `care`.
    *   Ensure `SharedRoomDocument` continues to use a record of memories keyed by `furnitureId`. (Oops, that's Task 1 stuff, let's focus on Pets).

2.  **Logic Update:**
    *   Update `src/lib/sharedRoomPet.ts` to include `cloneSharedRoomPetRecord` (updated), `migrateLegacySharedPet`, and an enhanced `toRuntimeOwnedPet`.
    *   Implement helper functions for managing a roster of shared pets if necessary.

3.  **Verification:**
    *   Add new tests to `tests/sharedRoomPet.test.ts` for legacy migration, multi-cat record creation, and care state persistence mapping.

## Success Criteria
- [ ] `SharedRoomPetRecord` includes `displayName`, `behaviorProfileId`, and `care` fields.
- [ ] Multiple cats can each have unique persisted names and personalities in the shared room state.
- [ ] Legacy shared-pet records (without names/care) can be migrated to the new schema.
- [ ] Regression tests for shared pet management pass.

## Output
After completion, create `.planning/phases/07-memory-collection-and-shared-pet-depth/07-02-SUMMARY.md`
