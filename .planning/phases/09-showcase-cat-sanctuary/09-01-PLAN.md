---
phase: 09-showcase-cat-sanctuary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pets.ts
  - src/lib/devLocalState.ts
  - src/app/hooks/useLocalRoomSession.ts
  - src/app/hooks/useAppRoomActions.ts
  - tests/pets.test.ts
  - tests/devLocalState.test.ts
autonomous: true
requirements:
  - PETS-03
  - SHOW-01
user_setup: []
must_haves:
  truths:
    - "Local showcase runtime can keep multiple active cats in one room and move cats between active room and stored roster without losing persistence."
    - "The local sandbox pet roster is richer, but the hosted shared-room path still preserves its current single canonical shared cat contract."
    - "Pet ownership is no longer blocked by a one-per-type sandbox gate for cats."
  artifacts:
    - path: "src/lib/pets.ts"
      provides: "Showcase cat roster types and multi-cat helper constructors"
      contains: "export type OwnedPetStatus = \"active_room\" | \"stored_roster\""
    - path: "src/lib/devLocalState.ts"
      provides: "Sandbox persistence validation for roster-ready cat records"
      contains: "value.status === \"active_room\" || value.status === \"stored_roster\""
    - path: "src/app/hooks/useAppRoomActions.ts"
      provides: "Local-only cat adopt/store/activate actions with shared-room compatibility guard"
      contains: "const handleStorePet = useCallback"
    - path: "tests/devLocalState.test.ts"
      provides: "Round-trip coverage for active and stored cats"
      contains: "persists active-room and stored-roster cats"
---

<objective>
Create a roster-ready local cat foundation for the showcase build before behavior or care-loop work starts.

Purpose: let the sandbox/local runtime own multiple cats, store cats without deleting them, and persist that richer roster safely while leaving the hosted single-cat shared-room path intact.
Output: richer `OwnedPet` records, persistence normalization, and local adopt/store/activate actions that can drive a visible multi-cat room during the showcase.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md
@.planning/phases/09-showcase-cat-sanctuary/09-RESEARCH.md
@src/lib/pets.ts
@src/lib/devLocalState.ts
@src/app/hooks/useLocalRoomSession.ts
@src/app/hooks/useAppRoomActions.ts
@src/lib/sharedRoomTypes.ts
@src/lib/sharedRoomPet.ts
@tests/pets.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add roster-ready cat state to the local pet model and sandbox persistence</name>
  <files>src/lib/pets.ts, src/lib/devLocalState.ts, tests/pets.test.ts, tests/devLocalState.test.ts</files>
  <read_first>src/lib/pets.ts, src/lib/devLocalState.ts, .planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md, .planning/phases/09-showcase-cat-sanctuary/09-RESEARCH.md, tests/pets.test.ts</read_first>
  <action>Extend `src/lib/pets.ts` with the exact exports `OwnedPetStatus = "active_room" | "stored_roster"` and `PetBehaviorProfileId = "lazy" | "curious" | "clingy" | "zoomies"`. Add the exact `OwnedPetCareState` fields `hunger`, `affection`, `energy`, `lastUpdatedAt`, and `lastCareActionAt`, then extend `OwnedPet` with `displayName`, `status`, `behaviorProfileId`, and `care`. Update `createOwnedPet(...)` to accept exact options `displayName`, `status`, `behaviorProfileId`, and `nowIso`. In `src/lib/devLocalState.ts`, update `isValidOwnedPet`, normalization, and load/save cloning so older cats that only have `id`, `type`, `presetId`, `acquiredFrom`, and `spawnPosition` normalize to `status: "active_room"`, `behaviorProfileId: "curious"`, and `care` values `hunger: 75`, `affection: 75`, `energy: 75`. Add `tests/devLocalState.test.ts` and expand `tests/pets.test.ts` first so the richer cat record and persistence round-trip are locked before action wiring lands.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/pets.test.ts tests/devLocalState.test.ts</verify>
  <acceptance_criteria>
    - src/lib/pets.ts contains "export type OwnedPetStatus = \"active_room\" | \"stored_roster\""
    - src/lib/pets.ts contains "behaviorProfileId: PetBehaviorProfileId"
    - src/lib/pets.ts contains "care: OwnedPetCareState"
    - src/lib/devLocalState.ts contains "value.status === \"active_room\" || value.status === \"stored_roster\""
    - src/lib/devLocalState.ts contains "behaviorProfileId"
    - tests/devLocalState.test.ts contains "persists active-room and stored-roster cats"
  </acceptance_criteria>
  <done>The local cat roster can now persist real showcase cat records instead of shallow one-off pet unlocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add local adopt, store, and activate callbacks while preserving hosted single-cat behavior</name>
  <files>src/app/hooks/useLocalRoomSession.ts, src/app/hooks/useAppRoomActions.ts, src/lib/pets.ts, tests/pets.test.ts</files>
  <read_first>src/app/hooks/useLocalRoomSession.ts, src/app/hooks/useAppRoomActions.ts, src/lib/pets.ts, src/lib/sharedRoomTypes.ts, src/lib/sharedRoomPet.ts, .planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md</read_first>
  <action>Add the exact local constants `MAX_ACTIVE_SHOWCASE_CATS = 6` and `MAX_TOTAL_SHOWCASE_CATS = 12` inside `src/app/hooks/useAppRoomActions.ts`. Update the local sandbox branch of `handleBuyPet` so `type === "minecraft_cat"` can be purchased multiple times until `MAX_TOTAL_SHOWCASE_CATS` is reached, assigns generated names `Cat 1`, `Cat 2`, and so on, and spawns new cats as `status: "active_room"` until the active cap is reached, then as `status: "stored_roster"`. Add the exact callbacks `handleStorePet(petId)`, `handleActivateStoredPet(petId)`, and `handleRemovePet(petId)` for the local sandbox path; `handleStorePet` must preserve the cat record and just flip `status`, `handleActivateStoredPet` must refuse activation when six cats are already active, and `handleRemovePet` must remove a local sandbox cat record cleanly. Keep the `sharedRoomActive` branch unchanged except for compatibility guards: it must still allow only the existing single shared `minecraft_cat` adoption flow through `createSharedRoomPetRecord(...)`.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/pets.test.ts tests/devLocalState.test.ts</verify>
  <acceptance_criteria>
    - src/app/hooks/useAppRoomActions.ts contains "const MAX_ACTIVE_SHOWCASE_CATS = 6"
    - src/app/hooks/useAppRoomActions.ts contains "const MAX_TOTAL_SHOWCASE_CATS = 12"
    - src/app/hooks/useAppRoomActions.ts contains "const handleStorePet = useCallback"
    - src/app/hooks/useAppRoomActions.ts contains "const handleActivateStoredPet = useCallback"
    - src/app/hooks/useAppRoomActions.ts contains "const handleRemovePet = useCallback"
    - src/app/hooks/useAppRoomActions.ts contains "type === \"minecraft_cat\""
  </acceptance_criteria>
  <done>The showcase build can now accumulate a visible local cat roster without breaking the hosted shared-cat path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/pets.test.ts tests/devLocalState.test.ts
- [ ] Local sandbox cats can be active or stored without deleting ownership
- [ ] Hosted shared-room adoption still stays on one canonical shared cat
</verification>

<success_criteria>
The repo now has a persistence-safe local cat roster that can visibly support a multi-cat showcase room.
</success_criteria>

<output>
After completion, create `.planning/phases/09-showcase-cat-sanctuary/09-01-SUMMARY.md`
</output>
