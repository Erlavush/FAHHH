---
phase: 09-showcase-cat-sanctuary
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/lib/petBehavior.ts
  - src/lib/petPathing.ts
  - src/components/room-view/RoomPetActor.tsx
  - src/components/mob-lab/MobPreviewActor.tsx
  - src/components/mob-lab/GlbMobPreviewActor.tsx
  - tests/petBehavior.test.ts
  - tests/petPathing.test.ts
autonomous: true
requirements:
  - PETS-04
  - SHOW-01
user_setup: []
must_haves:
  truths:
    - "Cats no longer read as walk-only props; the runtime exposes readable behavior states with sit, lick, and sleep outcomes."
    - "Multiple active cats move around the room more believably without abandoning the current room-safe obstacle math."
    - "The Better Cats GLB actor can present requested behavior states through clip selection or explicit fallback poses."
  artifacts:
    - path: "src/lib/petBehavior.ts"
      provides: "Pure behavior-state selection for showcase cats"
      contains: "export type PetBehaviorState = \"roam\" | \"follow_player\" | \"sitting\" | \"licking\" | \"sleeping\""
    - path: "src/lib/petPathing.ts"
      provides: "Smarter target selection for follow, rest, and recovery"
      contains: "export function pickPetRestTarget"
    - path: "src/components/room-view/RoomPetActor.tsx"
      provides: "Runtime cat state machine and behavior-driven motion"
      contains: "const behaviorStateRef = useRef"
    - path: "src/components/mob-lab/GlbMobPreviewActor.tsx"
      provides: "Behavior-state clip bridge and fallback poses for Better Cats"
      contains: "function playNamedAction"
---

<objective>
Upgrade the room runtime so showcase cats feel visibly alive instead of just wandering props.

Purpose: replace walk-only cat motion with a behavior-driven runtime that follows the player sometimes, rests in believable spots, and presents `sit`, `lick`, and `sleep` visually.
Output: pure behavior helpers, smarter target selection, and behavior-state animation support in the Better Cats actor.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md
@.planning/phases/09-showcase-cat-sanctuary/09-RESEARCH.md
@src/lib/pets.ts
@src/lib/petPathing.ts
@src/components/room-view/RoomPetActor.tsx
@src/components/mob-lab/MobPreviewActor.tsx
@src/components/mob-lab/GlbMobPreviewActor.tsx
@tests/petPathing.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure showcase cat behavior rules and smarter target helpers</name>
  <files>src/lib/petBehavior.ts, src/lib/petPathing.ts, tests/petBehavior.test.ts, tests/petPathing.test.ts</files>
  <read_first>src/lib/pets.ts, src/lib/petPathing.ts, .planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md, .planning/phases/09-showcase-cat-sanctuary/09-RESEARCH.md, tests/petPathing.test.ts</read_first>
  <action>Create the exact file `src/lib/petBehavior.ts` with `PetBehaviorState = "roam" | "follow_player" | "sitting" | "licking" | "sleeping"` and the exact exports `selectPetBehaviorState`, `getBehaviorStateDurationMs`, and `shouldFollowPlayer`. Use `behaviorProfileId`, `care.energy`, `care.affection`, player distance, and idle time as decision inputs. Extend `src/lib/petPathing.ts` with the exact helpers `pickPetRestTarget`, `pickPetFollowTarget`, and `pickPetRecoveryTarget`, plus an optional `otherPetPositions` input on target-picking so nearby cats behave as soft obstacles. Keep existing room clamps, furniture obstacle rules, and player clearance behavior intact. Add `tests/petBehavior.test.ts` and extend `tests/petPathing.test.ts` first to cover sleepy cats preferring sleep states, clingy cats following distant players, and recovery target selection when a cat gets trapped.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/petBehavior.test.ts tests/petPathing.test.ts</verify>
  <acceptance_criteria>
    - src/lib/petBehavior.ts contains "export type PetBehaviorState = \"roam\" | \"follow_player\" | \"sitting\" | \"licking\" | \"sleeping\""
    - src/lib/petBehavior.ts contains "export function selectPetBehaviorState"
    - src/lib/petPathing.ts contains "export function pickPetRestTarget"
    - src/lib/petPathing.ts contains "export function pickPetRecoveryTarget"
    - tests/petBehavior.test.ts contains "prefers sleeping when energy is low"
    - tests/petPathing.test.ts contains "avoids other pet positions as soft obstacles"
  </acceptance_criteria>
  <done>The repo now has a pure behavior-and-targeting layer strong enough to drive showcase cat life without a navmesh rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bridge behavior state into RoomPetActor and the Better Cats GLB actor</name>
  <files>src/components/room-view/RoomPetActor.tsx, src/components/mob-lab/MobPreviewActor.tsx, src/components/mob-lab/GlbMobPreviewActor.tsx, src/lib/petBehavior.ts</files>
  <read_first>src/components/room-view/RoomPetActor.tsx, src/components/mob-lab/MobPreviewActor.tsx, src/components/mob-lab/GlbMobPreviewActor.tsx, src/lib/petBehavior.ts, src/lib/petPathing.ts</read_first>
  <action>Extend `MobExternalMotionState` in `src/components/mob-lab/MobPreviewActor.tsx` with the exact field `behaviorState: "idle" | "walk" | "sit" | "lick" | "sleep"`. Update `src/components/room-view/RoomPetActor.tsx` to keep `behaviorStateRef`, `stateStartedAtRef`, and `homePositionRef`, switch between `walk` and the new idle states using `selectPetBehaviorState(...)`, and only move when the chosen state is `roam` or `follow_player`. Update `src/components/mob-lab/GlbMobPreviewActor.tsx` to add the exact helper `playNamedAction(preferredNames: string[])` and map requested behavior states to the preferred clip-name lists `["sleep", "idle_sleep", "lay"]`, `["sit", "idle_sit"]`, and `["lick", "groom"]`. When no matching clip exists, apply an explicit fallback pose by setting head, tail, and leg rotations for the requested behavior instead of silently falling back to the walk loop. Keep the current walk loop intact for moving cats.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/petBehavior.test.ts tests/petPathing.test.ts</verify>
  <acceptance_criteria>
    - src/components/room-view/RoomPetActor.tsx contains "const behaviorStateRef = useRef"
    - src/components/room-view/RoomPetActor.tsx contains "behaviorState:"
    - src/components/mob-lab/MobPreviewActor.tsx contains "behaviorState: \"idle\" | \"walk\" | \"sit\" | \"lick\" | \"sleep\""
    - src/components/mob-lab/GlbMobPreviewActor.tsx contains "function playNamedAction"
    - src/components/mob-lab/GlbMobPreviewActor.tsx contains "[\"sleep\", \"idle_sleep\", \"lay\"]"
    - src/components/mob-lab/GlbMobPreviewActor.tsx contains "[\"lick\", \"groom\"]"
  </acceptance_criteria>
  <done>The live room can now show cats sitting, licking, and sleeping instead of only wandering and walking.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/petBehavior.test.ts tests/petPathing.test.ts
- [ ] Behavior state drives visible sit, lick, and sleep outcomes in the runtime cat actor
- [ ] Movement stays inside the room and avoids furniture without regressing the current obstacle model
</verification>

<success_criteria>
Showcase cats now feel believably alive through smarter movement and readable idle states.
</success_criteria>

<output>
After completion, create `.planning/phases/09-showcase-cat-sanctuary/09-02-SUMMARY.md`
</output>
