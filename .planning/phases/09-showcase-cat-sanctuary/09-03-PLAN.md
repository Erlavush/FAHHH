---
phase: 09-showcase-cat-sanctuary
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/lib/catCare.ts
  - src/app/hooks/useLocalRoomSession.ts
  - src/app/hooks/useAppRoomActions.ts
  - src/App.tsx
  - src/app/shellViewModel.ts
  - src/components/ui/InventoryPanel.tsx
  - tests/catCare.test.ts
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - ACTV-02
  - SHOW-01
  - PETS-03
user_setup: []
must_haves:
  truths:
    - "The player can care for cats during the showcase session and earn coins without removing the existing PC loop."
    - "App and shell surfaces expose active cats, stored cats, and cats needing care with minimal non-destructive UI contract changes."
    - "The full test suite and production build still pass after the showcase loop is integrated."
  artifacts:
    - path: "src/lib/catCare.ts"
      provides: "Pure cat-care decay, reward, and summary helpers"
      contains: "export type CatCareActionId = \"feed\" | \"pet\" | \"play\""
    - path: "src/app/hooks/useAppRoomActions.ts"
      provides: "Cat-care action callback integrated into the existing local reward boundary"
      contains: "const handleCareForPet = useCallback"
    - path: "src/app/shellViewModel.ts"
      provides: "Showcase cat-sanctuary shell labels for the parallel UI owner"
      contains: "catsNeedingCareLabel"
    - path: "tests/catCare.test.ts"
      provides: "Cat-care reward and summary regression coverage"
      contains: "awards coins when feeding a hungry cat"
---

<objective>
Turn the richer cat roster and behavior work into a replayable showcase loop.

Purpose: add a lightweight cat-care reward domain, wire it into the existing local app callbacks and shell labels, and keep the PC loop as a complementary coin source.
Output: feed/pet/play care actions, cat-care summaries for the shell, and final app integration that stays friendly to the parallel UI owner.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md
@.planning/phases/09-showcase-cat-sanctuary/09-RESEARCH.md
@src/app/hooks/useLocalRoomSession.ts
@src/app/hooks/useAppRoomActions.ts
@src/App.tsx
@src/app/shellViewModel.ts
@src/components/ui/InventoryPanel.tsx
@src/lib/pcMinigame.ts
@tests/shellViewModel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a lightweight cat-care domain with readable rewards and summaries</name>
  <files>src/lib/catCare.ts, src/app/hooks/useLocalRoomSession.ts, tests/catCare.test.ts</files>
  <read_first>src/lib/pets.ts, src/app/hooks/useLocalRoomSession.ts, .planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md, .planning/phases/09-showcase-cat-sanctuary/09-RESEARCH.md</read_first>
  <action>Create the exact file `src/lib/catCare.ts` with `CatCareActionId = "feed" | "pet" | "play"`, `tickOwnedPetCareState`, `selectCatsNeedingCare`, `applyCatCareAction`, and `buildCatCareSummary`. Use normalized `0..100` needs and the exact reward/default adjustments `feed -> hunger +35, coins +2`, `pet -> affection +25, coins +1`, and `play -> energy +20, affection +10, coins +3`. A cat needs attention when any tracked value is below `55`. Update `src/app/hooks/useLocalRoomSession.ts` so active-room cats tick their needs based on `lastUpdatedAt` while stored cats keep their last saved values until reactivated. Add `tests/catCare.test.ts` first to lock decay, summary, and reward behavior.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/catCare.test.ts tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/lib/catCare.ts contains "export type CatCareActionId = \"feed\" | \"pet\" | \"play\""
    - src/lib/catCare.ts contains "rewardCoins: 3"
    - src/lib/catCare.ts contains "return value < 55"
    - tests/catCare.test.ts contains "awards coins when feeding a hungry cat"
    - tests/catCare.test.ts contains "selects cats needing care when any stat drops below 55"
  </acceptance_criteria>
  <done>The repo now has a simple, testable cat-care loop that can reward the player during the showcase session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire cat-care, adopt/store summaries, and final showcase integration into the app without fighting parallel UI work</name>
  <files>src/app/hooks/useAppRoomActions.ts, src/App.tsx, src/app/shellViewModel.ts, src/components/ui/InventoryPanel.tsx, tests/shellViewModel.test.ts</files>
  <read_first>src/app/hooks/useAppRoomActions.ts, src/App.tsx, src/app/shellViewModel.ts, src/components/ui/InventoryPanel.tsx, src/lib/catCare.ts, .planning/phases/09-showcase-cat-sanctuary/09-CONTEXT.md</read_first>
  <action>Update `src/app/hooks/useAppRoomActions.ts` with the exact callback `handleCareForPet(petId, actionId)` that calls `applyCatCareAction(...)`, updates the matching local cat record, and adds the returned coins through `commitPlayerCoins` on the sandbox path. Keep the existing PC minigame reward flow unchanged. In `src/App.tsx`, compute `activeCats`, `storedCats`, and `catsNeedingCare` from the local roster, pass the new cat actions through the existing app-player view and inventory surfaces, and keep shared-room mode on its current single shared cat path. In `src/app/shellViewModel.ts`, add the exact labels `activeCatCountLabel`, `storedCatCountLabel`, and `catsNeedingCareLabel` so the parallel UI agent can surface the loop without inventing new selectors. If `InventoryPanel.tsx` needs touchpoints, restrict edits to prop wiring and button/copy hooks only; do not perform layout or polish rewrites in this phase. Finish by updating `tests/shellViewModel.test.ts`, then run `cmd /c npm test` and `cmd /c npm run build`.</action>
  <verify>cmd /c npm test &amp;&amp; cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/app/hooks/useAppRoomActions.ts contains "const handleCareForPet = useCallback"
    - src/App.tsx contains "catsNeedingCare"
    - src/app/shellViewModel.ts contains "activeCatCountLabel"
    - src/app/shellViewModel.ts contains "storedCatCountLabel"
    - src/app/shellViewModel.ts contains "catsNeedingCareLabel"
    - tests/shellViewModel.test.ts contains "catsNeedingCareLabel"
    - cmd /c npm test exits 0
    - cmd /c npm run build exits 0
  </acceptance_criteria>
  <done>The showcase slice now has a full visible loop: cats need care, care grants coins, and the shell has enough state for the UI owner to present the loop cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test
- [ ] cmd /c npm run build
- [ ] Player can earn coins from PC play and cat care without losing local roster progress
- [ ] Shell/view-model contracts expose active cats, stored cats, and cats needing care
</verification>

<success_criteria>
Phase 09 now becomes a replayable showcase loop instead of only a tech demo: the room supports many cats, readable cat care, and immediate motivation to keep playing.
</success_criteria>

<output>
After completion, create `.planning/phases/09-showcase-cat-sanctuary/09-03-SUMMARY.md`
</output>
