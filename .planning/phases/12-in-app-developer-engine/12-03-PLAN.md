---
wave: 3
depends_on: ["12-01", "12-02"]
files_modified: ["src/components/ui/ContentManager.tsx"]
autonomous: true
requirements: ["ENG-01", "ENG-02"]
---

# Plan 12-03: Wire the UI to the local API to generate and overwrite `furnitureRegistry.ts` safely

## Objective
Connect the frontend "Content Manager" UI directly to the `inAppEditorPlugin` Vite middleware. This creates the full loop allowing visual edits to permanently persist back to the project's source code files.

<tasks>
<task>
<read_first>
- `src/components/ui/ContentManager.tsx`
- `scripts/inAppEditorPlugin.mjs`
</read_first>
<action>
Inside `src/components/ui/ContentManager.tsx`, implement the `handleSaveToCodebase` async function mapping to the API endpoint `/__editor/save-furniture`.

Add a `fetch` block using the `POST` method. Pass the serialized JSON payload composed from the actively edited local state (`JSON.stringify(registryState)`). Fire this save routine when the user clicks the explicit "Save to Codebase" action button. 

Append loading states and a success notification so the developer visually understands that the file has been successfully written on the backend.
</action>
<acceptance_criteria>
- `ContentManager.tsx` issues a `fetch('/__editor/save-furniture')` POST request on click.
- The `fetch` calls successfully transit the stringified registry.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/components/ui/ContentManager.tsx`
- `package.json`
</read_first>
<action>
Wrap the specific "Save to Codebase" button in an environment check so it completely disappears from production builds.

Rely on Vite's native environment check variable `import.meta.env.DEV` to conditionally render the Save UI functionality. This guarantees production users never even see or interact with the dev-only capabilities.
</action>
<acceptance_criteria>
- The `Save to Codebase` `<button>` string must only appear if `import.meta.env.DEV` matches a truthy validation check.
</acceptance_criteria>
</task>
</tasks>

## Verification
- Start the server (`npm run dev`), change the price or label of the `basic_chair` in the UI Dashboard, hit "Save to Codebase," and verify that `src/lib/furnitureRegistry.ts` literally changes within VSCode in exactly 1-2 seconds.
- Run `npm run build` and `npm run preview` to verify that the "Save to Codebase" button is safely removed in production layouts.

## Must Haves
- The structure of the pushed `FURNITURE_REGISTRY` object must fundamentally conform to its original object architecture (no accidental nulls or stripped typings) or HMR module reloading will forcefully crash React due to type assertion bounds.
