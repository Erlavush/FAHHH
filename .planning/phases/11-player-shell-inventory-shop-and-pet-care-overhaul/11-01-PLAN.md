---
phase: 11-player-shell-inventory-shop-and-pet-care-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/types.ts
  - src/app/shellViewModel.ts
  - src/App.tsx
  - src/components/ui/InventoryPanel.tsx
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - SHELL-01
user_setup: []
must_haves:
  truths:
    - "The player shell exposes an explicit drawer mode contract for Inventory, Shop, and Pet Care instead of encoding the split only in one mixed panel."
    - "App-level state can open the existing player drawer into a specific mode without changing room, furniture, or pet data semantics."
    - "Shared-room single-companion behavior and sandbox local-cat behavior stay on the same safe drawer contract."
  artifacts:
    - path: "src/app/types.ts"
      provides: "Shared player-drawer mode type for App and UI components"
      contains: "export type PlayerDrawerMode = \"inventory\" | \"shop\" | \"pet_care\""
    - path: "src/app/shellViewModel.ts"
      provides: "Drawer tab metadata and player-safe labels for the split commerce/care shell"
      contains: "export function getPlayerDrawerTabsState"
    - path: "tests/shellViewModel.test.ts"
      provides: "Regression coverage for split drawer tab labels and care-routing copy"
      contains: "returns split drawer tabs for inventory, shop, and pet care"
---

<objective>
Define the shell-state and information-architecture contract for the split player drawer before visual rebuild work begins.

Purpose: make `Inventory`, `Shop`, and `Pet Care` first-class drawer modes in App and the shell selector layer while preserving the current drawer mount point and behavior boundaries.
Output: mode types, shell tab metadata, App-level active-mode state, and updated drawer prop contracts ready for component split work.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-UI-SPEC.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-RESEARCH.md
@src/app/types.ts
@src/app/shellViewModel.ts
@src/App.tsx
@src/components/ui/InventoryPanel.tsx
@tests/shellViewModel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit drawer mode types and shell tab metadata</name>
  <files>src/app/types.ts, src/app/shellViewModel.ts, tests/shellViewModel.test.ts</files>
  <read_first>src/app/types.ts, src/app/shellViewModel.ts, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-UI-SPEC.md, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-RESEARCH.md, tests/shellViewModel.test.ts</read_first>
  <action>In `src/app/types.ts`, add the exact exported type `PlayerDrawerMode = "inventory" | "shop" | "pet_care"`. In `src/app/shellViewModel.ts`, add the exact exported function `getPlayerDrawerTabsState(activeMode: PlayerDrawerMode, sharedRoomActive: boolean, catsNeedingCareCount: number)` that returns three tab entries with the exact labels `Inventory`, `Shop`, and `Pet Care`. Give the `Pet Care` tab an optional urgency badge when `catsNeedingCareCount > 0`, and keep shared-room mode copy player-safe rather than tool-facing. Update `tests/shellViewModel.test.ts` first so it asserts the exact tab labels and includes a case named `returns split drawer tabs for inventory, shop, and pet care`.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/app/types.ts contains `export type PlayerDrawerMode = "inventory" | "shop" | "pet_care"`
    - src/app/shellViewModel.ts contains `export function getPlayerDrawerTabsState`
    - src/app/shellViewModel.ts contains `Pet Care`
    - tests/shellViewModel.test.ts contains `returns split drawer tabs for inventory, shop, and pet care`
    - tests/shellViewModel.test.ts contains `Pet Care`
  </acceptance_criteria>
  <done>The shell selector layer now knows the drawer has three real player-facing modes instead of one mixed panel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Thread active drawer mode through App and the current drawer prop contract</name>
  <files>src/App.tsx, src/components/ui/InventoryPanel.tsx</files>
  <read_first>src/App.tsx, src/components/ui/InventoryPanel.tsx, src/app/types.ts, src/app/shellViewModel.ts, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md</read_first>
  <action>Update `src/App.tsx` to add the exact state pair `const [playerDrawerMode, setPlayerDrawerMode] = useState<PlayerDrawerMode>("inventory")` near the existing player-shell UI state, and pass that state into the drawer node. Update `src/components/ui/InventoryPanel.tsx` so its props include the exact additions `activeMode: PlayerDrawerMode` and `onChangeMode: (mode: PlayerDrawerMode) => void`. Do not split the component layout yet in this plan; keep the current mixed render tree intact while making the mode contract available for the next plan.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts</verify>
  <acceptance_criteria>
    - src/App.tsx contains `const [playerDrawerMode, setPlayerDrawerMode] = useState<PlayerDrawerMode>("inventory")`
    - src/components/ui/InventoryPanel.tsx contains `activeMode: PlayerDrawerMode`
    - src/components/ui/InventoryPanel.tsx contains `onChangeMode: (mode: PlayerDrawerMode) => void`
    - src/App.tsx contains `activeMode={playerDrawerMode}`
  </acceptance_criteria>
  <done>The current drawer mount now carries an explicit active mode, so the visual split can land without inventing new shell state later.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts
- [ ] App owns `playerDrawerMode` and passes it into the drawer
- [ ] Shell selectors expose `Inventory`, `Shop`, and `Pet Care` as real player-facing modes
</verification>

<success_criteria>
Phase 11 now has the shell-state contract needed to split the drawer cleanly in later plans without touching room or pet domain rules.
</success_criteria>

<output>
After completion, create `.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-01-SUMMARY.md`
</output>
