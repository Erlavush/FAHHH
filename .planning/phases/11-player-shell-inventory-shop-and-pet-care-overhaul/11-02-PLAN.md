---
phase: 11-player-shell-inventory-shop-and-pet-care-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/ui/InventoryPanel.tsx
  - src/components/ui/PlayerDrawerTabs.tsx
  - src/components/ui/PlayerInventorySection.tsx
  - src/components/ui/PlayerShopSection.tsx
  - src/components/ui/PlayerPetCareSection.tsx
  - src/components/ui/index.ts
  - src/components/ui/player-inventory-panel.css
  - tests/inventoryPanel.test.tsx
autonomous: true
requirements:
  - SHELL-01
user_setup: []
must_haves:
  truths:
    - "The drawer visually belongs to the warm bottom HUD and digital cat clock rather than the older AMOLED panel style."
    - "Inventory, Shop, and Pet Care each render through dedicated section components with the current actions moved into the correct surface."
    - "The active drawer mode changes the visible player surface without introducing developer-only authoring chrome."
  artifacts:
    - path: "src/components/ui/PlayerDrawerTabs.tsx"
      provides: "Reusable top-level mode switch for the split player drawer"
      contains: "Pet Care"
    - path: "src/components/ui/PlayerPetCareSection.tsx"
      provides: "Dedicated player-shell cat management and care surface"
      contains: "Feed +2"
    - path: "src/components/ui/player-inventory-panel.css"
      provides: "Warm drawer theme tokens and mode-specific shell styling"
      contains: "--drawer-shell-bg"
    - path: "tests/inventoryPanel.test.tsx"
      provides: "Regression coverage for tab rendering and surface switching"
      contains: "renders Inventory, Shop, and Pet Care tabs"
---

<objective>
Split the current mixed drawer into dedicated player-facing surfaces and retheme it to the warm HUD and cat-clock language.

Purpose: create explicit section components for `Inventory`, `Shop`, and `Pet Care`, then rebuild the drawer CSS so it looks like part of the shipped player shell rather than a leftover AMOLED panel.
Output: tab switcher, dedicated section components, warm themed drawer CSS, and jsdom coverage for surface switching.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-UI-SPEC.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-RESEARCH.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-01-PLAN.md
@src/components/ui/InventoryPanel.tsx
@src/components/ui/player-inventory-panel.css
@src/components/ui/player-hud-dock.css
@src/components/ui/minecraft-clock.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split the mixed drawer into explicit Inventory, Shop, and Pet Care section components</name>
  <files>src/components/ui/InventoryPanel.tsx, src/components/ui/PlayerDrawerTabs.tsx, src/components/ui/PlayerInventorySection.tsx, src/components/ui/PlayerShopSection.tsx, src/components/ui/PlayerPetCareSection.tsx, src/components/ui/index.ts, tests/inventoryPanel.test.tsx</files>
  <read_first>src/components/ui/InventoryPanel.tsx, src/components/ui/index.ts, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-UI-SPEC.md, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-RESEARCH.md</read_first>
  <action>Create the exact new components `src/components/ui/PlayerDrawerTabs.tsx`, `src/components/ui/PlayerInventorySection.tsx`, `src/components/ui/PlayerShopSection.tsx`, and `src/components/ui/PlayerPetCareSection.tsx`. Refactor `src/components/ui/InventoryPanel.tsx` so it becomes a shell coordinator that renders one framed header, the top-level tab switcher with the exact labels `Inventory`, `Shop`, and `Pet Care`, and only the active section chosen by `activeMode`. Move stored-furniture cards and `Place` / `Sell` / info actions into `PlayerInventorySection`, move furniture shop plus cat adoption cards into `PlayerShopSection`, and move active/stored cat care cards plus `Feed +2`, `Pet +1`, `Play +3`, `Store`, `Activate`, and `Remove` actions into `PlayerPetCareSection`. Add `tests/inventoryPanel.test.tsx` first so it asserts tab rendering and surface switching, including a case named `renders Inventory, Shop, and Pet Care tabs`.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/inventoryPanel.test.tsx</verify>
  <acceptance_criteria>
    - src/components/ui/PlayerDrawerTabs.tsx contains `Pet Care`
    - src/components/ui/PlayerInventorySection.tsx contains `Furniture Inventory`
    - src/components/ui/PlayerShopSection.tsx contains `Furniture Shop`
    - src/components/ui/PlayerPetCareSection.tsx contains `Feed +2`
    - src/components/ui/InventoryPanel.tsx contains `activeMode === "pet_care"`
    - tests/inventoryPanel.test.tsx contains `renders Inventory, Shop, and Pet Care tabs`
  </acceptance_criteria>
  <done>The drawer now has real top-level player surfaces instead of one mixed scroll panel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace the AMOLED drawer styling with warm HUD-aligned theme tokens and layout</name>
  <files>src/components/ui/player-inventory-panel.css, src/components/ui/InventoryPanel.tsx, src/components/ui/PlayerDrawerTabs.tsx, src/components/ui/PlayerInventorySection.tsx, src/components/ui/PlayerShopSection.tsx, src/components/ui/PlayerPetCareSection.tsx</files>
  <read_first>src/components/ui/player-inventory-panel.css, src/components/ui/player-hud-dock.css, src/components/ui/minecraft-clock.css, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-UI-SPEC.md</read_first>
  <action>In `src/components/ui/player-inventory-panel.css`, add the exact custom properties `--drawer-shell-bg`, `--drawer-shell-panel`, `--drawer-shell-ink`, `--drawer-shell-accent`, `--drawer-shell-chip`, and `--drawer-shell-shadow`. Apply those warm drawer tokens to `.spawn-panel`, `.spawn-card`, `.spawn-card__price`, `.spawn-card__button`, and the new `.player-drawer-tabs*` classes so the drawer picks up the same walnut, cream, and amber language already used by `player-hud-dock.css` and `minecraft-clock.css`. Keep the drawer width inside the `420px` to `460px` desktop target from the UI-SPEC, and add a `@media (max-width: 900px)` block so the split surfaces remain readable on mobile.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/inventoryPanel.test.tsx</verify>
  <acceptance_criteria>
    - src/components/ui/player-inventory-panel.css contains `--drawer-shell-bg`
    - src/components/ui/player-inventory-panel.css contains `--drawer-shell-accent`
    - src/components/ui/player-inventory-panel.css contains `.player-drawer-tabs`
    - src/components/ui/player-inventory-panel.css contains `@media (max-width: 900px)`
    - src/components/ui/PlayerDrawerTabs.tsx contains `Inventory`
    - src/components/ui/PlayerDrawerTabs.tsx contains `Shop`
  </acceptance_criteria>
  <done>The drawer now feels visually native to the warm player shell instead of looking like a separate AMOLED panel.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test -- --maxWorkers 1 tests/inventoryPanel.test.tsx
- [ ] Drawer renders explicit `Inventory`, `Shop`, and `Pet Care` top-level modes
- [ ] Drawer styling follows the warm HUD / cat-clock palette instead of black/white neutral chrome
</verification>

<success_criteria>
Phase 11 now has the correct visual shell and dedicated player surfaces for inventory, shopping, and pet care.
</success_criteria>

<output>
After completion, create `.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-02-SUMMARY.md`
</output>
