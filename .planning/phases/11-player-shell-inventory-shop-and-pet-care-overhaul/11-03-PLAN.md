---
phase: 11-player-shell-inventory-shop-and-pet-care-overhaul
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/App.tsx
  - src/app/components/AppPlayerView.tsx
  - src/app/hooks/useAppShellCallbacks.ts
  - src/components/ui/InventoryPanel.tsx
  - src/components/ui/PlayerCompanionCard.tsx
  - tests/inventoryPanel.test.tsx
  - tests/shellViewModel.test.ts
autonomous: true
requirements:
  - SHELL-01
user_setup: []
must_haves:
  truths:
    - "The player can reach the correct drawer surface from the existing shell without adding bottom-HUD clutter."
    - "Shared-room mode keeps its single-companion truth and does not expose the sandbox's local multi-cat pet-care controls."
    - "The full test suite and production build still pass after the new shell flow is wired."
  artifacts:
    - path: "src/app/hooks/useAppShellCallbacks.ts"
      provides: "Dedicated helper for opening the split drawer in a specific mode"
      contains: "openPlayerDrawerMode"
    - path: "src/components/ui/PlayerCompanionCard.tsx"
      provides: "Care shortcut from the companion shell into the dedicated Pet Care surface"
      contains: "Open Pet Care"
    - path: "src/components/ui/InventoryPanel.tsx"
      provides: "Shared-room-safe Pet Care fallback state"
      contains: "Shared companion care uses the room interaction flow right now."
    - path: "tests/inventoryPanel.test.tsx"
      provides: "Regression coverage for shared-room-safe pet-care rendering"
      contains: "hides sandbox pet care controls in shared room mode"
---

<objective>
Finish the player-shell flow so the split drawer is easy to reach, shared-room-safe, and fully regression-verified.

Purpose: wire care-specific entry points into the existing shell, keep shared-room pet care honest, and verify the whole app still passes tests and build after the drawer overhaul lands.
Output: drawer-mode opening helpers, optional care shortcut, shared-room-safe pet-care fallback, and full regression verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-UI-SPEC.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-RESEARCH.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-01-PLAN.md
@.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-02-PLAN.md
@src/App.tsx
@src/app/components/AppPlayerView.tsx
@src/app/hooks/useAppShellCallbacks.ts
@src/components/ui/InventoryPanel.tsx
@src/components/ui/PlayerCompanionCard.tsx
@tests/inventoryPanel.test.tsx
@tests/shellViewModel.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire player-shell shortcuts so the drawer can open directly into Pet Care when needed</name>
  <files>src/app/hooks/useAppShellCallbacks.ts, src/App.tsx, src/app/components/AppPlayerView.tsx, src/components/ui/PlayerCompanionCard.tsx, tests/shellViewModel.test.ts</files>
  <read_first>src/app/hooks/useAppShellCallbacks.ts, src/App.tsx, src/app/components/AppPlayerView.tsx, src/components/ui/PlayerCompanionCard.tsx, src/app/shellViewModel.ts, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md</read_first>
  <action>In `src/app/hooks/useAppShellCallbacks.ts`, add the exact helper `openPlayerDrawerMode(mode: PlayerDrawerMode)` that sets the active drawer mode and opens the existing drawer without creating a new modal path. Keep the current inventory dock affordance and route it through `openPlayerDrawerMode("inventory")`. Update `src/App.tsx` and `src/app/components/AppPlayerView.tsx` so the new drawer-mode helper can be passed into the player shell. In `src/components/ui/PlayerCompanionCard.tsx`, add one secondary player-facing action with the exact label `Open Pet Care` that appears when the shell already knows at least one cat needs care, and route it through `openPlayerDrawerMode("pet_care")`. Do not add a second primary dock button for `Shop` or `Pet Care` in this plan.</action>
  <verify>cmd /c npm test -- --maxWorkers 1 tests/shellViewModel.test.ts tests/inventoryPanel.test.tsx</verify>
  <acceptance_criteria>
    - src/app/hooks/useAppShellCallbacks.ts contains `openPlayerDrawerMode`
    - src/app/hooks/useAppShellCallbacks.ts contains `openPlayerDrawerMode("inventory")`
    - src/components/ui/PlayerCompanionCard.tsx contains `Open Pet Care`
    - src/App.tsx contains `setPlayerDrawerMode`
    - tests/shellViewModel.test.ts contains `Open Pet Care`
  </acceptance_criteria>
  <done>The split drawer is now reachable through intentional player-shell shortcuts without bloating the bottom HUD.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keep shared-room Pet Care honest, then run full regression and build verification</name>
  <files>src/components/ui/InventoryPanel.tsx, tests/inventoryPanel.test.tsx, tests/shellViewModel.test.ts</files>
  <read_first>src/components/ui/InventoryPanel.tsx, src/lib/pets.ts, src/lib/catCare.ts, .planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-CONTEXT.md, tests/inventoryPanel.test.tsx</read_first>
  <action>Update `src/components/ui/InventoryPanel.tsx` so when `petCatalogMode === "shared_room"`, the `Pet Care` surface renders a shared-room-safe informational state with the exact sentence `Shared companion care uses the room interaction flow right now.` and does not render the sandbox local-cat controls `Feed +2`, `Pet +1`, or `Play +3`. Update `tests/inventoryPanel.test.tsx` with a case named `hides sandbox pet care controls in shared room mode`, and extend `tests/shellViewModel.test.ts` if needed to verify any new shortcut copy. Finish by running the full commands `cmd /c npm test` and `cmd /c npm run build`.</action>
  <verify>cmd /c npm test &amp;&amp; cmd /c npm run build</verify>
  <acceptance_criteria>
    - src/components/ui/InventoryPanel.tsx contains `Shared companion care uses the room interaction flow right now.`
    - src/components/ui/InventoryPanel.tsx does not contain a shared-room branch that renders `Feed +2` buttons for local roster care
    - tests/inventoryPanel.test.tsx contains `hides sandbox pet care controls in shared room mode`
    - cmd /c npm test exits 0
    - cmd /c npm run build exits 0
  </acceptance_criteria>
  <done>The new drawer flow is complete, shared-room-safe, and fully verified against the existing regression suite and build boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cmd /c npm test
- [ ] cmd /c npm run build
- [ ] Existing inventory dock still opens the drawer
- [ ] Sandbox mode can route to `Pet Care` directly when cats need attention
- [ ] Shared-room mode does not expose local multi-cat care controls
</verification>

<success_criteria>
Phase 11 now ends with a complete player-shell flow: the drawer is split, themed, reachable, truthful to runtime boundaries, and regression-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/11-player-shell-inventory-shop-and-pet-care-overhaul/11-03-SUMMARY.md`
</output>
