# Quick Task 260328-46n Summary

Status: completed
Date: 2026-03-28

## Findings
- `useSandboxWorldClock` was publishing Minecraft time every tick and pushing unnecessary top-level rerenders through `App.tsx`.
- `App.tsx` recreated the full room stage and drawer nodes on ordinary shell updates.
- Each `RoomPetActor` rebuilt the same obstacle list independently.
- Hovering one interactable furniture item pushed a shared hover id through every furniture actor.
- GPU-heavy post-processing and shadow settings stayed fully enabled even in build mode or dense scenes.

## Changes
- Reduced clock state churn in `src/app/hooks/useSandboxWorldClock.ts` by publishing visible minute changes instead of every tick and by avoiding repeated local-time state updates.
- Memoized room-stage, preview-stage, and inventory-stage inputs in `src/App.tsx` and memoized `src/app/components/AppRoomStage.tsx`.
- Hoisted pet obstacle generation into `src/components/RoomView.tsx` and passed a shared obstacle list plus player-position ref into `src/components/room-view/RoomPetActor.tsx`.
- Memoized `RoomFurnitureActor`, `RoomFurnitureLayer`, and `RoomSelectedFurnitureLayer`, and removed global hover-id prop churn from `RoomInteractionProxy` / `RoomMemoryFrameProxy`.
- Added a safe render-quality throttle in `src/components/room-view/useRoomViewLighting.ts` for build mode and dense scenes.
- Cleared unrelated literal newline corruption in `src/lib/devLocalState.ts` so the repo built cleanly again.

## Verification
- `cmd /c npm test`
- `cmd /c npm run build`