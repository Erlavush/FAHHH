# Quick Task 260328-l0i: Replace Desk PC Runner With Pacman

**Created:** 2026-03-28
**Status:** Ready for execution

## Goal
Replace the current desk PC Runner app with a Pacman app that feels native to the existing retro desktop shell, keeps the current payout/progression hooks intact, and removes runner-specific runtime/UI code.

## Constraints
- Preserve the existing desk PC entry flow, reward completion callback, and replayable desktop UX.
- Update shared/local activity ids cleanly instead of leaving `pc_runner` semantics behind.
- Keep backward compatibility for saved/shared progression that may still contain `pc_runner` claims or app progress.
- Use shell-only file editing in this worktree.
- Treat `https://github.com/SteveDunn/Pacman-TypeScript` as the gameplay/reference source and avoid blindly dropping its standalone app structure into the React shell.

## Tasks

### 1. Add a dedicated Pacman board/runtime
- Create a focused Pacman board component and supporting CSS/helpers instead of growing `PcMinigameOverlay.tsx` further.
- Implement a playable maze, pellets, power pellets, ghosts, collisions, controls, and score/lives state inside the desk PC session contract.
- Verify the component mounts cleanly from the desktop shell and exposes an accessible board container for tests.

### 2. Swap Runner wiring to Pacman across the app and progression layer
- Replace runner labels, app definitions, reward mapping, claim reasons, and activity ids with Pacman.
- Add normalization/migration support so older `pc_runner` activity records are treated as Pacman rather than dropped.
- Update any player-facing status rows so the desktop and room shell both call the third app Pacman.

### 3. Regressions, docs, and quick-task tracking
- Update desk PC tests and shared progression/runtime tests to assert Pacman behavior and the renamed activity id.
- Run `cmd /c npm test` and `cmd /c npm run build`.
- Record the quick-task summary and update `.planning/STATE.md` once the implementation is verified.
