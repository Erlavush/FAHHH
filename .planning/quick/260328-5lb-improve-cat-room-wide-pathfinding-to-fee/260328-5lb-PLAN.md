# Quick Task 260328-5lb: Improve cat room-wide pathfinding to feel more like a real Minecraft cat

Status: completed
Date: 2026-03-28

## Goal
Make cats navigate the full room with less repetitive target selection and more believable pacing without breaking existing pet behavior states or shared-room syncing.

## Tasks
1. Add a room-wide pet navigation graph and waypoint routing in the pathing helpers.
2. Rework `RoomPetActor` cat movement to use route memory, route-specific speeds, and variant-safe cat detection.
3. Cover the new routing behavior with tests and rerun `cmd /c npm test` and `cmd /c npm run build`.

## Verification
- `cmd /c npm test`
- `cmd /c npm run build`